---
sidebar_position: 98
---

# Research Framing: Chapter 5

> This document is for researchers and curriculum designers. For the
> curriculum-wide research orientation, see the
> [global research framing](../research-framing.md).

**Owner**: Research committee

---

## Abstraction Transition Taxonomy

The exercise design within the representation sequence is informed by the
Abstraction Transition Taxonomy (Cutts et al.,
[`./assets/abstraction-transition-taxonomy.pdf`](./assets/abstraction-transition-taxonomy.pdf)).
The paper identifies three abstraction levels — English, CS Speak, and Code —
and argues that the _transitions between them_ are the core skill, not just
operating within a single level. Our representation sequence is a richer
gradient across the same spectrum:

- **Code level**: traces, actual programs
- **CS Speak level**: pseudocode, decision tables, loop invariants, state
  transitions, flowcharts, pattern schemas
- **English level**: prose strategy descriptions

Two key insights from the taxonomy shape how exercises are designed:

**1. Adjacent vs. distant transitions.** Moving between neighboring
representations (code → prose, pseudocode → decision table) is easier than
jumping across multiple levels (code → loop invariant, flowchart → pattern
schema).

**2. Both "how" and "why" at every level.** Each representation transition has
two forms: "how" (perform the transition) and "why" (explain why your answer is
correct). "Why" questions are critical for deep comprehension but represent less
than 15% of typical assessments.

## PRIMMy Scaffold (Code Ownership Progression)

The fading scaffold (Study → Fill in blanks → Fix bugs → Write chunks → Empty
file) adapts the PRIMM framework (Predict, Run, Investigate, Modify, Make) for
this curriculum's comprehension-first approach.

_PRIMM citations (Sentance et al.) to be documented here._

## Block Model

The Block Model assessment framework (Schulte 2008) applies to both
human-written and LLM-generated code. Its 4×3 grid
(Atoms/Blocks/Relationships/Macro Structure × Text Surface/Program
Execution/Function-Purpose) provides specific, assessable dimensions of code
understanding.

_Full Block Model citations to be documented here._
