/**
 * Sidebar for welcome-to-programming.
 * Autogenerated from directory structure — chapter visibility is controlled
 * via the `exclude` array in docusaurus.config.mjs, not here.
 *
 * To hide individual files within a chapter: add `draft: true` to the
 * file's front-matter (visible in dev with banner, absent in production).
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
	welcomeSidebar: [
		{
			type: 'autogenerated',
			dirName: '.',
		},
	],
};

export default sidebars;
