<!-- TRANSITIONAL — retire together with SPEC.md (see its banner); this
ledger is the transport's omission record and travels with the transported
content. -->
<!-- cspell:ignore socratizing socratize quizzing reenrichment stonebraker -->
<!-- cspell:ignore tombstoned unbuilt relitigate PBSI -->

# Canon re-home — loss ledger

Per DEV.md § Documentation migration discipline: the R-1/R-1a transport of
`~/.claude/plans/read-and-execute-the-indexed-pony.md` (344 lines),
`…-the-playful-stonebraker.md` (407 lines), and
`…/supplement-indexed-pony-scope-gotchas.md` (59 lines) into
[SPEC.md](./SPEC.md) is verbatim-by-default; every omission, merge, or reword is
enumerated here with its justification. Method: heading-by-heading walk over all
three sources (AR-1 on C2 rejected the first recollection-built draft of this
ledger; this is the rebuilt version). All three files read end-to-end this
session.

## indexed-pony, by heading

- **Title + intake preamble (lines 1–19)** — OMITTED: fresh-session launch
  framing ("Your first move", "You own this campaign", repo/branch banner).
  Session mechanics, not canon. The durable ordering survives as the stage
  table's executed rows.
- **§ What we're building** — TRANSPORTED (SPEC § What is being built),
  including the ~15k LOC sizing and the engine/local-llm migration precedent
  (restored after AR-1 flagged their omission).
- **§ The quarry is READ-ONLY** — TRANSPORTED.
- **§ Read first (1–3)** — item 1 (governance chain) reduced to one pointer line
  per its own "do not restate — point" rule; item 2's per-file greenfield
  pointers REPLACED by SPEC § Greenfield contracts' links (the volatile line
  numbers dropped); item 3's per-stage quarry source paths TRANSPORTED into the
  stage-table cells (restored after AR-1 flagged the omission).
- **§ Locked decisions** — TRANSPORTED (all four), with decision 2 updated from
  "a flip to make" to landed fact (measured) and decision 4 gaining the
  registry-excision warning (NEW, from re-verification).
- **§ Scope adapter** — MERGED with stonebraker's Stage-2 material and REWRITTEN
  to landed reality: the shipped adapter reads embody's access classification
  and never re-walks the AST, so the source's ~50-LOC sketch, its "fiddly ~20
  LOC recompute" framing, and the AST-position rule are OMITTED as
  superseded-by-implementation (AR-1 concern 4; Q3 row records the
  supersession). The 401-LOC replaced-builder figure and the `getChildNodes`
  micro-vendor sanction survive (the latter past-tensed as executed precedent,
  relocated to socratizing — AR-2 concern 3: it never applied to quizzing).
- **§ Greenfield contracts** — SHRUNK to the campaign-specific delta (envelope
  idiom, gate/refusal constraints, the `Snippet → Facts` remap, roster state,
  purity rule) with authoritative shapes pointed at in the tree (AR-1
  counter-proposal B). The full `Lens`/`Gateable`/ `LensProperties`/`Facts` type
  restatement is OMITTED as restatement of `lenses/types.ts` +
  `embody/types.ts`; the three behavioral constraints (throwing gate, no phase
  discriminator crossing, composition-root naming guard) are KEPT (restored
  after AR-1 flagged their loss). The "roster looks host-injected (R6)" claim is
  REPLACED by the measured resolved state (AR-1 concern 3 / AR-2 concern 1).
- **§ Execution mode** — TRANSPORTED, including the light-where-verbatim Phase-0
  calibration (restored after AR-1 flagged its strengthening into uniform
  "full").
- **§ Campaign stages table** — MERGED with stonebraker's forward map; statuses
  updated to measured 2026-08-06 reality; Stage-3 cell gains the R-6 annotation
  (NEW ruling) and the supplement's file addresses.
- **§ Socratize DDD rewrite** — TRANSPORTED whole (KEEP/DELETE/CHANGE/RE-TYPE),
  with the R-4 un-colorized KEEP item added (NEW ruling, marked); PBSI
  expansion + American-spelling caution and the component structure restored
  after AR-1 flagged their omission.
- **§ Quiz port** — TRANSPORTED; the linked-origami pointer REPLACED by the
  surviving sources (the target was deleted before transport [measured: `ls
  ~/.claude/plans | grep -i origami` → empty]).
- **§ Flagged bits** — DISSOLVED into their owning sections: offset blast radius
  → executed (decision 2 landed); scope-adapter recompute → § Scope adapter;
  realm coupling elsewhere → Q12 row; quiz gate projection → Q4 row (which also
  gains the path-less-reference and gate-budget ratification items, NEW from
  this campaign's reviews).
- **§ Open questions A/B/C tiers** — the 14 questions TRANSPORTED as the
  resolved-questions table; the tier structure COMPRESSED to one preamble
  sentence (all A-tier resolved; every open row is per-stage B-tier; C-tier
  confirm-only rows marked Executed/superseded). "Never guess a BLOCKING one"
  survives as "no BLOCKING-tier question remains".
- **§ Governance (every stage)** — OMITTED as governance restatement (point,
  don't restate — its own rule). Campaign-specific residue kept: the
  baseline-delta-only quality gate and sandbox expectations (§ Verification),
  the light-Phase-0 calibration (§ stages), the Opus-4.8 trailer dropped
  (per-session detail; governance carries the rule).
- **§ Verification** — TRANSPORTED.
- **§ Housekeeping** — OMITTED: executed 2026-07-22 per its own text ("Nothing
  to prune or reconcile").

## stonebraker, by heading

- **Plan-of-record preamble** — OMITTED (session framing); its supplement
  citation is what surfaced the third source file (ruling R-1a).
- **RESUMPTION POINT** — SPLIT (AR-1 concern 5 rejected the first draft's
  blanket "historical" justification): the Stage-1 status + SHAs survive in the
  stage table; the HEAD-specific push-gate warning is historical; the
  **cspell-before-push obligation is STILL LIVE** and is carried in SPEC §
  Verification (both preconditions re-measured: the Stage-1 commits are not on
  origin/main; node is v20.11.0).
- **§ Stage-1 learnings** — TRANSPORTED (four gotchas), minus the "R1 did not
  fire" historical note.
- **§ Stage-2 cold-start specifics** — mostly executed; the live residue
  TRANSPORTED: the Q13/Q14/Q1/Q8 resolutions (Q-table), the
  entry-narrows-`.ok`-first rule (§ Stage-1 learnings, restored after AR-1
  flagged it), the offset-flip greenfield-native rule (landed, stage table). The
  read/write METHOD paragraph is OMITTED as superseded-by-implementation (same
  disposition as indexed-pony § Scope adapter).
- **§ Context / § Locked decisions** — duplicate of indexed-pony content;
  merged.
- **§ Campaign shape (mermaid DAG)** — DROPPED as a drawing; the live dependency
  content (3 → 4 → 5; Stage 5 ← Stages 1+3; Stage 4 ← landed Stage 2) survives
  as prose. Rationale: two of five nodes and both adapter edges are historical;
  the remaining graph is two live edges — below diagram threshold (AR-2
  concurred; edge count corrected from "one" to "two" per its review).
- **§ Governance ceremony** — OMITTED as governance restatement (same
  disposition as indexed-pony's).
- **§ STAGE 1 (all Phase-0/1/2 checklists + verified-facts block)** — OMITTED:
  executed increment. Its durable outputs (public shape Q8, the 142-test oracle
  fact) survive in the Q-table and Stage-1 row.
- **§ SOCRATIZE DDD** — TRANSPORTED into SPEC § Socratize DDD (merged with
  indexed-pony's copy; stonebraker's is the more detailed source for the core.ts
  surface, PBSI note, and component structure); the "author as drafts / do not
  commit — Stage-2 gated" immediate-steps block is OMITTED as superseded by
  ruling R-5 (copies, not fresh authoring).
- **§ Forward map (Stages 2/3/5)** — MERGED into the stage table; the
  linked-origami pointer replaced (see above); the R6 roster flag resolved (see
  above).
- **§ Verification** — merged with indexed-pony's.
- **§ Housekeeping / risks** — the risk register TRANSPORTED in residue form
  (restored after AR-1 flagged its omission): R1 tsc first-exposure → § Stage-1
  learnings byte-copy bullet; R2 docs re-authored → same section; R3 (types.ts
  gating) → dissolved, Stage 2 landed; R4 span-pick → § Socratize DDD named open
  question; R6 roster → resolved by measurement. The memory-write instruction is
  OMITTED (executed).

## supplement, by heading (R-1a — added mid-C2 when AR-1 surfaced it)

- **§ 1 buildScope three-way homonym** — TRANSPORTED (SPEC § Terms).
- **§ 2 three embody trees** — TRANSPORTED (SPEC § Terms).
- **§ 3 free vs scope-forest generators + file addresses** — TRANSPORTED
  (Stage-3 cell).
- **Closing one-liners** — the ignored-`_scope`-param fact TRANSPORTED (§ Scope
  adapter); the "frozen at `0fca239`" claim OMITTED as STALE (the quarry has
  moved since — the maintainer's sanctioned prettier sweep `59043f52` (SPEC §
  The quarry is READ-ONLY); copies verify against current HEAD blobs instead);
  the "live M2 owner — coordinate" note OMITTED as superseded by Q1 (marked in
  the Q1 row).

## Explicitly NOT omitted (checked against the audit's loss list)

The six socratize-spec losses (CM recipe, `data-socratize-*` contract,
tabbed-panel contract, reveal-persistence, midsize routing, shelf rationale) are
NOT transported into SPEC.md prose — they survive whole in the annotated
byte-copies ([lens-ddd/socratize/](./lens-ddd/socratize/), land at C5), which
SPEC.md's Stage-4 row and § Socratize DDD point at. Same for the quiz-side
test-pinned contracts, which [LIBRARY-CONTRACTS.md](./LIBRARY-CONTRACTS.md) pins
(lands at C3).
