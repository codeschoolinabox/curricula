<!-- TRANSITIONAL — this lens's fidelity ledger. Retires with SPEC.md, but only
once every row below carries a resolving `discharged by`. -->
<!-- cspell:ignore socratize dropdowns writeme parsons colorizing blankenate parsonizer -->
<!-- cspell:ignore colour distractor distractors ledgered throughs unrebutted -->
<!-- cspell:ignore Haden Explorotron underscored gsub RSTART RLENGTH oldd clauding -->
<!-- cspell:ignore firstblock glossterm behaviour -->
<!-- cspell:ignore affordances colours recognised recognises flexbox rgba srcdoc -->
<!-- cspell:ignore parsonize unclickable -->
<!-- quoted verbatim from Gen-2 sources; do not "fix" the spelling: -->
<!-- cspell:ignore misordered lise -->
<!-- quoted verbatim from Gen-1's second root; do not "fix" the spelling: -->
<!-- cspell:ignore intead descore jsparson unittests vartests Codeline -->
<!-- cspell:ignore dequery distrators codestring toggleable -->
<!-- cspell:ignore nocite idlist sortables -->
<!-- Finnish, quoted verbatim from Gen-1's translation table: -->
<!-- cspell:ignore Raahaa rivit ohjelmaasi tästä -->

# `parsons` — fidelity ledger

Method: [FIDELITY-METHOD.md](../FIDELITY-METHOD.md), read in full before the
first row. Scope and disposition: [SPEC.md](../SPEC.md).

> **PASS 1 — SEEDED, NOT AUDITED.** Every row below is OPEN by construction.
> Every `provenance` set carries `UNSETTLED`. **This ledger makes no fidelity
> claim**, and **it must not be handed to a lens session**:
> [SPEC.md § The two handoff tiers](../SPEC.md#the-two-handoff-tiers) requires a
> completed ledger. Those three sentences bind **all 120 rows**.
>
> ⚠️ **The rest of the Pass-1 disclaimer is STRUCK, not qualified** (human
> ruling 2026-08-20). ~~Its thinness or thickness is a property of the
> instruments, not a finding — do not read a row's absence as evidence of
> anything.~~ — **false of 73 of the 120 rows.** Only `001`–`047` came from
> [§ Pass 1](../FIDELITY-METHOD.md#pass-1--mechanical-seeding)'s listers; **68
> came from Pass 2 and 5 from Pass 3**, whole-file reads by fresh agents, which
> is a finding-producing instrument and not a grep. For those, thinness **is** a
> finding and an absence **is** evidence.
>
> **[§ Seed census](#seed-census)'s `pass` column is the authority on which pass
> produced which id range. This banner is not**, and it survives only because
> [`_TEMPLATE.md` § Close conditions](./_TEMPLATE.md#close-conditions)'s gate
> requires the literal string `PASS 1 — SEEDED` present exactly once. **That
> gate is therefore reporting a pass state this ledger left two sessions ago**,
> and fixing it is a template amendment, not a ledger edit.
>
> **What still binds for the whole ledger**: every row is open, every
> `provenance` set is `UNSETTLED`, no disposition is filled, and the handoff
> prohibition stands — Pass 2 has **not** run over the Gen-2 and Gen-3
> documents, and Pass 3 has run once, on one file, with its own reader reporting
> it is not discharged.

**Row ids are stable forever.** Append; never renumber, never re-sort. Handoffs
cite these ids, and a renumber silently re-points every citation.

**This ledger is the campaign's row-style reference** — the exemplar the other
seven are cut against. What it demonstrates is mostly **restraint**: **120
rows**, zero dispositions [measured 2026-08-20, row-scoped]. ⚠️ **This number
has gone stale three times — `47` → `115` → `120` — and twice inside a commit
whose own subject or heading announced a stale-number repair.** Re-measure it;
never transcribe it from here. `parsons-010` is the clearest case — it is
[FIDELITY-METHOD § Worked rows](../FIDELITY-METHOD.md#worked-rows)' own
`parsons-018`, whose disposition the method hands you outright
(`restore-as-doc`), and this ledger leaves the cell **empty** anyway, because
Pass 1 closes none. Its id is `parsons-010` and not `parsons-018`: the worked
ids are illustrative and not reserved (human ruling 2026-08-14).

---

## Reference inventory

Pasted from one run, not retyped.

```bash
REF=src/lib/study-lenses--deprecated-architecture/lenses/parsons
if [ -d "$REF" ]; then
  for f in README.md DOCS.md; do echo "== $f"; grep -nE '^#+ ' "$REF/$f"; done
  echo "== types.ts"; wc -l "$REF/types.ts"
  grep -nE '@remarks|^type |^\s+readonly ' "$REF/types.ts"
else
  echo "no Gen-2 reference — $REF does not exist"
fi
```

```text
== README.md
1:# lenses/parsons
37:## Public API
86:## Why this lens exists
122:## Glossary
167:## UI structure
244:## Pool + solution contract
262:### Interaction contract (native HTML5 DnD)
295:## Indent contract (when `canIndent`)
327:## Feedback contract (Check)
404:## View contract
425:## Feedback legend
437:## Hint blocks (educator `/* … */` guidance)
468:## Distractor-count hint
481:## Attempt history
507:## Edge cases
542:## What this lens does NOT do (lens-specific drops only)
581:## Two-layer module
641:## Dependencies (no install needed)
647:## Future direction
675:## Conventions inherited
692:## Navigation
== DOCS.md
1:# parsons — Architecture & Decisions
3:## Why this module exists
26:## Migration
53:## Modules
78:## Architectural sketch
85:### Execution phases
165:### Data flow
219:### Structural constraints
311:### Out of scope
336:## Phase-8 additions + browser-checkpoint reconciliations
431:## Why grade per-line independently (vs. the legacy sequential gate)
447:## Why a pure `arrange.ts` reducer (vs. inline drag handlers)
460:## Why restore the original LIS selection (vs. copying the parsonizer bug)
482:## Module ownership
492:## Future direction
== types.ts
     298 src/lib/study-lenses--deprecated-architecture/lenses/parsons/types.ts
30: * @remarks
69: * @remarks
101: * @remarks
129: * @remarks
148: * @remarks
166: * @remarks
198: * @remarks
212: * @remarks
250: * @remarks
277: * @remarks
```

⚠️ **The inventory command's type-declaration arm returned nothing, and that is
a real result rather than a broken command:**

```bash
grep -cE '^type |^\s+readonly ' "$REF/types.ts"   # -> 0 [measured 2026-08-15]
```

`parsons/types.ts` declares its members through `export type` and non-`readonly`
fields, so the type-contract universe that arm is supposed to enumerate is
carried entirely by the **10 `@remarks` blocks** above. Pass 2 reads the file in
full regardless; this note exists so a reviewer does not read the empty arm as a
skipped step. (The regex is shown in a fence rather than inline because both its
alternatives end in a significant space, which `MD038` forbids inside a code
span and prettier is free to wrap.)

**The port side**, which listers 1–3 diff against [measured 2026-08-15: `grep
-nE '^#+ '` over `src/lib/study-lenses/lenses/parsons/`]:

```text
== README.md            == DOCS.md
3:# parsons             3:# parsons — Architecture & Decisions
29:## The lens object    10:## Modules
49:## Configuration      36:## Execution phases
65:## UI structure       84:## Data flow
117:## Interaction contract (native HTML5 drag-and-drop)
138:## Indent contract (when `canIndent`)
150:## Feedback contract (Check)
187:## View contract      115:## Structural constraints
196:## Attempt history    141:## Decisions
206:## Edge cases         184:## Out of scope
221:## Future direction   194:## Navigation
234:## Navigation
```

**Gen-1 source**, for the provenance negative every non-G1 row owes
([§ The exemption needs evidence too](../FIDELITY-METHOD.md#the-exemption-needs-evidence-too)):
`ParsonsLens.jsx` **181** lines + `ParsonsLens.module.css` **664** lines, in the
Gen-1 tree [measured 2026-08-15: `wc -l`]. The `.jsx` is a thin `<iframe>` shell
over a vendored jQuery js-parsons port; the stylesheet describes a native board
it never rendered — which is what makes lister 4 the load-bearing instrument
here.

**Instruments that could run**, matching this lens's row in
[SPEC.md § Roll-up](../SPEC.md#roll-up): **listers 1–5 over `src/lenses/` only,
plus a Pass-2 whole-file read of `public/static/parsonizer/`.** All five listers
ran over the `ParsonsLens.jsx` / `.module.css` pair. **No lister has run, or may
run, over Gen-1's second root** — lister 4 recognises 1 class of that
stylesheet's 17 and reports it as an orphan when it is not one, and lister 5
channel B greps React idioms a jQuery file cannot contain, so its zero is
vacuous [measured 2026-08-15, recorded at
[SPEC.md § Gen 1's second root](../SPEC.md#gen-1s-second-root--the-lens-file-is-often-only-a-shell)].
Building the widened lister is design work and remains embargoed.

**A hand-read is not a lister result**, and that is the distinction this
ledger's second-root rows rest on. SPEC's ruling embargoes recording _a lister
result_ for `public/static/`; it puts the root itself in scope and says only the
instrument is missing. The precedent is `trace-debugging`, which
[SPEC.md § Roll-up](../SPEC.md#roll-up) already records as seeding **entirely
from Pass 2 — a whole-file read** — for the same reason, one root over.

~~That is why this ledger carries **zero `G1-live` rows** while its own
`parsons-010`, `-028` and `-031` quote `component.js` and `lis.js` by name.~~ —
**struck 2026-08-19.** Rows `048`–`080` were opened by a whole-file read of the
second root, one fresh agent per file, 3073 lines across seven files; see
[§ Seed census](#seed-census). The three rows that quoted `component.js` and
`lis.js` by name now sit beside rows cut from those files.

⚠️ **What the read found about `component.js` bears on `parsons-010` and is not
this pass's to settle.** Gen-2 README § Hint blocks says the feature was
_"Ported faithfully from the legacy JSParsons parsonizer (`component.js`)"_. The
feature is unambiguously in that file — and the deployed host **never loads
it**: `parsons-iframe.html` pulls eight assets and `component.js` is not among
them [measured 2026-08-19: `grep -c 'component\.js'` → **0**]. The two paths
also do opposite things with an educator's block comments — `component.js` lifts
them out and renders them above the puzzle (`parsons-074`); the deployed host
strips and discards them (`parsons-059`). Recorded as rows, adjudicated by
Pass 2.

Lister 5's zero over `src/lenses/` is separately a **measured zero** rather than
a limit — see [§ Seed census](#seed-census).

---

## Source inventory

### Lister 4 — orphan CSS

Run verbatim from
[`_TEMPLATE.md` § Lister 4](./_TEMPLATE.md#lister-4--orphan-css) with `<Lens>` =
`ParsonsLens` [measured 2026-08-15]. **27 orphans of 37 defined classes**,
reproducing FIDELITY-METHOD § 4's published count exactly, in **3 clusters**:

```text
(prologue)	blockContent          Modal styles	guess-entry
(prologue)	blockHeader           Modal styles	guess-status
(prologue)	blockNumber           Modal styles	parsons-modal
(prologue)	blockType             Modal styles	parsons-modal-close
(prologue)	blocksContainer       Modal styles	parsons-modal-content
(prologue)	blocksPanel
(prologue)	checkButton           SL1 Parsonizer Integration	codeContainer
(prologue)	codeBlock             SL1 Parsonizer Integration	parsons-fallback
(prologue)	controls
(prologue)	dropMessage
(prologue)	emptyMessage
(prologue)	exerciseContent
(prologue)	feedback
(prologue)	hint
(prologue)	hintButton
(prologue)	insertZone
(prologue)	resetButton
(prologue)	solutionBlock
(prologue)	solutionContainer
(prologue)	solutionPanel
```

⚠️ **The partition is reproducible, not semantic.** The stylesheet carries 9
column-0 banners but the first is at line 287, so **20 of the 27 orphans fall
before any banner** and land in one undifferentiated `(prologue)` cluster
[measured 2026-08-15: `grep -n '^/\*' ParsonsLens.module.css` → 9 banners, first
at 287]. Reading the names, `(prologue)` plainly contains at least a board
(`blocksPanel`, `solutionPanel`, `insertZone`, `dropMessage`), a control bar
(`hintButton`, `resetButton`, `checkButton`, `controls`) and a feedback pair
(`hint`, `feedback`) — but **that reading is Pass 2's**, and Pass 2 splits
`parsons-045` by appending ids, never by renumbering.

**Both direct checks, run and recorded whether or not they found anything** —
FIDELITY-METHOD § 4 calls the counts a lower bound, but computed access and
kebab-case names make the error run in **both** directions:

- **Computed access:** none. [measured 2026-08-15: `grep -c 'styles\['
  ParsonsLens.jsx` → **0**]. So no orphan here is a false positive by that route
  — unlike `BlanksLens.jsx:735` and `WritemeLens.jsx:710`, which do use it.
- **Kebab-case orphans — six, and the lister cannot verify any of them**, since
  `styles.parsons-modal` is not expressible: `guess-entry`, `guess-status`,
  `parsons-fallback`, `parsons-modal`, `parsons-modal-close`,
  `parsons-modal-content`. Each direct-checked: **all six are absent from the
  `.jsx` in every form** [measured 2026-08-15: `grep -c
  "guess-entry\|guess-status\|parsons-fallback\|parsons-modal" ParsonsLens.jsx`
  → **0**], so all six are true orphans. A fact someone checked, which is the
  point.

### Lister 5 — switched-off code

**Channel B — zero, and it is a measured zero** [measured 2026-08-15]:

```bash
grep -nE '^// *export const (render|execute|renderConfig)' ParsonsLens.jsx  # (none)
grep -n '{false &&' ParsonsLens.jsx                                        # (none)
```

`ParsonsLens` has no suppressed exports and no falsy render guards. It is one of
the Gen-1 render lenses where channel B is silent — unlike `BlanksLens` (one
guard, hiding ~190 lines of hints UI) or `TracingLens`/`StepThroughsLens` (whose
`render` exports are commented out entirely). **Channel B therefore opens no
rows in this ledger.**

<details>
<summary>Channel A — candidate lines for Pass 2. A reading list, not a count.</summary>

```text
16   // Get current content from enliven file
23   // Build iframe URL with encoded code
38   // Handle iframe load
44   // Handle iframe error
50   // Fallback component for when iframe fails or code is empty
70   // Get iframe URL
73   // Show fallback if no code or URL generation failed
137  // sandbox="allow-scripts allow-same-origin"
170  // Parsons lens - unified interface
```

Eight of these nine are ordinary section comments. **Line 137 is not**, and it
is exactly the case FIDELITY-METHOD § 5 names as the reason channel A cannot be
counted: a commented-out iframe `sandbox` attribute is a security-relevant
suppression that "no reasonable code-shape regex catches, because the line looks
like prose". It is carried here **for Pass 2 to adjudicate**, and it opens no
row now.

</details>

### Seed census

**The `pass` column is load-bearing and is read before the banner** (human
ruling 2026-08-20). This ledger's rows were not all produced by the same pass,
so a document-level pass marker cannot be true of it; the marker is per cohort,
here.

| pass  | instrument                                      | hits                     | rows opened | ids       | note                                                                      |
| ----- | ----------------------------------------------- | ------------------------ | ----------- | --------- | ------------------------------------------------------------------------- |
| **1** | lister 1/3 README                               | 21 headings              | **19**      | `001–019` | less the H1 title; less `## Glossary`, whose terms seed at `034–044`      |
| **1** | lister 1/3 DOCS                                 | 15 headings              | **14**      | `020–033` | less the H1 title                                                         |
| **1** | lister 2                                        | ref 5 · port **0**       | **0**       | —         | every `^#+ Why` is already a lister-1 row; lister 2 marks them, adds none |
| **1** | lister 3 glossary                               | 11 terms                 | **11**      | `034–044` |                                                                           |
| **1** | lister 4                                        | 27 orphans               | **3**       | `045–047` | clustered by the stylesheet's own banners                                 |
| **1** | lister 5 channel B                              | **0**                    | **0**       | —         | measured zero, not an instrument limit                                    |
| **1** | lister 5 channel A                              | list of 9                | **0**       | —         | carried above for Pass 2; never counted                                   |
| **2** | **second root — whole-file read, NOT a lister** | 7 files · **3073** lines | **33**      | `048–080` | one fresh agent per file, each reading end to end; see below              |
| **2** | **the core re-read — NOT a lister**             | 2 files · **1506** lines | **35**      | `081–115` | four fresh readers, briefed on the FILE only; see below                   |
| **3** | **the counter-read — question 1**               | `parsons.js` · 6 named   | **5**       | `116–120` | one contaminated reader; it also corrected `parsons-097`, see below       |
|       |                                                 |                          | **120**     | `001–120` | **Pass 1: 47 · Pass 2: 68 · Pass 3: 5**                                   |

**The second-root read, per file** [all line counts measured 2026-08-19:
`/usr/bin/wc -l`]. Vendored jQuery — four minified files — was excluded as
library rather than pedagogy:

| file                                              | lines    | rows opened      |
| ------------------------------------------------- | -------- | ---------------- |
| `public/parsons-iframe.html`                      | **586**  | `048`–`060` (13) |
| `public/static/parsonizer/parsons.js`             | **1367** | `061`–`070` (10) |
| `public/static/parsonizer/lis.js`                 | **148**  | `071`–`072` (2)  |
| `public/static/parsonizer/component.js`           | **574**  | `073`–`077` (5)  |
| `src/lenses/ParsonsLens.jsx`                      | **181**  | `078`–`080` (3)  |
| `public/static/parsonizer/parsons.css`            | **139**  | 0 — see below    |
| `public/static/parsonizer/parsonize-selection.js` | **78**   | 0 — see below    |
|                                                   | **3073** | **33**           |

**The core re-read, per file** [line counts measured 2026-08-19: `/usr/bin/wc
-l`]. STEP 1a's rows fell on the wrong side of the nesting doll — **23 of its 33
came from files other than `parsons.js`, and `parsons.js` itself got 10**
[measured 2026-08-19, from the per-file table above] — so this pass points the
same instrument at that one file. ⚠️ An earlier revision said "21 … and the core
got 12". That reconciles only by counting `lis.js` into "the core" while the
same sentence defines the core as "the 1367-line core", i.e. `parsons.js` alone.
**Struck 2026-08-20**, found by AR-1; name the file set every number covers.
**Four readers, three of them on `parsons.js` with three different questions**,
each reading the whole file end to end; that is perspective-diversity, not a
split, and invariant 9 holds.

| file                                   | lines    | readers | rows opened              |
| -------------------------------------- | -------- | ------- | ------------------------ |
| `public/static/parsonizer/parsons.js`  | **1367** | 3       | `081`–`107`, `113`–`115` |
| `public/static/parsonizer/parsons.css` | **139**  | 1       | `108`–`112`              |
|                                        | **1506** | 4       | **35**                   |

⚠️ **SIX rows carry a claim only a file the readers were forbidden to open can
settle**, and that adjudication is the seeder's work rather than a reader's:
`parsons-084`, `-086`, `-090`, `-092`, `-098` and `-105` each rest on what
`parsons-iframe.html` configures, and `parsons-084` additionally on the vendored
`jquery.min.js`. All three `parsons.js` readers found that loop's missing bound
and all three declined to state its consequence — correctly, since settling it
needed files none of them was allowed to open. ⚠️ An earlier revision of this
paragraph declared **two**. **Struck 2026-08-20**, found by AR-1; the disclosure
now lists every row carrying a host-derived claim, because an undeclared
cross-file premise is the thing this paragraph exists to prevent.

⚠️ **AUTHORING AND SOLVING ARE ONE SURFACE, and rows `081`–`089` are cut on that
reading** (human ruling 2026-08-19): _"educators should be able to write those
in as well then configure the codeblock for parsons, or learners from the editor
(it's the same thing really)."_ The evidence was already in the ledger —
`parsons-048` builds the puzzle from the URL's `code` parameter and
`ParsonsLens.jsx` encodes the editor's source into it — so **a learner editing
code and opening this lens is authoring their own exercise, distractors
included.** "Educator" therefore names a **stance**, not a second person, and a
row in that voice is not a row about somebody else. This is why the authoring
rows carry `author` where `048`–`080` carry `educator`; **the two words are one
role and the vocabulary is owed a reconciliation** (AR-1, 2026-08-19).

**How the readers were briefed, because it is the pass's own method claim:**
each brief named its file, its question and its return shape, and named **no
prior finding, no row id, no count, and no earlier read**. A completeness check
over three mechanical sets ran **after** the returns landed, so it could not
reach a reader, and it found **zero uncovered members** [measured 2026-08-19].
**Publish the number by publishing the command** — the sets are what the three
below return, and a bare `grep -c function` returns 97 against the third one's
68, so the number means nothing without its form:

```bash
export LC_ALL=C
GEN1="/Users/master/Documents/0-teach-code/0-spiralearn/0-study-lenses-committee/zz--oldd-clauding-and-context-dump/spiral-lens"
F="$GEN1/public/static/parsonizer/parsons.js"
/usr/bin/grep -oE '\b[A-Z][A-Za-z_0-9]*\.[a-zA-Z_$][a-zA-Z_$0-9]*' "$F" | sort -u          # 16
/usr/bin/grep -oE 'this\.[a-zA-Z_$][a-zA-Z_$0-9]*[[:space:]]*=' "$F" | sort -u             # 17
/usr/bin/grep -cE '^[[:space:]]*(function [A-Za-z_$][A-Za-z_$0-9]*|[A-Za-z_$.]+\.prototype\.[A-Za-z_$0-9]+[[:space:]]*=[[:space:]]*function|[A-Za-z_$][A-Za-z_$0-9]*[[:space:]]*:[[:space:]]*function)' "$F"   # 68
```

The three affordance families the campaign had flagged as missing were found by
the readers **unprompted**, so the backstop was never needed [relayed: the four
readers' returns — a reader's return cannot be re-run].

**Remainders from the core re-read — the surplus, named rather than dropped.**
The four readers returned roughly **331** candidates and this pass opened 35
[relayed: the readers' own counts]. **No ratio is published**: that denominator
is a reader's self-count of how it chunked its own findings, which is the
unstable-instrument class this campaign already strikes twice. What is published
is the named surplus:

- **Code-quality observations that are not affordances** — dead local variables
  in `_getToggleStates`, `hashToIDList` and the sortable `receive` handler; a
  discarded return value from `updateIndentsFromHash`; four call sites passing a
  second argument to a one-parameter `addLogEntry`; loose-versus-strict equality
  in adjacent grader comparisons; a `for…in` over arrays. Real, and none of them
  is something a learner, an author or a reader can do, see or know.
- **An implicit global.** `new_line` is assigned twice with no declaration
  keyword and the file has no `'use strict'`, so it leaks onto `window`. A
  property of the page, not an affordance.
- **A latent index confusion in save-and-restore.** `hashToIDList` treats a
  line's `orig` — its index in the authored text — as an index into
  `modified_lines`, which holds solution lines followed by a random subset of
  distractors. The two index spaces differ whenever the snippet has blank lines
  or distractors. **No row is opened, because the deployed host never calls the
  restore path**; owed to Pass 2 if any host ever does.
- **A vacuous guard.** `normalizeIndents` opens with `if (lines.length === 0)`
  whose loop body iterates the array it has just established is empty.
- **The unreachable parse-error branch**, which would throw twice over if it ran
  — `this` is the code line inside a `$.each` callback, and it reads `.orig` off
  an array. Its message text exists in both languages and no caller.
- **Provenance markers**: `hack` (11×), `descore` (3×), `dequery` (4×), six
  `TODO`s and three preserved misspellings. Documentation of how the port was
  made, not affordances of the thing ported.

**The counter-read, and the row it corrected.**
[FIDELITY-METHOD § Pass 3](../FIDELITY-METHOD.md#pass-3--the-counter-ledger)'s
first question, scoped to `parsons.js`: one fresh agent read the file end to end
**and then** the rows claiming to describe it. It named six affordances; five
became `116`–`120` and one was a duplicate of `parsons-086`.

⚠️ **Its most valuable return was not a missing row but a WRONG one, and that is
question 2's territory reached from question 1.** `parsons-097` asserted a
correct answer marks the column _"not the individual fragments"_ — an
unsupported negative, and false: `markCorrect` adds `correctPosition` to one
`li`. The row is corrected in place and says so. **A counter-ledger asked only
"what is missing" cannot see a row like that** — the reason FIDELITY-METHOD
carries two questions, demonstrated here on this campaign's own style exemplar.

**Two of its findings were declined, with the ground:** the built-in toggle
vocabularies still carrying Python's `True`/`False` and `and`/`or`, declined
because `parsons-086` already establishes the whole toggle path is unclickable
and `_addToggles` emits no `data-type`, so those handlers are doubly dead; and
the file's `noConflict` promise failing against four direct uses of the global
`jQuery`, declined as integrator-facing rather than something a learner, an
author or a reader of the pedagogy can do, see or know.

**The reader's own verdict on exhaustion, recorded because it is a finding about
the method** [relayed: the counter-read]: the row set is not thin in general but
thin in two nameable places — **the configuration space**, because every row
before `117` and `118` read the engine through the single configuration the
deployed host happens to pass, and **the structured feedback channel**, where
`log_errors` occurred 0× in the whole ledger. It reports that a further pass
briefed generically would likely return nothing, while one briefed on _what the
engine does that the deployed host never asks for_ probably still would. **That
is the stopping point named rather than assumed**, and Pass 3 is not discharged
by this run.

**Remainders from the second-root read — instrument output that opened no row,
named rather than dropped.** The read returned roughly **110** candidate
affordances and this pass opened 33. The surplus is Pass 2's, and it is named
here rather than left to be rediscovered:

- **`parsons.css` opened no row of its own, and that is a finding rather than a
  gap.** Its 17 class tokens split **12 live / 5 comment-only**, and the earlier
  instrument's "1 recognised, and it is an orphan" verdict is now explained: a
  tokenizer that does not strip `/* … */` counts 5 dead class names as styled
  [measured 2026-08-19]. But its **geometry never rendered** —
  `parsons-iframe.html` overrides it at runtime with
  `width: auto !important; float: none !important; margin-left: 0 !important`
  under the comment `/* Override parsons.css fixed widths */`, and `float`
  occurs exactly **1×** in that file, as `float: none !important` [measured
  2026-08-19]. ~~Its **colours** did apply — indent correctness on a 10px left
  bar, position correctness by fill, two orthogonal channels — and those are
  owed rows at Pass 2.~~ — **STRUCK 2026-08-19.**

  ⚠️ **Its first replacement was struck the same day, and that failure is worth
  more than either sentence.** The replacement read _"The colours did apply …
  but there is no indent-CORRECTNESS signal"_, and it was wrong about its own
  evidence in exactly the way the sentence it replaced was wrong — **which is
  the defect the strike was performed to remove.** It read `parsons.css` and did
  not read the page that loads it, stopping **three declarations short of the
  override it had itself cited for the geometry.** Found by AR-1, 2026-08-19.

  **What is true, measured across both files.** `parsons-iframe.html` links this
  stylesheet and then opens its own `<style>` after it, redefining the same
  element:
  `.sortable-code li { background: #1e1e1e; border: 1px solid #464647; }`. Both
  selectors weigh **(0,1,1)**, the host's block is later in document order, and
  `parsons.css` declares `!important` **0 times**. So **every per-fragment
  feedback colour, and the 10px indent bar, lost the cascade and never reached a
  learner.** The column-level verdicts did render, because the host's
  `.sortable-code ul` sets only `list-style`, `padding` and `margin`, leaving
  `ul.incorrect` and `ul.correct` standing. Rows `108`–`110` carry that scoping.
  Still true and unaffected: nothing ever applies `li.correctIndent`, since
  `markCorrectIndent` occurs **0 times** (`parsons-070`).

  **The lesson, recorded because it cost two strikes:** a claim about what a
  learner SAW cannot be settled from a stylesheet alone, and both wrong
  sentences were written from the stylesheet alone. **The cascade is a property
  of the page, not of the file** — which is the same shape as `parsons-084`,
  where the answer lived in a third file neither reader could open.

- **⚠️ There are THREE Gen-1 parsons boards and two never rendered.**
  `ParsonsLens.module.css` describes a native React board (rows `045`–`047`);
  `parsonizer/parsons.css` describes a floated two-column board, overridden
  before a learner saw it; `parsons-iframe.html` is the flexbox board that ran.
  A row or a twin that treats "the Gen-1 stylesheet" as one thing is wrong about
  which.
- **`parsonize-selection.js` is dead as a whole file**, on three independent
  measurements: no caller anywhere in the tree; every load path resolves under
  `./live-study-static/static-study/parsonizer/`, which does not exist; and the
  real host loads the parsonizer from a **different** prefix. Its six
  affordances are written and never run.

⚠️ **WHICH Gen-1 root these rows were read from, and the one place the two
disagree** [all measured 2026-08-19: `diff -q` per file]. The campaign has two
Gen-1 roots in circulation and does not rule between them: `FIDELITY-METHOD.md`
§ 4's `GEN1=` names `0--study-lenses--it-begins/`, while `parsons-021` quotes
Gen-2 naming `zz--…/spiral-lens/`. **These rows were read from `spiral-lens/`.**

Six of the seven files are **byte-identical** across the two roots —
`parsons.js`, `component.js`, `lis.js`, `parsons.css`, `parsonize-selection.js`
and `ParsonsLens.jsx` — so every row cut from them holds under either. **The
seventh, `parsons-iframe.html`, differs**, and only in the asset-path prefix: 8
lines, `/19-07-2025/static/…` in `spiral-lens/` against `/static/…` in
`0--study-lenses--it-begins/`. Both are 586 lines and identical otherwise.

**No row depends on the divergence** — `parsons-073` names the eight assets by
filename and not by path, deliberately. An earlier revision of the bullet above
published the date-stamped prefix as a flat fact; it is true of one root only,
and is struck rather than left standing beside this note. **Which root the
campaign means is a Gate-1 question**, not this ledger's to settle.

**The remainders continue:**

- **`parsons.js`'s dead extension surface** — a custom-grader hook, a fixed
  permutation option, `action_cb`, `skipHighlight`, `colorFeedback`,
  `createHTMLFromHashes`, the toggle-hole query API, a complete Finnish
  translation set never selected, and `UnitTestGrader` / `VariableCheckGrader`
  referenced by `new` and **defined nowhere in the bundle**. Roughly 20
  candidates, none learner-facing on the deployed path.
- **`component.js`'s study-tool region** — run, debug, a loop guard that
  rewrites every loop head with a `RangeError` counter, and "open in:" to four
  external visualizers. All gated on a free `config` global with no assignment
  anywhere in `public/` or `dist/`. One safety finding inside it: if prettier
  throws, the raw code is `eval`'d whichever tool was requested.
- **Toggle holes.** `$$toggle::a::b$$` renders a `jsparson-toggle` span on every
  shuffle, but the click handler returns early unless the exercise declares
  `unittests` or `vartests`, which the host never passes — so a Gen-1 toggle
  rendered as an empty, unclickable span.
- **Accessibility, measured as a zero:** `parsons-iframe.html` carries **no
  `aria-*`, no `role=`, no `tabindex`** — there is no keyboard-accessible
  alternative to dragging. No row is opened because the affordance is an
  absence; Pass 2 decides whether an absence of this size is a row.

**Remainders — instrument output that opened no row, named rather than
dropped:**

- **Four port-only headings**, which lister 1's diff produces from the port side
  and this ledger's seeding order does not reach: README `## The lens object`
  and `## Configuration` (jointly the candidate successor to ref
  `## Public API`, `parsons-001`); DOCS `## Decisions` and `## Navigation`.
  **`## Decisions` is the one that matters** — it is lister 2's corroborating
  signal, sitting where five named decisions used to be. A port-only heading
  carrying no reference ancestor is an `ADDITION` candidate, and `ADDITION` rows
  owe `walked`/`found`, which Pass 1 cannot write. **They are Pass 2's to
  open.**
- **Lister 2's ratio, which is a severity marker on rows that already exist:**
  ref **5** `^#+ Why` headings → port **0**, with **1** generic `## Decisions`
  appearing [measured 2026-08-15]. Note the convention: FIDELITY-METHOD § 2's
  table reads **4** for parsons because it counts DOCS-only `^## Why`; both
  documents at `^#+ Why[[:space:]]` gives **5**. Both reproduce — **do not
  "correct" the method's table.** The five are `parsons-002`, `-020`, `-029`,
  `-030`, `-031`.
- **Glossary literal-presence counts in the port**, evidence for Pass 2 and not
  a survival verdict: the port has **no `## Glossary` at all**, so no term is
  _defined_ there. Three terms have zero literal hits either —
  `Order correctness`, `Indent correctness`, `Line correctness`, the three
  graded states [measured 2026-08-15: `grep -ciF` over the port's README +
  DOCS].

---

## Rows

Columns and their rules: [§ Columns](../FIDELITY-METHOD.md#columns). What Pass 1
fills and what it leaves:
[`_TEMPLATE.md` § What Pass 1 writes](./_TEMPLATE.md#what-pass-1-writes-and-what-it-leaves)
— which also carries the `G3` exact-match rule, the
`heading absent from the port` wording, and the three permitted annotation
classes. This ledger follows them and does not restate them.

**Which extractor produced a cell is a property of the row's seed class, not a
blanket fact about the ledger.** A single "every quotation came from
`firstblock`" is false the moment a ledger carries a glossary, and every Gen-2
lens has one (8 of 8):

| rows        | seed class         | extractor                                                                        |
| ----------- | ------------------ | -------------------------------------------------------------------------------- |
| `001`–`033` | reference headings | `firstblock` — the cited section's first non-blank block, truncated at 240 chars |
| `034`–`044` | glossary terms     | `glossterm` — the term's own bullet, cut at the source's line wrap               |
| `045`–`047` | lister-4 clusters  | **neither** — a cluster row cites no heading, so it quotes no section            |

`…` marks the cut in both extractors. `\|` is `firstblock`'s pipe escape.

| #             | affordance                                                                                                                                                                                                                                                                                                             | provenance                  | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | disposition | discharged by | gate |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------- | ---- |
| `parsons-001` | A reader can find the module's single default export and the frozen type it conforms to, named in one place.                                                                                                                                                                                                           | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Public API: _"The module's default export is a frozen `LensModule` per [`../types.ts`](../types.ts) § LensModule:"_ — **heading absent from the port**; candidate successors are port README §§ The lens object, Configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-002` | A reader can find out what this exercise form isolates pedagogically, and the published work it comes from.                                                                                                                                                                                                            | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Why this lens exists: _"`parsons` is the learner's **assembly workbench**. A Parsons problem (Parsons & Haden 2006) gives the learner the correct lines of a program in scrambled order and asks them to assemble it. The pedagogy isolates \*\*program structure and …"_ — **heading absent from the port**; one of the five `^#+ Why` headings that fell to zero.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-003` | A reader can find the lens's rendered DOM shape, including the view-mode and indent attributes a stylesheet or test keys off.                                                                                                                                                                                          | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § UI structure: _"`<div data-lens="parsons" data-view-mode="work\|complete" data-can-indent="true\|false">`"_ · Gen-3 `README.md` § UI structure: _"`<div data-lens="parsons" data-view-mode="work\|complete" data-can-indent="true\|false">`"_ — the heading and its opening block both survive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-004` | The learner starts every exercise with the pool shuffled, so the order lines appear in carries no information about the answer.                                                                                                                                                                                        | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Pool + solution contract: _"- **Initial shuffle.** On mount, all solution lines and the selected distractors are placed in the available pool in a shuffled order (Fisher–Yates, bare `Math.random()` per the mechanical-conversion mandate — see [What this lens …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-005` | A reader can find the drag interaction specified concretely, and the reason it is — the prior shell satisfied its tests and did nothing in a browser.                                                                                                                                                                  | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Interaction contract (native HTML5 DnD): _"The drag interaction is the surface where the prior shell silently failed (it satisfied the structural contract but did nothing in a browser). The contract is therefore specified concretely, and the **arrangement logic is a pure reducer** …"_ — **heading absent from the port**; candidate rename to port README § Interaction contract (native HTML5 drag-and-drop), also re-levelled `###`→`##`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-006` | The learner can see a placed line's nesting depth as fixed-width guide steps rather than as a literal margin.                                                                                                                                                                                                          | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Indent contract (when `canIndent`): _"- Each placed line carries an **indent level** (non-negative integer), exposed semantically as `data-indent="N"`. In the **work view** the level is shown as `N` compact, fixed-width **guide steps** (`data-parsons-indent-step` — faint …"_ · Gen-3 `README.md` § Indent contract (when `canIndent`): _"Each placed line carries an indent level (`data-indent="N"`), shown in the work view as `N` compact fixed-width **guide steps** — a relative nesting cue, not a literal margin — with the indent/outdent buttons on the **right** so the …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-007` | The learner can press Check and see every placed line resolve to exactly one graded state.                                                                                                                                                                                                                             | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Feedback contract (Check): _"Clicking **Check** grades the current arrangement and sets `data-correctness` on each placed line plus the aggregate score. The grader (`lib/evaluate.ts`) computes five internal states, but \*\*two of them never become learner-visible …"_ · Gen-3 `README.md` § Feedback contract (Check): _"Check grades the arrangement via `lib/evaluate.ts` and resolves each placed line to exactly one state under the precedence `distractor > wrong-order > wrong-indent > correct`:"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-008` | The learner can switch between working the board and reading the model solution in order.                                                                                                                                                                                                                              | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § View contract: _"- **Work view** (`data-view-mode="work"`) — the interactive pool + solution board described above. - **Complete view** (`data-view-mode="complete"`) — the model solution rendered read-only in `<pre data-parsons-complete>` (lines in …"_ · Gen-3 `README.md` § View contract: _"One `data-parsons-view-toggle` button flips between the work view (the board) and the complete view (`<pre data-parsons-complete>` — the model solution in order at literal `level * indentSize`, no distractors), seeding from …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-009` | The learner can open a collapsed legend that keys the per-line feedback, listing only the states they can act on.                                                                                                                                                                                                      | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Feedback legend: _"A collapsible legend (`data-parsons-legend`, collapsed by default) keys the per-line feedback so a learner can read a Check result without guessing. It lists **only the three placed states a learner can act on** — `correct` / …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-010` | An educator can attach guidance to the exercise by embedding a block comment in the snippet, extracted with its surrounding whitespace.                                                                                                                                                                                | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Hint blocks (educator `/* … */` guidance): _"The educator may embed **hint blocks** in the snippet as C-style block comments. Ported faithfully from the legacy JSParsons parsonizer (`component.js`): each `/* … */` block (with its surrounding horizontal whitespace) is \*\*extracted …"_ — **heading absent from the port**. This is FIDELITY-METHOD § Worked rows' `parsons-018`; its disposition is Pass 2's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |             |               |      |
| `parsons-011` | The learner can find out that some pool lines do not belong, from a collapsed hint that appears only when the exercise has distractors.                                                                                                                                                                                | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Distractor-count hint: _"When the exercise includes distractors, a `data-parsons-distractor-count` hint (`<details>`, collapsed; shown only when N > 0) signals that some pool lines do not belong. **The exact count is a spoiler** (it tells the learner how many …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |             |               |      |
| `parsons-012` | The learner can open a modal listing every Check they have made since this lens mounted.                                                                                                                                                                                                                               | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Attempt history: _"Each **Check** appends an `Attempt` (see [`./types.ts`](./types.ts)) to an in-mount history. A `data-parsons-history-open` control **in the toolbar** opens a modal (`data-parsons-history-modal`, `role="dialog"`, closed by a …"_ · Gen-3 `README.md` § Attempt history: _"Each Check appends an `Attempt` to in-mount history; a toolbar button opens a modal (`role="dialog"`, closed by button or Escape) listing every attempt — number, pass/fail, score, and a read-only \*\*snapshot of the arrangement as it was …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-013` | A reader can find out what the lens does with a degenerate program, and why an empty one scores 100.                                                                                                                                                                                                                   | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Edge cases: _"- **Empty snippet** (`embodiment.source.code === ''`). No solution lines, no distractors; pool and solution both empty; Check reports `total === 0` → score 100% (vacuously complete). `evaluate-line-order.ts` \*\*short-circuits on zero …"_ · Gen-3 `README.md` § Edge cases: _"- **Empty program** — pool and solution both empty; Check scores 100 (vacuously complete); the order evaluator short-circuits before the LIS. - **Single solution line** — a degenerate exercise; still offered (the gate is total; …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-014` | A reader can find every affordance this lens deliberately does not offer, each with its reason.                                                                                                                                                                                                                        | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § What this lens does NOT do (lens-specific drops only): _"Inherited from the lenses peer (single-writer state, disposable practice, no `embody/`-top imports, no consumer branching on `source.code`): see [`../README.md` § Conventions](../README.md#conventions). Lens-specific drops vs. the …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-015` | A reader can find which file holds which layer, and why the core is split one file per internal subsystem.                                                                                                                                                                                                             | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Two-layer module: _"Per [`../README.md` § How to add a lens](../README.md#how-to-add-a-lens), the lens lives across the two required layers (pure-TS core + React wrapper). The core is split into a `lib/` subdirectory with one file per internal subsystem so …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-016` | A reader can find out that this lens adds no dependency beyond React, and that it does not parse.                                                                                                                                                                                                                      | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Dependencies (no install needed): _"- **None beyond React.** Native HTML5 Drag-and-Drop is a browser API; the LIS and parsing algorithms are vendored as in-tree TS. No drag library, no jQuery, no AST parser (parsons does not parse)."_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-017` | A reader can find what this lens defers, and the restoration path for each deferral.                                                                                                                                                                                                                                   | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Future direction: _"- **WS2 `recommend()`.** Lens ships with `recommend: () => []`. Once WS2's analysis surface lands, `recommend(embodiment)` populates Block-Model placements and down-ranks snippets too trivial to order (single-line, or all-flat). - …"_ · Gen-3 `README.md` § Future direction: _"- **`recommend`** — propose next steps and down-rank programs too trivial to order (single-line, all-flat). - **Seeded RNG** — the shuffle and distractor selection take an injectable `random` (already threaded through …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-018` | A reader can find which peer conventions bind this lens and how they apply here specifically.                                                                                                                                                                                                                          | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Conventions inherited: _"Follows all conventions in [`../README.md`](../README.md) and [`../DOCS.md`](../DOCS.md). Notable lens-specific application:"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |             |               |      |
| `parsons-019` | A reader can reach every sibling document of this lens from its README.                                                                                                                                                                                                                                                | `G2-doc` + `G3` + UNSETTLED | Gen-2 `README.md` § Navigation: _"- **Parent**: [`../README.md`](../README.md) — lenses peer. - **Architectural sketch**: [`./DOCS.md`](./DOCS.md). - **Type contract**: [`./types.ts`](./types.ts). - **Lens contract**: [`../types.ts`](../types.ts) — `LensModule` + …"_ · Gen-3 `README.md` § Navigation: _"- Region: [`../README.md`](../README.md) — the lens kind's mechanics. - [`DOCS.md`](./DOCS.md) — this lens's architectural sketch and decisions. - [`types.ts`](./types.ts) — the lens-local domain model. - Kind contract: …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |             |               |      |
| `parsons-020` | A reader can find out what the module is for, in the learner's terms, before reading any of its structure.                                                                                                                                                                                                             | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Why this module exists: _"The `parsons` lens is the learner's **assembly workbench**: a place to reconstruct a program from its scrambled lines. The learner sees the snippet's lines shuffled into an available pool (mixed with optional distractor lines that do not …"_ — **heading absent from the port**; one of the five `^#+ Why` headings that fell to zero.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-021` | A reader can find out where this lens came from and what its predecessor actually shipped.                                                                                                                                                                                                                             | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Migration: _"The pre-refactor lens lived at `zz--…/spiral-lens/src/lenses/ParsonsLens.jsx` — a thin React shell that `encodeURIComponent`'d the source into a sandboxed `parsons-iframe.html?code=…`. The pedagogy lived inside that iframe in the …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-022` | A reader can find a per-file table naming each module's layer and purpose.                                                                                                                                                                                                                                             | `G2-doc` + `G3` + UNSETTLED | Gen-2 `DOCS.md` § Modules: _"\| File \| Layer \| Purpose …"_ (a table; the extractor takes its first line) · Gen-3 `DOCS.md` § Modules: _"\| File \| Layer \| Purpose …"_ — heading survives on both sides.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-023` | A reader can find the structural target the implementation was held against, written before any code.                                                                                                                                                                                                                  | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Architectural sketch: _"> Written Phase 0, before implementation. The Refactor step of each increment is > held against this sketch. Domain terms only — no function names, no variable > names, no pseudocode (React hook names like `useState` / `useReducer` / > …"_ — **heading absent from the port**, whose four `###` children were promoted to `##`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |             |               |      |
| `parsons-024` | A reader can find the ordered phases a mount passes through, from config resolution onward.                                                                                                                                                                                                                            | `G2-doc` + `G3` + UNSETTLED | Gen-2 `DOCS.md` § Execution phases: _"1. **Mount + resolve config** (sync, pure) — the orchestrator passes a frozen embodiment and a frozen lens config via props. The wrapper reads four known config fields (`canIndent`, `maxDistractors`, `indentSize`, `viewMode`) with …"_ · Gen-3 `DOCS.md` § Execution phases: _"1. **Mount + resolve config** (sync, pure) — the orchestrator mounts `main` with the frozen embodiment and resolved config. The component reads the four known fields with their defaults; view mode seeds from config into local state."_ (re-levelled `###`→`##`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-025` | A reader can find the module's data path as a diagram rather than as prose.                                                                                                                                                                                                                                            | `G2-doc` + `G3` + UNSETTLED | Gen-2 `DOCS.md` § Data flow: _"flowchart TD"_ (a Mermaid fence; the extractor takes its first line) · Gen-3 `DOCS.md` § Data flow: _"flowchart TD"_ (re-levelled `###`→`##`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-026` | A reader can find the constraints the implementation must not violate, stated separately from what it does.                                                                                                                                                                                                            | `G2-doc` + `G3` + UNSETTLED | Gen-2 `DOCS.md` § Structural constraints: _"- **Two-layer module shape** — `core.ts` + the files under `lib/` do NOT `import React`. `lib/lis.ts`, `lib/parse-parsons.ts`, `lib/evaluate*.ts` (the two evaluators + the composing `lib/evaluate.ts`), and `lib/arrange.ts` are pure …"_ · Gen-3 `DOCS.md` § Structural constraints: _"- **Two-layer module.** `core.ts` and `lib/` import no React; `index.tsx` is the only React file. Heavy derivation lives in the core; the component renders what the core derives. - **Purity rule.** The lens imports no runtime value …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-027` | A reader can find what the lens deliberately leaves to another owner, separately from what it defers.                                                                                                                                                                                                                  | `G2-doc` + `G3` + UNSETTLED | Gen-2 `DOCS.md` § Out of scope: _"- **Cross-mount persistence of the arrangement / feedback.** Per-mount React state only; nothing survives unmount. - **URL state.** v1 ships none (legacy parsons had none beyond `?code=`). A Future-direction item; would lift to the …"_ · Gen-3 `DOCS.md` § Out of scope: _"- **Cross-mount persistence** of the arrangement, feedback, or history. - **URL state** — orchestrator domain, not a per-lens surface. - **Program mutation** — the lens is a read-only view; the source changes only through the …"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-028` | A reader can find the four parity features a second design pass added, and which browser gate reshaped each.                                                                                                                                                                                                           | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Phase-8 additions + browser-checkpoint reconciliations: _"> The wrapper's UX was reshaped at the per-increment browser gates, and a second > DDD pass added four parsonizer-parity features (read faithfully from the > legacy `…/parsonizer/component.js`). These are the deltas to the sketch phases …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-029` | A reader can find out why the grader marks each line independently rather than stopping at the first error.                                                                                                                                                                                                            | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Why grade per-line independently (vs. the legacy sequential gate): _"The legacy `LineBasedGrader` stops at the first error class and only marks lines correct when the whole arrangement is error-free — so a learner with one misordered line gets a wall of nothing on every other line, and never sees indent …"_ — **heading absent from the port**; candidate successor is the first bullet of port DOCS § Decisions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-030` | A reader can find out why arrangement transitions are a pure reducer rather than logic inside the drag handlers.                                                                                                                                                                                                       | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Why a pure `arrange.ts` reducer (vs. inline drag handlers): _"The prior shell died in the browser despite passing tests — the interaction layer was the gap. Native HTML5 DnD cannot be exercised by jsdom, so RTL component tests cannot prove drag works. Extracting the arrangement transitions into a …"_ — **heading absent from the port**; candidate successor is a bullet of port DOCS § Decisions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-031` | A reader can find out which of two vendored LIS lineages this lens restored, and what the other one got wrong.                                                                                                                                                                                                         | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Why restore the original LIS selection (vs. copying the parsonizer bug): <em>"There are two vendored lineages of the LIS code. The original `Explorotron/libs/js-parsons/lib/lis.js` uses underscore: `best_lise` selects the maximal-consecutive-run subsequence via `_.max(scores, s => s.score)`. The de-underscored …"</em> — **heading absent from the port**; candidate successor is a bullet of port DOCS § Decisions. ⚠️ Instrument caveat: quoted in `<em>` rather than `_…_` because the block carries a code span holding an underscore, which breaks prettier's emphasis pairing for the rest of the cell — the closing marker had been escaped and the quotation left unterminated, so the published transport check read this row as carrying no quotation at all [measured 2026-08-17: `0 of 1 cited` before, clean after].                                                                                                                                                                                                                              |             |               |      |
| `parsons-032` | A reader can find out exactly which files this lens owns, and by implication which it must not edit.                                                                                                                                                                                                                   | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Module ownership: _"The lens owns its own `README.md`, `DOCS.md`, `types.ts`, source (`core.ts`, `lib/lis.ts`, `lib/parse-parsons.ts`, `lib/evaluate-line-order.ts`, `lib/evaluate-indentation.ts`, `lib/evaluate.ts`, `lib/arrange.ts`, `index.tsx`), and tests. …"_ — **heading absent from the port**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-033` | A reader can find the architecture-level deferrals separately from the README's feature-level ones.                                                                                                                                                                                                                    | `G2-doc` + UNSETTLED        | Gen-2 `DOCS.md` § Future direction: _"See [`./README.md` § Future direction](./README.md#future-direction) for the full list. Key directions in scope of this lens's evolution:"_ — **heading absent from the port's DOCS**. Distinct from `parsons-017`, the README's section of the same name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-034` | A reader can look up what a **Parsons problem** is without leaving the module.                                                                                                                                                                                                                                         | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Parsons problem** — an exercise where the learner reconstructs a program by…"_ — the port carries **no `## Glossary` heading at all**, so no term below is _defined_ there [measured 2026-08-15].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-035` | A reader can look up what counts as a **Solution line**, and what is excluded from the count.                                                                                                                                                                                                                          | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Solution line** — a non-blank line of the snippet that is **not** a…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-036` | A reader can look up how a **Distractor** is marked in the source.                                                                                                                                                                                                                                                     | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Distractor** — a line whose source ends with the `// distractor` marker. It…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-037` | A reader can look up what the **Available pool** contains.                                                                                                                                                                                                                                                             | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Available pool** — the column of shuffled lines (solution lines +…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |             |               |      |
| `parsons-038` | A reader can look up what the **Solution column** is and that its order is meaningful.                                                                                                                                                                                                                                 | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Solution column** — the column the learner drags lines **into**, in order;…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-039` | A reader can look up what an **Indent level** is and that it is a non-negative integer.                                                                                                                                                                                                                                | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Indent level** — a line's nesting depth as a non-negative integer (0 = no…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-040` | A reader can look up what **Order correctness** means as a graded state.                                                                                                                                                                                                                                               | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Order correctness** — whether a placed line participates in a correct…"_ — **zero literal hits anywhere in the port** [measured 2026-08-15: `grep -ciF`].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-041` | A reader can look up what **Indent correctness** means, and that it is judged only on order-correct lines.                                                                                                                                                                                                             | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Indent correctness** — whether a placed, order-correct line's indent level…"_ — **zero literal hits anywhere in the port** [measured 2026-08-15].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-042` | A reader can look up the closed set of per-line graded states.                                                                                                                                                                                                                                                         | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Line correctness** — the per-line graded state, one of: `correct`,…"_ — **zero literal hits anywhere in the port** [measured 2026-08-15].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-043` | A reader can look up what the **Score** is a percentage of.                                                                                                                                                                                                                                                            | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Score** — the aggregate percentage of solution lines that are fully correct…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-044` | A reader can look up what the **Check** action does.                                                                                                                                                                                                                                                                   | `G2-doc` + UNSETTLED        | Gen-2 `README.md` § Glossary: _"- **Check** — the learner action that grades the current arrangement and renders…"_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-045` | Twenty class definitions in the stylesheet's pre-banner region — `blocksPanel`, `solutionPanel`, `insertZone`, `dropMessage`, `checkButton` among them — describe UI surfaces the shipped 181-line `<iframe>` shell never renders.                                                                                     | `G1-dead` + UNSETTLED       | Gen-1 `ParsonsLens.module.css`, lister-4 cluster `(prologue)` — **20 orphan classes**: `blockContent`, `blockHeader`, `blockNumber`, `blockType`, `blocksContainer`, `blocksPanel`, `checkButton`, `codeBlock`, `controls`, `dropMessage`, `emptyMessage`, `exerciseContent`, `feedback`, `hint`, `hintButton`, `insertZone`, `resetButton`, `solutionBlock`, `solutionContainer`, `solutionPanel`. None referenced from `ParsonsLens.jsx`, which ships a 181-line `<iframe>` shell [measured 2026-08-15]. ⚠️ Cluster boundary is the stylesheet's first banner (line 287), not a semantic reading — Pass 2 splits by appending ids.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-046` | Five class definitions under the stylesheet's `Modal styles` banner — `parsons-modal`, `parsons-modal-content`, `parsons-modal-close`, `guess-entry`, `guess-status` — describe a modal surface the shipped `<iframe>` shell never renders.                                                                            | `G1-dead` + UNSETTLED       | Gen-1 `ParsonsLens.module.css`, lister-4 cluster `Modal styles` — **5 orphan classes**: `guess-entry`, `guess-status`, `parsons-modal`, `parsons-modal-close`, `parsons-modal-content`. All five are kebab-case, so `styles.<name>` cannot express them; each direct-checked absent from the `.jsx` in every form [measured 2026-08-15].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |             |               |      |
| `parsons-047` | Two class definitions under the stylesheet's `SL1 Parsonizer Integration` banner — `codeContainer`, whose seven descendant rules style the in-page parsonizer's own markup, and `parsons-fallback` — are unreferenced from the shipped shell.                                                                          | `G1-dead` + UNSETTLED       | Gen-1 `ParsonsLens.module.css`, lister-4 cluster `SL1 Parsonizer Integration` — **2 orphan classes**: `codeContainer`, `parsons-fallback`. `parsons-fallback` is kebab-case and direct-checked absent [measured 2026-08-15]. `codeContainer` carries a base rule plus **7** descendant rules for the parsonizer's own markup — `.parsons-exercise`, `.sortable-code`, `.parsons-text`, `.parsons-controls`, `.parsons-button`, `.parsons-button:hover`, `.parsons-feedback` [measured 2026-08-16: `grep -nE '^\s*\.codeContainer'`]. ⚠️ Instrument caveat, per the live-sibling rule the lister structurally cannot apply: `.fallbackContainer` **is** referenced at `ParsonsLens.jsx:52` and defined 3× [measured 2026-08-16], so a styled fallback **does** render — via the distinct, referenced `.fallbackContainer`, not via the orphaned `parsons-fallback` in this cluster.                                                                                                                                                                                                        |             |               |      |
| `parsons-048` | The learner's puzzle is built from whatever source arrives in the page URL's `code` query parameter.                                                                                                                                                                                                                   | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `decodeURIComponent` (1×) — <em>"const code = decodeURIComponent(encodedCode);"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-049` | A learner who reaches the page with no `code` parameter sees the whole page replaced by one red centred line and nothing else.                                                                                                                                                                                         | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, the string literal `Error: No code provided` (1×), written into a `div` styled <em>"text-align: center; padding: 50px; color: #f44336;"</em> and replacing the document body                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |             |               |      |
| `parsons-050` | The learner works across two labelled regions, "📦 Available Blocks" and "🎯 Your Solution", sitting side by side at exactly equal width.                                                                                                                                                                              | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `class="parsons-area"` (2×) carrying the `h3` labels <em>"📦 Available Blocks"</em> and <em>"🎯 Your Solution"</em>; `parsons-areas` (3×) — <em>".parsons-areas { display: flex; … flex: 1; }"</em> with <em>".parsons-area { flex: 1; }"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-051` | Each region scrolls on its own and the page itself never scrolls, so the control bar stays reachable however long the program is.                                                                                                                                                                                      | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `overflow-y: auto` (3×) — <em>"body { … height: 100vh; overflow: hidden; }"</em> and <em>".sortable-code { … overflow-y: auto; min-height: 0; max-height: 100%; }"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-052` | On a viewport 768px wide or narrower the two regions stack vertically instead of sitting side by side.                                                                                                                                                                                                                 | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `max-width: 768px` (1×) — <em>"@media (max-width: 768px) { .parsons-areas { flex-direction: column; gap: 5px; }"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-053` | The learner has exactly three controls, in one row above the board: 🔄 New Instance, ✅ Get Feedback, 📝 Review Guesses.                                                                                                                                                                                               | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `parsons-controls` (9×) — the three labels are <em>"🔄 New Instance"</em> on `id="new-instance-btn"`, <em>"✅ Get Feedback"</em> on `id="feedback-btn"`, and <em>"📝 Review Guesses"</em> on `id="review-btn"`, the last an anchor styled to match the two buttons                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |             |               |      |
| `parsons-054` | The blocks are already scrambled when the page opens — the learner presses nothing to start.                                                                                                                                                                                                                           | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `initializeParsons` (3×) — <em>"parsonWidget.init(processedCode);"</em> then <em>"// Shuffle lines"</em> and <em>"parsonWidget.shuffleLines();"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-055` | Pressing 🔄 New Instance to get a fresh scramble also erases the learner's entire guess history, silently and without confirmation.                                                                                                                                                                                    | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `guesses = []` (2×) — <em>"// Reset guesses history for new instance"</em> / <em>"guesses = [];"</em> / <em>"updateGuessesDisplay();"</em>, in the same handler that calls `shuffleLines()`. The record 📝 Review Guesses exists to show is destroyed by the control a stuck learner reaches for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |             |               |      |
| `parsons-056` | While feedback is open the learner cannot reach any of the three controls — a backdrop covers the whole viewport and clicking it dismisses the feedback instead.                                                                                                                                                       | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `feedback-backdrop` (6×) — <em>".feedback-backdrop { … top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.5); z-index: 999; }"</em>, while `.parsons-controls` declares no `position` and no `z-index`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |             |               |      |
| `parsons-057` | A learner whose arrangement is wrong reads "❌ Errors Found:" over a bulleted list of the engine's own messages; a learner told "✅ Perfect!" may have had nothing checked at all.                                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `Errors Found` (1×) — <em>"❌ Errors Found:"</em>. The success branch is the `else` of <em>"if (fb.errors && fb.errors.length > 0)"</em>, so a feedback object carrying no `errors` property also renders <em>"✅ Perfect! Your solution is correct!"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-058` | 📝 Review Guesses opens a full-screen overlay with no JavaScript at all, and the only way back out is its × link — no click-outside, no Escape.                                                                                                                                                                        | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `review-btn` (1×, markup only — no JS references it) with <em>".modal-window:target { opacity: 1; pointer-events: auto; }"</em>; `modal-close` (3×), and no handler is bound to `#review-modal` anywhere in the file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-059` | An educator's block comments are stripped out of the source before the puzzle is built, so no hint the educator wrote reaches the learner. Line comments are not stripped.                                                                                                                                             | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `blockCommentRegex` (2×) — <em>"processedCode = processedCode.replace(blockCommentRegex, '');"</em>, the regex matching a C-style block comment together with its surrounding horizontal whitespace. No equivalent regex for a line comment exists in the file [measured 2026-08-19].                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-060` | Lines an educator marked `// distractor` are detected by the deployed host and then nothing is done with them — both branches return the identical value.                                                                                                                                                              | `G1-dead` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `distractor` (4×) — the matched branch is <em>"return line; // Keep distractor lines as-is"</em> and the fall-through is <em>"return line;"</em>, under the comment <em>"// Handle distractor lines (lines ending with // distractor)"</em>, which advertises an authoring affordance the file does not implement [measured 2026-08-19]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-061` | An educator marks a line as a distractor by ending it with a `//distractor` comment, and the marker is stripped before the learner ever sees the fragment.                                                                                                                                                             | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, file header (1×) — <em>"distractors are marked by JS comments //"</em>; the strip runs in the `ParsonsCodeline` constructor, which `parseCode` calls for every line, via two chained `replace` calls on the same trailing-comment pattern                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-062` | An educator can bind consecutive lines into a single draggable block by writing an escaped newline between them.                                                                                                                                                                                                       | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// Consecutive lines to be dragged as a single block of code have strings` (1×) — <em>"// represent newlines => replace them with actual new line characters"</em>, applied by a global `replace` in the same constructor                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |             |               |      |
| `parsons-063` | Blank and whitespace-only lines in the educator's snippet do not become fragments.                                                                                                                                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// The line is non-empty, not just whitespace` (2×) — <em>"if (lineObject.code.length > 0) {"</em>, inside `parseCode`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-064` | The learner sets a fragment's nesting depth by dragging it sideways; the drag snaps to a 50-pixel grid and cannot go below level zero.                                                                                                                                                                                 | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `x_indent` — <em>"grid: that.options.can_indent ? [that.options.x_indent, 1] : false,"</em> (1×) with <em>"x_indent: 50,"</em> and <em>"new_indent = Math.max(0, new_indent);"</em> (1×)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-065` | Dragging a fragment back to the pool resets its indentation to zero, so a learner cannot park an indent while they think.                                                                                                                                                                                              | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `receive` handler on the trash sortable — <em>"that.getLineById(ui.item[0].id).indent = 0;"</em> (1×)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |             |               |      |
| `parsons-066` | The learner is graded on indentation level — the rank order of the snippet's distinct indent widths — while their own indentation is deliberately left un-normalized, so they must replicate the original indentation.                                                                                                 | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// hack: normalize to least-indented line, not the first` (1×) — <em>"// js isn't white-space sensitive so there should be no penalty"</em> / <em>"// intead students must replicate the initial indentation"</em>; and `// js hack: don't normalize student code indents - students must replicate indentation` (1×). Source spelling `intead` left as written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-067` | A learner with the wrong number of fragments is told so in words, and the whole solution list is marked incorrect.                                                                                                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `Your program has too few code fragments.` (1×) and `Your program has too many code fragments.` (1×), with `addClass('incorrect');` (2×) in the same count check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-068` | The learner is shown which of their placed fragments could be moved to fix the program, rather than being told everything after the first mistake is wrong.                                                                                                                                                            | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// Highlight the lines that could be moved to fix code as defined by the LIS computation` (1×) — <em>"lisStudentCodeLineObjects[lineObjectIndex].markIncorrectPosition();"</em>; the plain-language message is <em>"Code fragments in your program are wrong, or in wrong order. This can be fixed by moving, removing, or replacing highlighted fragments."</em> (1×)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-069` | Indentation feedback is withheld until order and count are already right; once it fires, every indent error is shown at once and each names the offending fragment by its position in the learner's own solution.                                                                                                      | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// Finally, check indent if no other errors` (1×) guarding <em>"if (errors.length === 0) {"</em>; `// hack: highlight all indentation errors since they don't cause errors` (1×), which replaced a disabled first-error-only guard; and `belongs to a wrong block (i.e. indentation).` (1×) — <em>"The highlighted fragment "</em> plus the 1-based line number                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-070` | A reader of the engine's options and feedback-style tables would believe two things that are false: that only the first error is reported, and that correctly-indented fragments are marked as such.                                                                                                                   | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `first_error_only: true,` (1×) — read by nothing; its only other occurrence sits inside a comment. And `correctIndent: 'correctIndent',` (1×) — `markCorrectIndent` occurs **0 times** in the file, so the class is only ever removed, by `clearFeedback` [all measured 2026-08-19]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-071` | The learner is told the fewest lines that must move, because the grader marks the complement of a longest correct subsequence rather than everything after the first error.                                                                                                                                            | `G1-live` + UNSETTLED       | Gen-1 `lis.js`, `inverse_indices` (5×) — <em>"// Returns an array of those indices of the input that"</em> / <em>"// are not included in the chosen longest increasing"</em> / <em>"// subsequence of the input values."</em>. `LIS.best_lise_inverse_indices` is the sole `LIS` entry point `parsons.js` calls                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |             |               |      |
| `parsons-072` | Among equally-long candidates the engine computes a consecutive-run score and then does not select by it, so the learner can be marked wrong on a different set of lines than the scoring intends.                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `lis.js`, `lis_scores.sort` (1×) — the live selection is <em>"var best = lis_scores.sort(function (item) { return item.score; })[lis_scores.length - 1];"</em>, sitting directly beneath its own commented original tagged `// descore`, which used underscore's `max` with the same key extractor. A one-argument key extractor handed to `sort`, which expects a two-argument comparator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-073` | A reader tracing Gen-2's hint-block provenance to `component.js` finds the feature there and finds that the deployed host never loads that file.                                                                                                                                                                       | `G1-dead` + UNSETTLED       | Gen-1 `parsons-iframe.html` loads eight assets — `jquery.min.js`, `jquery-ui.min.js`, `prism/style.css`, `prism/script.js`, `prism/toolbar.js`, `parsons.css`, `parsons.js`, `lis.js` — and `component.js` is not among them [measured 2026-08-19: `grep -c` → **0**]. Its only loader, `parsonize-selection.js`, is referenced by nothing and targets a directory absent from the tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |             |               |      |
| `parsons-074` | On the `component.js` path an educator's block comments are lifted out of the puzzle and shown as standing prose above it, delimiters included, rendered as HTML.                                                                                                                                                      | `G1-dead` + UNSETTLED       | Gen-1 `component.js`, `blockComments` (5×) — <em>"this.blockComments = this.code.match("</em> … <em>"commentPre.innerHTML = blockComment;"</em>. The regex's capture group is never read anywhere in the file, so the comment delimiters are part of what renders. Dead by `parsons-073`, not by anything in this file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-075` | An educator can fold a hint shut with a `parsons-collapse:` directive, the text after the marker becoming its clickable summary.                                                                                                                                                                                       | `G1-dead` + UNSETTLED       | Gen-1 `component.js`, the directive occurs 2× — <em>"const toCollapse = blockComment.match("</em> … <em>"const details = document.createElement('details');"</em>. ⚠️ A grep for `parsons-collapse` returns **0**: the in-file literal is a regex carrying backslashes [measured 2026-08-19]. No campaign document records this directive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-076` | On the `component.js` path feedback is a browser alert of the comma-joined errors, and a correct solution produces no message at all.                                                                                                                                                                                  | `G1-dead` + UNSETTLED       | Gen-1 `component.js`, `displayErrors` (2×) — <em>"function displayErrors(fb) { if (fb.errors.length > 0) { alert(fb.errors.toString()); } }"</em> — the function has no `else`, and `feedback_cb: displayErrors` is its only wiring                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-077` | On the `component.js` path every backslash in the learner's own source gains a trailing space before the puzzle is built, so an escape sequence reaches the board broken.                                                                                                                                              | `G1-dead` + UNSETTLED       | Gen-1 `component.js`, `replaceAll` (1×) — a global replace of every backslash by backslash-plus-space, unconditional in the constructor and applied to the learner's own stripped source before it becomes fragments                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-078` | Gen-1 executed the engine over the learner's own source in a first-party iframe with no sandbox attribute at all, and the restriction that would have fenced it in sits beside it, commented out.                                                                                                                      | `G1-live` + UNSETTLED       | Gen-1 `ParsonsLens.jsx`, `sandbox` (1×, the sole occurrence) — a commented attribute naming `allow-scripts allow-same-origin`. The live element carries seven attributes — `ref`, `src`, `className`, `style`, `onLoad`, `onError`, `title` — and no `sandbox`, `allow`, `referrerpolicy`, `loading` or `srcdoc`. ⚠️ The commented form is strictly more restrictive than what shipped; commenting it out was a loosening.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-079` | The shell can never learn whether the learner solved, attempted, or abandoned the puzzle — nothing crosses back from the frame.                                                                                                                                                                                        | `G1-live` + UNSETTLED       | Gen-1 `ParsonsLens.jsx` — `postMessage`, `contentWindow`, `contentDocument`, `onmessage` and `addEventListener` all occur **0 times**; `parsons-iframe.html` likewise carries **0** `postMessage` [both measured 2026-08-19]. The seam is the query string in one direction and nothing in the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-080` | A reader of the shell's instructions panel finds the engine's control vocabulary written down on the shell side, where none of it is implemented.                                                                                                                                                                      | `G1-live` + UNSETTLED       | Gen-1 `ParsonsLens.jsx`, `New Instance` (1×), `Review Guesses` (1×) and `Get Feedback` (1×), each inside a `strong` element in the instructions list — <em>"to shuffle blocks and try"</em>, <em>"to see your previous"</em>. The three names match the deployed host's three buttons (`parsons-053`) — two files, independently, the same vocabulary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |             |               |      |
| `parsons-081` | An author marks a line as not belonging to the solution by ending it with a distractor comment, and the engine routes it into a separate pool rather than into the answer.                                                                                                                                             | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `item.search(/\/\/( \|\t)+distractor\s*$/) >= 0` (1×) — <em>"lineObject.indent = -1;"</em> / <em>"lineObject.distractor = true;"</em>. Exactly two spellings are accepted and both are anchored to end of line, so a marker followed by any further text is not recognised and the line joins the solution instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-082` | A line that is blank once its marker is stripped is silently discarded, and the author is told nothing.                                                                                                                                                                                                                | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// The line is non-empty, not just whitespace` (2×) — <em>"if (lineObject.code.length > 0) {"</em>, guarding the push in both the distractor branch and the solution branch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |             |               |      |
| `parsons-083` | Whatever indentation an author writes on a distractor line is discarded before the learner ever sees it.                                                                                                                                                                                                               | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `lineObject.indent = -1;` (1×), in the distractor branch of `parseCode`; every widget line is then flattened again by <em>"item.indent = 0;"</em> (1×) at the end of `init`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-084` | The deployed host asks for one hundred distractors from a snippet that will have a handful, and the selection loop is bounded by that request rather than by how many exist — so it appends roughly ninety-five empty entries per exercise, and nothing breaks only because the copy that follows silently drops them. | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `max_wrong_lines: 100,` (1×) in the sole `new ParsonsWidget({` call. Gen-1 `parsons.js`, `for (var i = 0; i < max_distractors; i++) {` (1×) — <em>"selected_distractors.push(distractors[permutation[i]]);"</em> / <em>"widgetData.push(distractors[permutation[i]]);"</em> — while `permutation` is built as <em>"var permutation = this.getRandomPermutation(distractors.length);"</em>. Its own comment names only the opposite case: <em>"// Remove extra distractors if there are more alternative distrators"</em>. ⚠️ Instrument caveat: the absorption is in neither parsons file — jQuery v3.6.0's `extend` skips undefined source values, `void 0!==r&&(a[t]=r)` [measured 2026-08-19 against the vendored `jquery.min.js`], so `$.extend(true, [], widgetData)` returns the correct length.                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-085` | An author who indents with tabs on one line and spaces on another at the same visual depth gets two different indent levels, because depth is counted in characters before it is ranked.                                                                                                                               | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, the `ParsonsCodeline` constructor — <em>"this.indent ="</em> / <em>"codestring.length - codestring.replace(/^\s+/, '').length;"</em> (1×); the ranking that follows reads those counts, `rawIndents` (2×) — <em>"const rawIndents = lines.map((line) => line.indent);"</em>.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-086` | An author who writes a toggle marker into a line gets a span the learner can see and can never click, because the handler that would bind it is registered after a return the deployed configuration always takes.                                                                                                     | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `// toggleable elements are only enabled for unit tests` (1×) — <em>"if (!widget.options.unittests && !widget.options.vartests) {"</em> / <em>"return;"</em>, sitting after the loop that calls `_addToggles` on every line and before the click registration. The span is emitted with no content: `"<span class='jsparson-toggle' data-jsp-options='" +`. ⚠️ Instrument caveat: quoted in a code span because the fragment opens with a literal `<`, which `MD033` rejects as inline HTML and which an entity would silently alter. The deployed host passes neither option.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-087` | Everything an author writes is escaped before display so angle brackets appear as characters — except on a line carrying a toggle marker, where the raw source is put straight back into the page as markup.                                                                                                           | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `escapeHTML` (4×) — defined as <em>"function escapeHTML(string) {"</em> and applied unconditionally in the live return of `codeLineToHTML`; then `_addToggles` runs <em>"var html = this.code;"</em> followed by <em>"this.elem().html(html);"</em> (1×), replacing the escaped `pre`/`code` wrapper with the unescaped source.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |             |               |      |
| `parsons-088` | An author's mistakes are collected into an error list and then thrown away, so no authoring error is ever reported.                                                                                                                                                                                                    | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// TODO: Error handling, parseCode may return errors in an array in property named errors.` (1×), sitting directly above the `parseCode` call in `init`; the returned `errors` property is read nowhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-089` | The two region captions are put into the page as raw markup, unlike the code they sit above.                                                                                                                                                                                                                           | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `this.options.trash_label` (3×) — `? '<p>' + this.options.trash_label + '</p>'` — with no `escapeHTML` on either label, against the escaped path taken by every code fragment. ⚠️ Instrument caveat: quoted in a code span for the same reason as `parsons-086` — the fragment carries literal angle brackets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-090` | The exercise's whole configuration is fixed by the host in one place — two lists, indentation on, JavaScript highlighting and a feedback callback — and nothing about it is reachable by the person solving or authoring.                                                                                              | `G1-live` + UNSETTLED       | Gen-1 `parsons-iframe.html`, `new ParsonsWidget({` (1×) — <em>"sortableId: 'sortable-code',"</em> / <em>"trashId: 'trash-code',"</em> / <em>"max_wrong_lines: 100,"</em> / <em>"feedback_cb: displayErrors,"</em> / <em>"can_indent: true,"</em> / <em>"language: 'js',"</em>. Six of the engine's options are set and every other default stands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-091` | Two of the engine's published options do nothing at all, because the only code that ever read them is commented out.                                                                                                                                                                                                   | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `first_error_only: true,` (1×) whose sole other occurrence is inside a comment — `//   ((!parson.options.first_error_only) \|\| errors.length === 0)) {` — quoted in a code span because the source's three spaces after `//` do not survive emphasis. ⚠️ Its two pipes still take the escape: a `\|` inside a **code span** is required in a GFM table too, and without it the row silently gained four cells and `MD056` reported the tail as lost [measured 2026-08-19]; and `this.options.prettyPrint` (2×), both occurrences on one commented line — <em>"// if (prettyPrint && (typeof (this.options.prettyPrint) === "undefined" \|\| this.options.prettyPrint)) {"</em>.                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-092` | A complete second language sits in the engine and never renders, because the option that selects it is defaulted to English and the deployed host does not override it.                                                                                                                                                | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `lang: 'en',` (1×) in the `defaults` block, with `translations.hasOwnProperty(this.options.lang)` (1×) — <em>"this.translations = translations[this.options.lang];"</em>; the Finnish table carries every message, e.g. <em>"trash_label: 'Raahaa rivit ohjelmaasi tästä',"</em>. The deployed host passes no `lang`, so the merged value is the default. ⚠️ Instrument caveat: an earlier revision of this row attributed the outcome to a silent **fallback** and quoted the `else` arm as its mechanism. That arm is unreachable — `hasOwnProperty('en')` is true — so the quotation was of dead code presented as the live path. **Struck 2026-08-19**, found by AR-1.                                                                                                                                                                                                                                                                                                                                                                                            |             |               |      |
| `parsons-093` | The engine distinguishes four kinds of move, and one of them — rearranging the pile the learner has not drawn from yet — is recorded although it changes nothing about the answer.                                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, the four sortable handlers — <em>"{ type: 'moveOutput', target: ui.item[0].id },"</em>, <em>"{ type: 'addOutput', target: ui.item[0].id },"</em>, <em>"{ type: 'removeOutput', target: ui.item[0].id },"</em> and <em>"{ type: 'moveInput', target: ui.item[0].id },"</em> (1× each as a whole word).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |             |               |      |
| `parsons-094` | A reader can find that clearing feedback reaches only the solution column and never the pile — an asymmetry with no learner-visible consequence on the deployed path, because clearing also fires on drag start, before the fragment moves.                                                                            | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `clearFeedback` — <em>"var li_elements = $('#ul-' + this.options.sortableId + ' li');"</em> (1×) followed by <em>"li_elements.removeClass(value);"</em>; `trashId` occurs nowhere in the function. ⚠️ Instrument caveat: an earlier revision claimed the learner would see a marking survive into the pile. It does not — `clearFeedback` is also bound to both sortables' `start` (`parsons-095`), which fires before jQuery UI moves the element, so the class is stripped while the fragment is still a child of the solution list. **Struck 2026-08-19**, found by AR-1. The code asymmetry is real; the consequence claimed for it was not.                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-095` | Every colour the learner was just given disappears the instant they touch anything, so the feedback cannot be consulted while acting on it.                                                                                                                                                                            | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `start: function () {` (2×), one on each sortable — <em>"that.clearFeedback();"</em>. The handler fires on drag start, before any drop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-096` | The whole solution column is marked wrong only when the learner has the wrong NUMBER of fragments — an answer in the wrong order with the right count never gets that mark.                                                                                                                                            | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `addClass('incorrect')` (2×), both inside the length branches — <em>"if (parson.model_solution.length < student_code.length) {"</em> and <em>"} else if (parson.model_solution.length > student_code.length) {"</em>. The wrong-order path pushes an error and adds no container class.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-097` | A learner who has the order and the count right sees every individually correct fragment turn green — in the same Check that turns a mis-indented one red — and a fully correct answer additionally marks the whole solution column.                                                                                   | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `markCorrect` (2×) — <em>"this.elem().addClass(this.widget.FEEDBACK_STYLES.correctPosition);"</em>, which marks one fragment, called at <em>"!options.skipHighlight && code_line.markCorrect();"</em>; and separately `// if answer is correct, mark it in the UI` (1×) — <em>"if (fb.success) {"</em> / <em>"$('#ul-' + this.options.sortableId).addClass('correct');"</em>, which marks the column. The gate <em>"if (errors.length === 0) {"</em> is evaluated once, **outside** the loop, and the loop then pushes indent errors without re-testing it — so one Check can paint one fragment red and every other one green. ⚠️ An earlier revision of this row claimed the marking was **not** on individual fragments; that was an unsupported negative and it was false. **Struck 2026-08-19**, found by the Pass-3 counter-read of this file.                                                                                                                                                                                                                  |             |               |      |
| `parsons-098` | The pool can arrive in the author's own order, because the scramble is a run of random pairwise swaps rather than a shuffle and nothing checks the result against the answer — though the learner still starts with an empty solution column and must move every fragment into it.                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `getRandomPermutation` (3×) — `swap1 = Math.floor(Math.random() * n);` and `swap2 = Math.floor(Math.random() * n);` inside <em>"for (i = 0; i < n; i++) {"</em>. The solution column starts empty: the host passes `trashId`, so `shuffleLines` takes <em>"this.createHTMLFromLists([], idlist);"</em> (1×). ⚠️ Instrument caveat: those two swap lines are quoted in **code spans** rather than `<em>`, because their `*` operators pair as emphasis markers across the cell — prettier silently rewrote both to `_`, corrupting the quotation, and markdownlint then reported `MD037` twice [measured 2026-08-19]. Same amendment as `parsons-113`. ⚠️ An earlier revision of this row said "the board can arrive already solved"; the board cannot, because the solution column is rendered empty and the quoted swap loop supports nothing about it. **Struck 2026-08-19**, found by AR-1. The deployed host passes no `permutation` override, so the scramble itself is the live path.                                                                           |             |               |      |
| `parsons-099` | A fragment the author marked as not belonging is recognised at grading time by its text being absent from the answer, not by the marker — so a distractor whose text duplicates a real line grades as that line.                                                                                                       | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// it must be a distractor` (1×) — <em>"// Could not find the line in the model solution at all,"</em> / <em>"wrong_order = true;"</em>, reached only when `lastFoundCodeIndex` has no entry for the fragment's text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-100` | A learner who places the same fragment twice is shown the extra copy as misplaced rather than told it is a duplicate.                                                                                                                                                                                                  | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `lisIgnore` (4×) — <em>"// The line is part of the solution but there are now"</em> / <em>"// too many instances of the same line in the student's code"</em> / <em>"// extra duplicates are left in the inverse and highlighted as"</em> / <em>"// errors."</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-101` | Indentation is judged only across as many fragments as the shorter of the answer and the attempt, so a surplus fragment is never indent-checked at all.                                                                                                                                                                | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `lines_to_check` (2×) — <em>"var lines_to_check = Math.min("</em> / <em>"student_code.length,"</em> / <em>"parson.model_solution.length"</em>. Reached only inside the gate that requires no other error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |             |               |      |
| `parsons-102` | Every fragment is rendered at seventy per cent of the surrounding text, written directly onto the element so no stylesheet can change it without forcing.                                                                                                                                                              | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `style="font-size: 70%;"` (2×, one live and one on a commented alternate) — the live return of `codeLineToHTML`, which also stamps <em>"no-line-numbers"</em> on every fragment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |             |               |      |
| `parsons-103` | The whole exercise depends on a syntax-highlighting library the engine never loads and never checks for; if it is absent nothing renders at all.                                                                                                                                                                       | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `Prism.highlightAllUnder` (2×) — <em>"Prism.highlightAllUnder($('#' + this.options.trashId).get(0)); // hack"</em> and the same for `sortableId`, both unguarded inside `createHTMLFromLists`. `Prism` is defined nowhere in the file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-104` | A reader can find the highlighter this one replaced, preserved commented out directly above it, together with the opt-out that went with it.                                                                                                                                                                           | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `prettyPrint` (8×), every occurrence inside a comment — <em>"// allow to use https://github.com/google/code-prettify/"</em> / <em>"// var prettyPrint = window.PR.prettyPrint \|\| window.prettyPrint;"</em> / <em>"// hack - js styling with Prism"</em>. The replacement has no equivalent opt-out.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |             |               |      |
| `parsons-105` | Every action the learner takes is timestamped and kept on the widget with a snapshot of the board, and on the deployed path it is handed to nobody.                                                                                                                                                                    | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `this.user_actions.push(logData);` (1×) under <em>"//To collect statistics, feedback should not be based on this"</em>, with <em>"time: new Date(),"</em> and <em>"output: this.solutionHash(),"</em>; the outbound hook is <em>"this.options.action_cb.call(this, logData);"</em> (1×) and `parsons-iframe.html` passes no `action_cb`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-106` | The very first thing recorded, before the learner has moved anything, is every fragment's text together with whether it belongs to the answer.                                                                                                                                                                         | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `bindings.push({ code: line.code, distractor: line.distractor });` (1×) inside <em>"this.addLogEntry({"</em> / <em>"type: 'init',"</em>, run unconditionally at the end of `createHTMLFromLists`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-107` | The engine records every distinct arrangement the learner has passed through and can say how many times they have been in this one and how long ago — and nothing on the deployed path ever asks.                                                                                                                      | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `state_path` (11×) — <em>"//State history for feedback purposes"</em> / <em>"this.state_path = [];"</em> / <em>"this.states = {};"</em>; the query is `whatWeDidPreviously` (1×, its definition only) returning <em>"{ visits: visits, stepsToLast: stepsToLast }"</em>.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |             |               |      |
| `parsons-108` | The stylesheet designs two per-fragment visual channels — a fill for whether a fragment is in the right place, a thick left bar for whether it is indented right — and the learner saw neither, because the page that loads it redefines the same element later at equal specificity.                                  | `G1-live` + UNSETTLED       | Gen-1 `parsons.css`, `border-left: 10px` (2×) — <em>"li.incorrectIndent { … border-left: 10px solid red; }"</em> and <em>"li.correctIndent { … border-left: 10px solid green; }"</em>; against Gen-1 `parsons.js`, where `markCorrectIndent` occurs **0 times** and `correctPosition` is added only by `markCorrect` (`parsons-070`). ⚠️ Instrument caveat: the two channels can never appear together **on one fragment** — a position error suppresses the whole indent pass via <em>"if (errors.length === 0) {"</em>, and inside it the two marks are exclusive on the indent test — so the `border` shorthand in the later position rules never overwrites a live indent bar [measured 2026-08-19]. **Across fragments they do coexist**: that gate is outside the loop, so one Check paints a mis-indented fragment red while every other correct one goes green (`parsons-097`).                                                                                                                                                                                                   |             |               |      |
| `parsons-109` | The stylesheet's indent verdicts are told apart by a colour keyword and nothing else — identical geometry, and no live rule anywhere in the file uses weight, an icon, a symbol or a dash pattern — so a learner who cannot separate red from green had no second channel to read even where the design did render.    | `G1-live` + UNSETTLED       | Gen-1 `parsons.css`, `border: 1px solid red` (3×) against `border: 1px solid green` (2×) — the two indent rules differ in the colour keyword and in nothing else — <em>"border-left: 10px solid red;"</em> under `li.incorrectIndent` against <em>"border-left: 10px solid green;"</em> under `li.correctIndent`, each above an identical 1px all-round border. ⚠️ **The channel never reached the screen, and neither did the fill.** `parsons-iframe.html` links this stylesheet and then opens its own `<style>` after it, redefining the same element — <em>"background: #1e1e1e;"</em> / <em>"border: 1px solid #464647;"</em> under `.sortable-code li`. Both selectors weigh (0,1,1), the host's block is later in document order, and this stylesheet declares `!important` **0 times** [all measured 2026-08-19]. So every per-fragment feedback colour and the 10px bar lose the cascade. The column-level rules survive, because the host's `.sortable-code ul` sets only `list-style`, `padding` and `margin`. No live rule uses weight, an icon, a symbol or a dash pattern. |             |               |      |
| `parsons-110` | The stylesheet grades its wrong-side colour by how specific the complaint is — a faint wash for the whole column, a strong fill for one fragment — and the right side has no such gradation; only the column half of that scheme ever reached the learner.                                                             | `G1-live` + UNSETTLED       | Gen-1 `parsons.css`, `#ffefef` (1×) on <em>"ul.incorrect { … background-color: #ffefef; }"</em> against `#ffbaba` (1×) on <em>"li.incorrectPosition, … background-color: #ffbaba;"</em>; the correct side uses `#dff2bf` (2×) at both scales, identically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |             |               |      |
| `parsons-111` | The only thing telling the learner a fragment can be moved is the mouse cursor, and a learner navigating by keyboard is given nothing at all.                                                                                                                                                                          | `G1-live` + UNSETTLED       | Gen-1 `parsons.css`, `cursor: move` (1×), the file's only live cursor declaration; `focus` occurs **0 times** and `outline` **0 times** [measured 2026-08-19]. The one `cursor: pointer` sits inside a commented block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |
| `parsons-112` | A second exercise type was designed into the stylesheet and switched off — clickable blanks embedded in the code, marked by a dashed border, shouting in red when still unanswered.                                                                                                                                    | `G1-dead` + UNSETTLED       | Gen-1 `parsons.css`, `jsparson-toggle` (3×), the whole block commented — <em>"border: 1px dashed black;"</em> / <em>"cursor: pointer;"</em>, and the unanswered state carries <em>"border-color: red;"</em> together with `content: "??";`, quoted in a code span because its own double quotes would nest inside the emphasis form. The dashed-versus-solid distinction between an editable thing and a verdict exists nowhere in the live file, where every border is solid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |             |               |      |
| `parsons-113` | Whatever asks the engine for the learner's assembled program gets it re-indented with two literal spaces a level, regardless of the width the exercise was configured with.                                                                                                                                            | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `ParsonsWidget.prototype.getStudentCode` (2×, at two separate definitions) — the surviving one runs `var indents = repeatStringNumTimes('  ', item.indent);` then <em>"lines.push(indents + text);"</em>. ⚠️ Instrument caveat: that fragment is quoted in a **code span** rather than `<em>` because its two literal spaces are this row's whole subject, and prettier collapses them inside emphasis — measured 2026-08-19 by isolation, one fixture row per form: `<em>` returned one space, the code span returned both. This is the **inverse** of the hazard `_TEMPLATE.md` § What Pass 1 writes records, and it is owed there as an amendment. The earlier definition is overwritten and would have thrown regardless: it reads <em>"this.parson.getModifiedCode("</em>, and `parson` is a property of the grader, never of the widget.                                                                                                                                                                                                                        |             |               |      |
| `parsons-114` | Setting either of the two options that would enable testing crashes the exercise before it starts, because both grader classes were removed and only their filenames survive.                                                                                                                                          | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `UnitTestGrader` (1×) and `VariableCheckGrader` (1×), each occurring only at its `new` site — <em>"this.grader = new UnitTestGrader(this);"</em>. The four grader sources are empty comment blocks naming <em>"graders/variable-check.js"</em>, <em>"graders/turtle.js"</em>, <em>"graders/unit-test.js"</em> and <em>"graders/language-translation.js"</em>; the sole registration is <em>"graders.LineBasedGrader = LineBasedGrader;"</em>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |             |               |      |
| `parsons-115` | The engine's only path for showing the learner an error is a blocking browser dialog, and nothing ever calls it.                                                                                                                                                                                                       | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `displayError` (1×, its definition only) — <em>"ParsonsWidget.prototype.displayError = function (message) {"</em> / <em>"if (this.options.incorrectSound && $.sound) {"</em> / <em>"alert(message);"</em>. The sound it would play depends on a jQuery plugin the file neither loads nor defines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-116` | A reader of the engine's message catalogue would believe it diagnoses block structure to the learner — a block that never started, one never closed, a mismatched end — and half the catalogue has no caller in either language.                                                                                       | `G1-dead` + UNSETTLED       | Gen-1 `parsons.js`, `no_matching_open` (2×), `no_matching_close` (2×), `block_close_mismatch` (2×), `unittest_error` (2×), `unittest_output_assertion` (2×) and `unittest_assertion` (2×) — two occurrences each, which is one per translation table and **no call site**; `translations.` occurs 8× and reaches only `order`, `lines_too_many`, `lines_missing`, `block_structure` and `no_matching`. One is declared <em>"no_matching_open: function (lineNro, block) {"</em> and ends <em>"' never started.'"</em> — the message is built across seven source lines and is not quoted here as one, because a composition is not a quotation. The block-matching trio promises a grader class the engine never possessed.                                                                                                                                                                                                                                                                                                                                                               |             |               |      |
| `parsons-117` | An author can build a one-column exercise in which the learner reorders the program in place with no pool to drag from, and the engine renders, logs and grades that shape throughout.                                                                                                                                 | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `if (this.options.trashId) {` (6×) — the mode is guarded at six separate sites, among them <em>"this.createHTMLFromLists([], idlist);"</em> against <em>"this.createHTMLFromLists(idlist, []);"</em> in `shuffleLines`, and <em>"html = this.codeLinesToHTML(solutionIDs, this.options.sortableId);"</em> in the markup path. The deployed host passes `trashId`, so the learner met the two-column shape (`parsons-050`); the other shape is the ancestor of Gen-2's pool-and-solution split.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |             |               |      |
| `parsons-118` | An author can turn indentation off entirely and offer an order-only puzzle: sideways dragging then does nothing and every fragment stays at level zero.                                                                                                                                                                | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `can_indent` (3×) — the default plus two consumers, <em>"var new_indent = this.options.can_indent"</em> / <em>"? code_line.indent + Math.floor(leftDiff / this.options.x_indent)"</em> / <em>": 0;"</em> (the tab indent is not transported — prettier collapses leading tabs inside emphasis), and <em>"grid: that.options.can_indent ? [that.options.x_indent, 1] : false,"</em>. `parsons-064` quotes the same ternary and claims only its true arm. The deployed host passes `can_indent: true`; Gen-2's `canIndent` (`parsons-003`, `parsons-006`) descends from this option.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |             |               |      |
| `parsons-119` | Whatever hosts the exercise is handed, on every Check, a machine-readable error record naming each misplaced fragment by its position in the author's original snippet — a different numbering from the one the learner is shown.                                                                                      | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `log_errors` (7×) — <em>"log_errors.push({"</em> / <em>"type: 'incorrectPosition',"</em> / <em>"lines: incorrectLines,"</em> filled by <em>"incorrectLines.push(lineObject.orig);"</em>, against <em>"log_errors.push({ type: 'incorrectIndent', line: i + 1 });"</em>. The two channels number differently: `orig` is the index in the authored text, `i + 1` the position in the learner's own column. It reaches the host through <em>"this.options.feedback_cb(fb); //TODO(petri): what is needed?"</em>, and the deployed host does pass `feedback_cb` — unlike `action_cb` (`parsons-105`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |             |               |      |
| `parsons-120` | Anyone with the page open can reach the engine itself by name — the widget, the fragment type and the grader registry are all published as globals, one of them with the file's own stated reason.                                                                                                                     | `G1-live` + UNSETTLED       | Gen-1 `parsons.js`, `// expose the type for testing, extending etc` (1×) — <em>"window.ParsonsCodeline = ParsonsCodeline;"</em>, with `ParsonsWidget._graders = graders;` (1×) — a code span, because prettier escapes the bare underscore to `\_` inside emphasis and corrupts the quotation — and <em>"window['ParsonsWidget'] = ParsonsWidget;"</em> (1×), all three executing at load. `parsons-090` records that the configuration object is not reachable; the constructors and the registry are.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |             |               |      |

---

## Close conditions

**The Pass-1 gate** — run and recorded at the seeding commit, not asserted. Use
the **row-scoped** form in
[`_TEMPLATE.md` § Close conditions](./_TEMPLATE.md#close-conditions), never a
whole-file grep: this ledger explains the marker and names the banner in running
prose, so an unscoped `grep -c 'UNSETTLED'` returned **51** against the 47 rows
this ledger then held, and the banner counted **2** [measured 2026-08-15 at
`c0bd56a6` — this ledger is what found that defect in the check as first
published; the numbers are that commit's and are kept as the record of it].

**Row-scoped, at the current 120 rows, all eight checks pass** [measured
2026-08-19]: 120 rows, 120 carrying `UNSETTLED`, banner once, zero `—` cells,
zero disposition values, zero `walked`/`found`/`Design owed`, ids `001`–`120`
with no gap or duplicate. The unscoped `UNSETTLED` count exceeds the row count,
which is the same defect the 2026-08-15 measurement records, one generation on.

⚠️ **The published transport check is structurally blind to rows `045`–`120`.**
It parses a citation shaped as _Gen-2_ or _Gen-3_, a backticked `.md` filename
and a `§` heading; those rows cite Gen-1 source by function, method or literal
instead — so they land in `NO-CITATION` and no quotation of theirs is ever
diffed. The seeding run's own numbers show it: `rows=120 parsed=57 nocite=76`,
where **57 is unchanged from the 80-row baseline across an append of 40 rows**.
~~**A Gen-1 arm does not exist and is owed to `_TEMPLATE.md`.**~~ **BUILT and
RUN at `ea381b82`** —
[`_TEMPLATE.md` § The Gen-1 arm](./_TEMPLATE.md#the-gen-1-arm). It closes its
census over this ledger: `citations=78 checked=69 refused=9`, every refusal
named [measured 2026-08-21 against the pinned `spiral-lens` quarry root].

Pending it, this pass re-grepped every `<em>` quotation in every Gen-1 row back
against its source with `grep -F` — 147 fragments across rows `045`–`120`, 9
skipped as truncated [measured 2026-08-19]. It found **seven transport defects
in this pass's own rows before they were committed**, which is why those are not
in the tree. Run over the whole population it then found ~~eight more, and
**none of them is this pass's**~~ — ⚠️ **both halves STRUCK 2026-08-24: it is
sixteen, and three of them ARE this pass's. The correction is under the table,
and this sentence stated the same two facts wrongly one line above it.**

| rows                                                 | non-transporting quotations                 |
| ---------------------------------------------------- | ------------------------------------------- |
| ~~`045`–`080` — STEP 1a's, committed at `fd6066b3`~~ | ~~**8**, across 7 rows~~ → **12** ⚠️        |
| ~~`081`–`120` — this pass's~~                        | ~~**0**~~ → **3**, in `108` ×2 and `110` ⚠️ |

The seven are `parsons-050`, `-052`, `-058`, `-066` (two fragments), `-068`,
`-072` and `-076`, and every one is a class this pass hit and documented: a
multi-line CSS rule or statement **composed onto one line**, which is not a
quotation; or **whitespace prettier collapsed inside emphasis** — `parsons-066`
quotes a comment whose source carries two spaces after `//` where the cell has
one.

**They are named and measured here rather than repaired**, because they belong
to another pass and another commit group, and a repair is its own unit with its
own gate run. **This is the defect class the campaign exists to catch, sitting
in the ledger it holds up as its own exemplar.**

⛔ **UPDATED 2026-08-24, after the Gen-1 arm ran. Three claims above are wrong
and are struck rather than left standing beside this.**

1. **It is 16, not 8.** The arm found seven more non-transporting quotations —
   `050`, `051` ×2, `056`, `108` ×2, `110` — every one a fragment the 2026-08-19
   stopgap **skipped as truncated**, and every one the same
   composed-onto-one-line class. Plus a sixteenth of a different class:
   **`parsons-119` declares `log_errors` at 7× where it occurs 8 times across 7
   lines** — the count was taken by LINE.
2. ⚠️ **"This pass's rows carry 0" is false.** Rows `108` and `110` were added
   by `7c3da9aa`, which IS this pass [measured 2026-08-21 by AR-1: `git log -S`
   on the row id]. Three of the **fifteen non-transporting quotations** are this
   pass's own — and so is `parsons-119`'s miscount, which makes **four of the
   sixteen defects**. ⚠️ The two denominators are different populations and this
   sentence conflated them until 2026-08-24.
3. **The check no longer has no Gen-1 arm.** It was built and run at `ea381b82`,
   and it is what found items 1 and 2.

**Canonical enumeration, including the two non-defects on `parsons-109` that are
an artefact of the one-cell schema:**
[`_TEMPLATE.md` § The Gen-1 arm](./_TEMPLATE.md#the-gen-1-arm). ⛔ **That
enumeration is owed a move INTO this file** — the skeleton every ledger is
copied from should not carry a parsons-specific run — and that move is recorded
as the first item of the ledger-re-cut session.

Campaign-close conditions, none of which this ledger yet meets:

- **Every** row has a non-empty `discharged by` that **resolves** — on any
  disposition (human ruling 2026-08-14);
  [§ At AR-5](../FIDELITY-METHOD.md#at-ar-5) is the single definition.
- **Open rows = 0**, under § At AR-5's four-part definition.
- **No `UNSETTLED` survives**, and the `PASS 1 — SEEDED` banner is gone.
- Every `restore — DEFERRED` row names an owner **and** a ruling, and the ruling
  resolves as a heading in the reference it cites.
- **Pass 2 has run over every source, and it has NOT.** Stated per source rather
  than as one condition, because this ledger's cohorts are at different passes
  (human ruling 2026-08-20; see [§ Seed census](#seed-census)'s `pass` column):
  - Gen-1 second root, 7 files — **DONE**, `048`–`080`.
  - Gen-1 core, `parsons.js` + `parsons.css` — **DONE**, `081`–`115`.
  - Gen-2 `README.md`, `DOCS.md`, `types.ts` — **NOT RUN.** Rows `001`–`044` are
    lister output only, and `types.ts` has never been read in full.
  - Gen-3 port — **NOT RUN.**
- **Pass 3's counter-ledger can no longer answer either question — NOT MET.** It
  has run once, on `parsons.js` alone, opening `116`–`120`, and its own reader
  reported the ledger still thin in two named places: the engine's configuration
  space, and the structured feedback channel. Question 2 has never been asked of
  this ledger at all
  ([§ Pass 3](../FIDELITY-METHOD.md#pass-3--the-counter-ledger)).
- This lens's row in [SPEC.md § Roll-up](../SPEC.md#roll-up) has no blank cells.
