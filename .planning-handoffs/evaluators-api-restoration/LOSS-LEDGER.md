<!-- TRANSITIONAL — pruned when the restoration campaign completes. -->
<!-- cspell:ignore arities astless backpressure blackbox blackboxing kinding codegen desugars disjointness Embedder empts extensioned instrumentable instrumenter lifecycles mistrace mistracing numstat retarget rethrowing thunked unbuilt uncoerced widenings unproduced unrun -->

# Evaluator public-API restoration — LOSS LEDGER

Every reference public-API member, classified. This artifact's absence is the
recorded root cause of the original failure — a multi-week port verified every
increment against its own new contract and never once diffed that contract
against the implementation it replaced, so no gate could catch the loss; its
per-increment discharge rule is the campaign's anti-failure mechanism (plan § 1:
every Phase-1 increment names the rows it discharges).

**The discharge rule extends to PHASE 0 (human ruling 2026-08-12, HR-21).**
Phase 1 was never the only place a contract can be authored thin. Every Phase-0
unit's artifacts must NAME what they encode: which rulings of record, which of
the fourteen tracer forward-compatibility requirements
(`git show a8a0128d:<this directory>/research-digests-2026-08-05.json`, key
`.result.tracers`), and which classification rows (by member name) and FLAGs the
design answers — carried as a `## Discharges` section in the unit's README, so
the check is whether every identifier in it resolves. A Phase 0 that cannot cite
what it discharges is thin by definition, and `ar-1`/`ar-2` check that citation
IN ADDITION TO the judgment their focus areas call for — a complete citation
list is necessary, never sufficient. The rule exists because a design gate can
pass on a plausible-looking artifact that silently drops the nuance its inputs
carried — the same failure as the original port, one phase earlier.

**Classification vocabulary** (HR-4): `restore` (the default — the member
returns, reference-faithful) · `supersede` (a port-side or new design wins —
ONLY with a named strength argument) · `drop` (the member does not return — ONLY
with human sign-off). Compound classes in ratified and audit use, defined here
so the vocabulary stays closed: `ADDITION` (an enrichment the reference lacked,
riding in reference style per HR-8); `restore-as-doc` (the member returns as a
documentation obligation); `already survives` (no loss row — the member lives on
under another name); `drop-as-loss` (a drop recorded WITH its loss
acknowledged); and the deferral form `restore — DEFERRED (<owner>, <ruling>)` —
the member restores, discharged by the named future campaign; spelling pinned
here, with the ratified error-`line` row's "restore on intercept; DEFERRED on
run" as its precedent. **RATIFIED 2026-08-06 in one pass** (human ruling
2026-08-06, § Rulings of record below: one bulk confirm of every proposed row
plus eight individual escalated decisions): no row remains open; each carries
its ruling or its sign-off date. Evidence cites the reference/port files
directly; the full audit trail with per-member verification is
`research-digests-2026-08-05.json` — recoverable via
`git show a8a0128d:.planning-handoffs/evaluators-api-restoration/research-digests-2026-08-05.json`
(the working-tree copy was retired with the ruling logs, 2026-08-11; the audit
keys are nested under `.result` — `.result.runAudit`, `.result.interceptAudit`)
`[relayed: the two fidelity audits wf_7cb9c40a-f4c and wf_5c5b0dc6-37e, re-verified per row by the planning research agents]`.

## Shared handle contract (`shared/`)

| member                                                                                        | classification                          | rationale                                                                                                                                                                                                                                                                             | evidence                                                    |
| --------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `Execution<TEvent,TResult>` (AsyncIterable & PromiseLike & {result, cancel})                  | restore (ruled HR-3/HR-8)               | the contract half that never crossed; the campaign's centerpiece                                                                                                                                                                                                                      | `shared/types.ts:48`                                        |
| `ExecutionBase<TResult>` (PromiseLike & {result, cancel} — the settle base beneath Execution) | ADDITION (ruled 2026-08-17, P0-K ar-1)  | STRENGTH: forward-compat requirement 13 needs a nameable result-only floor, and the reference's own RunHandle is not AsyncIterable — HR-3's third ground bars an `AsyncIterable<never>` sham; named in base vocabulary because the reference reserves the Handle suffix for widenings | quarry `run/types.ts:199-205` vs `shared/types.ts:48`       |
| `.then` / `await handle` batch mode                                                           | restore (ruled HR-6)                    | both consumption modes return                                                                                                                                                                                                                                                         | `create-execution.ts:29-30`                                 |
| `.result` memoized, always-settles                                                            | restore (ruled HR-3)                    | —                                                                                                                                                                                                                                                                                     | `shared/types.ts:52`                                        |
| `.cancel()` named, idempotent                                                                 | restore (ruled HR-3)                    | a consumer outside the `for await` (Stop button) can stop the run again                                                                                                                                                                                                               | `shared/types.ts:55-56`                                     |
| creation-time auto-start (queueMicrotask drain)                                               | supersede (ruled — ratified 2026-08-06) | STRENGTH: creation-inert start-at-first-consumption keeps both modes byte-equivalent from first consumption on, adds hold-without-running, and kills the reference's one-microtask claim race                                                                                         | `create-execution.ts:107-117`                               |
| replay / re-iteration (`===`-identity re-yield, `.result.logs` cache)                         | supersede (ruled HR-2)                  | human-affirmed non-goal; the engine's items array is the record                                                                                                                                                                                                                       | `create-execution.ts:166-190`                               |
| `createExecution`'s test suite                                                                | restore ~16 of 21 (ruled HR-4)          | 3 replay rows drop per HR-2; ~2 adapt (cancel-before-iteration's `undefined` result contradicts the restored typed contract — adapted row, this table is its record)                                                                                                                  | `shared/tests/create-execution.test.ts` (21 tests measured) |

## run — handle

| member                                                       | classification                | rationale                                                                                                                                         | evidence                                                       |
| ------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| `RunHandle` name + shape                                     | restore (ruled HR-8)          | —                                                                                                                                                 | `run/types.ts:199-205`                                         |
| `.code` eager                                                | restore (ratified 2026-08-06) | HR-4 default; trivial echo of `spec.facts.source.value`; "caller already holds it" is redundancy, not strength                                    | `run/types.ts:202`                                             |
| `.ast` SYNC (`Program \| undefined`)                         | restore (ratified 2026-08-06) | echo of gate-guaranteed `facts.ast`; the `undefined`-iff-parse-failed arm is unreachable under the gate — documented at the field, not re-modeled | `run/types.ts:203`                                             |
| `.options` / `ResolvedRunOptions` (seconds always populated) | restore (ruled HR-8/HR-9)     | —                                                                                                                                                 | `run/types.ts:166-170, :204`                                   |
| handle `Object.freeze`                                       | restore (ratified 2026-08-06) | port returned a bare literal with no recorded rationale; both neighbors hardened                                                                  | quarry `run.ts:452-459` vs port `create-run-stream.ts:118-123` |

## run — options

| member                                                                       | classification                  | rationale                                                                                                                                                                                                                                                                                                                                      | evidence                                              |
| ---------------------------------------------------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| `seconds` (default 5, settable)                                              | restore (ruled HR-4 round)      | engine already carries `EvaluateSpec.seconds`; this row is the recorded authority overturning the ratified no-seconds kind ruling (the two `carries no seconds budget` test rows ride into the deprecated region unedited)                                                                                                                     | `run/types.ts:153`; engine `types.ts:50-51`           |
| `iterations`                                                                 | already survives                | renamed through `workerConfig.iterationLimit`; no loss row                                                                                                                                                                                                                                                                                     | `run/types.ts:154`                                    |
| "iterations omitted = no guard injection" (source-runs-unmodified guarantee) | supersede (ratified 2026-08-06) | STRENGTH: always-splice is iteration-guard's documented commitment; guard-still-counts is the safer default; the run total is then real on every halt                                                                                                                                                                                          | quarry `run/types.ts:148-149`                         |
| `io` / `IoMocks` (3 dialog slots, sync-or-Promise)                           | restore (ruled HR-9)            | worker dialog traps over the engine's existing `onCall` seam; the evaluator's onCall wrapper validates/classifies ITSELF (plan § 4 closure-flag pattern)                                                                                                                                                                                       | `run/types.ts:134-141`; engine `types.ts:85-91`       |
| native-dialog fallback for unmocked verbs                                    | supersede (ruled HR-9)          | STRENGTH: no mock → classified io-error outcome beats a main-thread-blocking native dialog AND beats today's bare worker `ReferenceError`. History carried so this is never re-litigated blind: the reference's own DOCS record the no-dialog posture as tried, tested, and RESCINDED (D5b) — HR-9 re-decides it WITH that record on the table | quarry `run.ts:510-522`; quarry DOCS D5b              |
| cancel-waits-for-in-flight-mock                                              | supersede (ratified 2026-08-06) | STRENGTH (structural): the port engine's uninterruptible-call + discard-on-stop design cannot express it; a transported slow-mock cancel test would fail — adapted, with the discard semantics documented                                                                                                                                      | quarry `run.ts:214-216`; engine `evaluate.ts:319-353` |
| `io-error` termination cause (mock failure as a classified run outcome)      | restore (ruled HR-9)            | live again the moment io returns; classified consumer-side via the closure flag, no engine change                                                                                                                                                                                                                                              | quarry `run.ts:274-289`, `types.ts:130-133`           |

## run — result and error taxonomy

| member                                      | classification                                           | rationale                                                                                                                                                                                                                                                                                                                                                                                                                        | evidence                                                                              |
| ------------------------------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `RunResult` name; `outcome` 5-value; `ok`   | restore (ruled HR-8)                                     | reference vocabulary wholesale; the port's ended×reason retires with the deprecated region; map-settlement translates at the engine seam (spec = table T1, committed into P0-K README)                                                                                                                                                                                                                                           | `run/types.ts:91-96, :117-120`                                                        |
| machinery-defect discrimination             | ADDITION (ruled HR-8)                                    | port-stronger, rides as an added error kind — the reference disguised machinery failure as `{kind:'javascript', name:'WorkerError'}`                                                                                                                                                                                                                                                                                             | port `run/types.ts:64-68`                                                             |
| `result.ast` on every parsed outcome        | restore (ratified 2026-08-06)                            | echo; rides the result per HR-8                                                                                                                                                                                                                                                                                                                                                                                                  | quarry `run/types.ts:117-120`                                                         |
| `rejections` (JeJ violations)               | drop-as-loss (resolved 2026-08-06)                       | the ORCHESTRATOR already owns level marking (screening `Violation` + language-levels `validate` + orchestrate marking, live-wired), and embody is level-blind by contract [read: embody/DOCS.md:101-105 — "a language level decides what is ALLOWED"] — the assignment is already recorded in the regions' own docs; nothing returns to evaluators                                                                               | `validating/types.ts:160-164`                                                         |
| parse error arm                             | supersede (ratified 2026-08-06)                          | STRENGTH: the kind's gate-guaranteed facts make a parse arm unreachable — genuine re-partition upstream (embody `StageCause`); residue noted: error `name` has no home, offset optional-vs-guaranteed                                                                                                                                                                                                                            | quarry `run/types.ts:67-72`; embody `types.ts:63-70`                                  |
| formatting error arm                        | drop (resolved 2026-08-06 — no doc line needed)          | formatting is level-owned editor-support DATA (the `format` channel [read: language-levels/README.md:46-48]) consumed by the orchestrator's editor adapter [read: orchestrate/README.md:113]; it is not a run gate anywhere in the current architecture — the assignment was already recorded, so the drop was never silent                                                                                                      | `validating/types.ts:130-132`                                                         |
| error `line` on a learner throw             | restore on intercept; DEFERRED on run (ruled 2026-08-06) | intercept's threw arm already carries the wrap-style loc — richer than the reference's line; run has NO wrap layer, so run-side call-site instrumentation is a NAMED future increment, not this campaign's; wrap-style per the region ruling — NEVER a stack parse, except the one sanctioned no-live-frame residual (human ruling 2026-08-19, the P0-I bullet); the reference's untested numbers are not the conformance target | quarry `run/types.ts:35`, `create-worker-script.ts:219-237`                           |
| error `column`                              | drop (ruled — human-supplied prior, re-measured)         | declared-never-populated in the reference itself: the extractor regex discards the column group; 1 occurrence = the declaration                                                                                                                                                                                                                                                                                                  | quarry `run/types.ts:36` `[measured: grep -n column over reference run/*.ts → 1 hit]` |
| `phase: 'creation' \| 'execution'`          | restore (ruled 2026-08-06 — E2 IN SCOPE)                 | pedagogically real (didn't-construct vs ran-then-threw); E2 splits the engine's one try/catch + adds a halt field — additive, its own AR pair under ceremony full                                                                                                                                                                                                                                                                | quarry `run/types.ts:37`; engine bootstrap single-try                                 |
| `TimeoutResultError.limit` (+ `durationMs`) | restore (ratified 2026-08-06)                            | once seconds is settable, echoing the budget on the timeout arm follows; `durationMs` is computed by the engine and currently discarded — free                                                                                                                                                                                                                                                                                   | quarry `run/types.ts:41-47`; engine `types.ts:193`                                    |
| `IterationLimitResultError.limit`           | drop (signed 2026-08-06)                                 | caller supplied `spec.iterations` and holds its own copy; the trip record (loop index + span) is strictly richer                                                                                                                                                                                                                                                                                                                 | quarry `run/types.ts:50-57`                                                           |
| clean-arm `iterationCount`                  | ADDITION (ruled 2026-08-06)                              | surfaced on the clean arm: cost already paid under always-splice; reference-plus in reference style (rides RunResult/InterceptResult, not the settlement floor); the old kind's withholding ruling retires with the deprecated region                                                                                                                                                                                            | port `run/types.ts:107-113`                                                           |

## run — protocol and infrastructure

| member                                                                    | classification                       | rationale                                                                                                                                                                                     | evidence                            |
| ------------------------------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| dialog io-request round-trip protocol (3 typed SAB writers, worker traps) | supersede (ruled HR-3)               | STRENGTH: transport re-homed into the generic engine call channel; the consumer-visible loss is entirely the io-option row, restored above                                                    | engine `types.ts:222-234`           |
| exported worker-protocol surface (19 symbols)                             | drop (signed 2026-08-06)             | engine internals are deliberately private; zero external importers ever existed; FLAG: the quarry trace/semantics WAS a real cross-module consumer — resurfaces at that future port, not here | engine `worker/types.ts:5-6`        |
| tagged `IoResult` union                                                   | drop (audit-refuted NOT_A_LOSS)      | port wire self-describes finer (4 codes vs 3) and validates what the reference silently coerced                                                                                               | Appendix A                          |
| Blob-URL worker script (source-as-a-value)                                | supersede (ratified 2026-08-06)      | STRENGTH: static module URL is the bundler-supported path; per-run variation is pinned unsupported. FLAG carried: no test catches a bundler regression — prose + eslint-disable only          | port `create-run-stream.ts:149-153` |
| per-round-trip budget charge on the SAB handshake                         | drop (signed 2026-08-06)             | engine charges per-yield, zero per-call — deliberate (calls pause the budget); a call-heavy program pays no synthetic fee                                                                     | engine `evaluate.ts:339`            |
| D5b decision record engagement                                            | restore-as-doc (ratified 2026-08-06) | the new run DOCS must cite and answer the reference's io rescission note — cheapest row in the ledger, closes the "re-took a rescinded posture without engaging the record" finding           | quarry run DOCS D5b                 |
| README runnable commands (sandbox launch, 2 test-tier commands)           | restore (ratified 2026-08-06)        | docs-only, cheap                                                                                                                                                                              | quarry `run/README.md`              |

## intercept — handle

| member                                                                      | classification                                 | rationale                                                                                                                                                                                                                                                                                                                            | evidence                                                                         |
| --------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| `InterceptHandle` name; full generator surface (`.next`/`.return`/`.throw`) | restore (ruled HR-5)                           | —                                                                                                                                                                                                                                                                                                                                    | quarry `intercept/types.ts:261`                                                  |
| the lib-`AsyncGenerator` TYPE token                                         | supersede (ratified 2026-08-06)                | STRENGTH: TS 5.9's `AsyncGenerator` drags in `AsyncDisposable` + a required `return()` argument; an explicit interface reproduces the reference's MEMBER signatures faithfully instead                                                                                                                                               | the TS lib's `AsyncGenerator` + `AsyncDisposable` declarations (es2018 + esnext) |
| reference `.throw` semantics                                                | supersede (ratified 2026-08-06)                | the reference never specified it — `.throw` was the NATIVE generator method (only next/return overridden), a latent defect that skipped teardown; spec: `.throw(e)` ≡ `fail(e)` + settle `outcome:'fail'`, `reason: e`                                                                                                               | quarry `intercept.ts:735-894`                                                    |
| reference `.return()` drain-through-`origNext`                              | supersede (ratified 2026-08-06)                | STRENGTH: verbatim it deadlocks on a suspended ask against the engine's out-of-band teardown (pin intercept:292, retained); spec: latch → release ask → engine stop → settlement → resolve with the full result. Stated behavior change: `for await`-break now awaits settlement (reference behavior) where the port broke instantly | plan § 4                                                                         |
| `.fail(reason)` + `outcome:'fail'` + `result.reason`                        | restore (ruled HR-8 — outcome includes `fail`) | the only structured consumer-stop channel; both quarry tracers carry it too                                                                                                                                                                                                                                                          | quarry `intercept/types.ts:282`, README § Fail                                   |
| `.code`, `.options` eager                                                   | restore (ratified 2026-08-06)                  | HR-4 default; trivial echoes                                                                                                                                                                                                                                                                                                         | quarry `intercept/types.ts:289, :296`                                            |
| `.ast` promise on the handle                                                | restore, re-derived (ruled HR-12)              | resolves to the CURRENT embody's entwined record (`facts.entwined.byPath`), not a link/ shadow record; the member is renamed `entwined` and made eager (two ruled departures, human ruling 2026-08-19 — the P0-I bullet)                                                                                                             | quarry `intercept/types.ts:315`; port embody `types.ts:191-215`                  |
| `then`/`result`/`cancel`                                                    | restore (ruled HR-3/HR-6)                      | drain-on-await cancels at the first STRUCTURALLY-unanswered ask (ruled HR-7)                                                                                                                                                                                                                                                         | shared rows above                                                                |

## intercept — options

| member                                           | classification                    | rationale                                                                                                                                                                                                                                           | evidence                            |
| ------------------------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `InterceptOptions` name; `seconds`               | restore (ruled HR-8 / HR-4 round) | as run's seconds row                                                                                                                                                                                                                                | quarry `intercept/types.ts:226-230` |
| `io` dialog mocks                                | restore (ruled HR-9)              | answered at the `serveAsk` seam BEFORE a pending-interaction is minted; no mock → pending-interaction (the named port-stronger carve-out — lens-rendered interaction beats native dialogs)                                                          | quarry `intercept/types.ts:201-209` |
| `io.console` / `IoConsole` per-method callbacks  | restore (ruled HR-9)              | callbacks awaited before the program continues, per reference; the unmocked-method native forwarding does NOT return (record-only) and the event's `method` is an open string over a whole-surface trap (human ruling 2026-08-19 — the P0-I bullet) | quarry `intercept/types.ts:177-181` |
| `iterations` `Infinity`/omitted = skip injection | drop (signed 2026-08-06)          | as run's always-splice row                                                                                                                                                                                                                          | quarry `intercept/types.ts:216-224` |

## intercept — result

| member                                               | classification                      | rationale                                                                                                                                               | evidence                            |
| ---------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `InterceptResult` name; `events` array on the result | restore (ruled HR-8)                | kills the awaiting-without-pulling-stalls regression; replay identity NOT restored (HR-2)                                                               | quarry `intercept/types.ts:154-162` |
| `outcome` 6-value incl. `fail`; `ok`                 | restore (ruled HR-8)                | —                                                                                                                                                       | quarry `intercept/types.ts:114-120` |
| `reason?: unknown`                                   | restore (ruled HR-8, rides `.fail`) | —                                                                                                                                                       | quarry `intercept/types.ts:157`     |
| `code` / `options` echoes                            | restore (ratified 2026-08-06)       | HR-4 default                                                                                                                                            | quarry `intercept/types.ts:158-159` |
| `ast` record on the result                           | restore, re-derived (ruled HR-12)   | the entwined record (real graph), not a shadow tree; renamed `entwined` with the handle member (human ruling 2026-08-19 — the P0-I bullet)              | quarry `intercept/types.ts:160`     |
| `visitCounts`                                        | restore (ruled HR-12)               | accumulated thread-side by resolved `nodePath`; null-key policy decided in P0-I (loc-null events excluded or sentinel-bucketed — named, not accidental) | quarry `intercept/types.ts:161`     |
| `eventsByNode`                                       | ADDITION (ruled HR-12)              | the §13-clean replacement for `node.events[]` back-refs                                                                                                 | semantics-tracer precedent          |

## intercept — entwining

| member                                                  | classification                                                    | rationale                                                                                                                                                                                                                                                                                                                                 | evidence                                                               |
| ------------------------------------------------------- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `nodePath` per event                                    | restore (ruled HR-12)                                             | enumerable plain string, joined thread-side via offsets → `entwined.byOffset` → deepest-exact-span ascent                                                                                                                                                                                                                                 | quarry `link/types.ts:145`                                             |
| offset pair on events                                   | ADDITION (ruled HR-12)                                            | stamped near-free from the wrap's original-text parse; the coordinate valid in both spaces — answers the I8 carried-forward question                                                                                                                                                                                                      | wrap parse `start`/`end`                                               |
| `node` live reference                                   | restore-as-accessor (ruled HR-12 — mechanism ratified 2026-08-06) | NON-ENUMERABLE accessor resolving the REAL `EntwinedNode` — JSON-safe, §13-clean; an enumerable own-property ref would re-create the shadow-graph/cycle problems the quarry's own newer tracer abandoned                                                                                                                                  | quarry `link/types.ts:147`; `trace/semantics/tracing/types.ts:845-850` |
| `prev` / `next` timeline links                          | restore-as-accessors (ruled HR-12)                                | installed inside `onMessage` before yield; mutable-pointer closure = the NAMED no-mutable-closures exception                                                                                                                                                                                                                              | quarry `link/types.ts:152, :162`                                       |
| `callee` / `calleePath`                                 | restore-as-accessor/derived (ruled HR-12)                         | resolved off the joined CallExpression node                                                                                                                                                                                                                                                                                               | quarry `link/types.ts:170, :175`                                       |
| `node.events[]` back-refs                               | supersede (ruled HR-12)                                           | STRENGTH: embody's graph is frozen and cannot grow arrays; `eventsByNode` on the result carries the same join, JSON-safe                                                                                                                                                                                                                  | quarry `link/types.ts:100`                                             |
| `nodePathSource` 3-state provenance                     | drop (audit-refuted)                                              | 2 of 3 values unreachable in the port architecture; `'instrumented'` ≡ `loc !== null`                                                                                                                                                                                                                                                     | Appendix A                                                             |
| `loc === node.loc` identity                             | drop (signed 2026-08-06)                                          | loc survives as a fresh per-event span; identity served replay-adjacent navigation                                                                                                                                                                                                                                                        | quarry `link/types.ts:148`                                             |
| `'enclosing-fallback'` (stack → deepest enclosing node) | restore (ruled 2026-08-06 — AST-side)                             | deepest-enclosing-node via `entwined.byOffset` at halt time — cheap, exact, §13-clean, NEVER a stack parse except the one sanctioned no-live-frame residual (human ruling 2026-08-19, the P0-I bullet — the fallback's input, named); supersedes pin intercept:208 (disposition flipped in § Pin dispositions below by this ratification) | quarry `link/lookup-node-path.ts:45,:79`                               |
| link/ shadow AST tree (456 lines)                       | supersede (ruled HR-12)                                           | STRENGTH: duplicates `entwined` nearly field-for-field, mints a second node-identity space, and its post-freeze mutation pattern conflicts with DEV.md §13                                                                                                                                                                                | `link/build-location-index.ts` vs embody `types.ts:191-215`            |

## intercept — error taxonomy and stream

| member                                                              | classification                     | rationale                                                                                                                                                                                             | evidence                                                         |
| ------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| streamed `ErrorEvent` (errors as step-stamped events IN the stream) | restore (ruled 2026-08-06)         | in-timeline error rendering returns — the event union gains an error arm (step + loc stamped, landing in order); the settlement keeps the structured form. Both, as the reference had                 | quarry shared `types.ts:188-195`                                 |
| `phase` on error events                                             | drop (signed 2026-08-06)           | near-constant under the gate architecture                                                                                                                                                             | quarry `intercept/types.ts:52-55`                                |
| `ValidationResultError` + violations                                | drop-as-loss (resolved 2026-08-06) | relocated live (screening `Violation` + language-levels validate + orchestrate marking); behavioral delta recorded: intercept no longer gates execution on level — sanctioned (level-agnostic ruling) | port `lib/screening`                                             |
| `ExecuteMessage.scriptMode` (sloppy/`with`)                         | supersede (ratified 2026-08-06)    | STRENGTH: the kind's execution axis is a recorded deliberate collapse                                                                                                                                 | port `src/lib/study-lenses/evaluators-deprecated/types.ts:44-49` |
| `createWorkerScript(): string`                                      | drop (signed 2026-08-06)           | replaced by an importable module + fake transport; 27 string tests became 170 behavioral tests                                                                                                        | audit digest                                                     |

## sandbox pages (BOTH lineages per HR-15)

| member                                                                                                                                                                                          | classification        | rationale                                                                                                                                                                      | evidence                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| REFERENCE run sandbox: IO toggles, styled dialogs, console dumps (`[run handle]`/`[run result]`), unexpected-throw catch, presets (+ timeout preset), max-seconds control                       | restore (ruled HR-15) | the reference pages are fidelity targets; the slow-mock cancel rig returns ADAPTED (cancel-waits-for-mock is superseded — the rig demonstrates discard-on-stop instead)        | quarry `run/sandbox.html`                         |
| DEPRECATED-PORT sandboxes: intercept C2 card flows (5 checkpoint items), full-data serializer (present-but-`undefined` rendered, live `respond` shown), per-arm presets; run's page equivalents | restore (ruled HR-15) | "carefully crafted — migrate them too with the same attention to fidelity"; full feature inventory of both deprecated pages taken at R6/I8 execution and appended here as rows | port `run/sandbox.html`, `intercept/sandbox.html` |
| sandbox cadence                                                                                                                                                                                 | ruled HR-15           | pages built EARLY in each chain, extended per increment; every user-observable increment fires its own 🔍; variables' declared skip OVERRIDDEN — it gets a page                | human ruling 2026-08-06 (HR-15) below             |

## docs-only rows

| member                                                                          | classification                    | rationale                                                                                      | evidence                                          |
| ------------------------------------------------------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| cancel-mechanics 6-phase table + promptness commitment                          | drop (signed 2026-08-06)          | docs density; the new DOCS states the sequenced teardown instead                               | quarry run README                                 |
| engine docs "three arms" sentence + links into `evaluators-deprecated/types.ts` | restore-accuracy (W1.d increment) | goes stale under HR-8/HR-14; architectural-contract edit with human approval, never a drive-by | engine `types.ts:163-166`, README:203/413, DOCS:6 |

## Appendix A — refuted findings (NOT losses; barred from classification rows)

Recorded so nobody re-audits them:

- **async-mock arm** (Promise-returning mocks, rejection mapping,
  cancel-during-mock semantics, per-pause charge): all limbs live in the shared
  engine (`onCall` sync-or-Promise, rejection → call-error,
  uninterruptible-call + discard-on-stop, per-yield charge). The original
  finding scoped its greps to danger/intercept and missed the engine.
- **`nodePathSource`**: 2 of 3 values unreachable in the port architecture; the
  real residue is the enclosing-fallback row above (now ruled restore).
- **`IoResult`**: the port wire is strictly finer and validates what the
  reference silently coerced (e.g. `undefined`→`false` on confirm).

## Appendix B — trace/variables (landed by the W1.b audit; rows ratified at gate G3)

The W1.b fidelity audit's product: 195 proposed rows over the quarry variables
tracer (`src/lib/embody/lib/evaluating/trace/variables/`, 15 files / 3813
lines), grouped B1–B6 below, every row awaiting G3 ratification. Provenance: six
surface readers (whole-file, one file one reader) then four adversarial
verifiers over the merged set — ZERO rows killed, ZERO members missing, 30
corrections applied at synthesis
`[relayed: the audit fan-out's labeled payload — readers r1–r6, verifiers v1 evidence-fidelity / v2 ruling-consistency / v3 completeness / v4 refuter]`.
Verifiers re-opened every cited cross-file source and the enumerated drift set;
residual drift where present is ≤3 lines
`[relayed: v1's blanket adjacency finding]`. Classification defaults per HR-4;
the DEFERRED compound on B6's consumer-seam rows follows the ratified ledger's
own restore-DEFERRED precedent (error-`line` row) — those members live in
embody's adapter seam, which HR-10 places in a future campaign.

### History verdict — HR-16 CLOSED for variables (sweep question); cross-lineage config open

The additive-history / richest-revision-is-HEAD claim HOLDS: the tree was born
`1200a164` (2026-06-13 — README/DOCS/types), `a8af4776` landed
project-scope-table the same day, implementation followed incrementally
(`095dcbc9`, `7c8e0eb5`, `039a9645`, `43127973`, `f742a700`, `b3b85a2a`,
`6fecf9e5`, `139b2ca8`), and post-`12b02327` exactly two commits touch the tree
— `0fca239e` (whole-tree path move; renders as RENAME under `--follow --numstat`
on nine files, and as ADD on the six files whose old paths git pairs with the
copy-B snapshot instead) and `59043f52` (prettier sweep; README 214→213, the
sole net line regression in the tree's history). No content-deleting commit
exists; the only `--diff-filter=D` entries are the old-path sides of the two
path moves (`12b02327`, `0fca239e`)
`[relayed: r6 historyVerdict, re-verified and twice corrected by v1]`. HEAD ==
working tree == the richest revision of THIS lineage: no `git show <sha>:<path>`
recovery is warranted for any of the 15 files. Two load-bearing small commits
the rebuild should know: `6fecf9e5` is the ORIGIN of the `createTransport` seam
(flag 4), and `b3b85a2a` is the Array.from-for-Map Docusaurus-bundle fix — a
live browser-bundle constraint a rebuild can silently regress. The two HR-16
anchors (`68fa9981`/`c969412b`) predate this tracer by ~2 months and are
semantics-only — they remain PATTERN prior art for P0-K's config forward-compat,
not variables fidelity targets. A DIFFERENT-lineage historical tracer exists and
is flagged below (Aran advice, deleted), not folded into this verdict. The
`0--home` checkout's `…/lib/evaluating/` subtree is hollow — excluded; the audit
sourced only this repository.

### Copy-B seal

Copy B
(`src/lib/study-lenses--deprecated-architecture/embody/lib/evaluating/trace/variables/`,
6 files) is a DEAD ORPHAN: byte-identical to copy A outside engine-import
retargeting (deprecated→live, the naming inversion is real), but a structurally
broken subset — its `./types.js` / scope / validating import targets do not
exist under its tree, it is excluded from tsc (`tsconfig.json` excludes the
deprecated-architecture glob), and its whole history is the one migration commit
`0fca239e`. It contributes no independent behavior and no fidelity target; its
one value is as a worked example of the exact deprecated→live engine specifier
mapping the rebuild must perform
`[relayed: r6 sealFindings, verifier-confirmed]`.

### Forward-compat coverage

The 14 tracer forward-compat requirements (recover via
`git show a8a0128d:<this directory>/research-digests-2026-08-05.json`, key
`.result.tracers`) were re-pulled and re-verified at audit time; v3's
completeness sweep found every requirement either row'd or flagged — none
silently unaddressed. Requirement 9 is discharged by the campaign's W1.a engine
increment (plan of record § 8, W1.a — the D4 yield-charge opt-out); requirement
10 converges with the Aran-lineage config flag below.

### Flags — P0-V's design questions (FLAG, never silently classified)

The five standing topics, consolidated across all six readers:

1. **Settlement/outcome vocabulary (HR-8's business).** The quarry surfaces the
   engine's five outcomes AS-IS with an identity facade mapping
   (`types.ts:248-267`, `trace-variables.ts:160-176`); the worker authors its
   own halt shape `{natural, errorName, message, nodePath}`
   (`variables-worker-setup.ts:374-388`); the docs pin the five names and the
   browser suite asserts three as string literals (README:138-141; browser test
   :24/:32/:48/:57 — plus `engineError.cause === 'timeout'` as a SECOND layer
   under the outcome name); the instrumenter hard-codes the abrupt-reason
   literals `break`/`continue`/`error` INSIDE generated source
   (`instrument-variables.ts:416-417, :479` — AbruptReason is EXACTLY those
   three, `types.ts:413`), so a rename is a coordinated instrumenter+worker
   change; and embody re-exports the result vocabulary user-facing
   (`embody/types.ts` re-export block, DOCS), so an HR-8 rename has a second
   blast radius beyond the tracer tree.
2. **The kind reshape (code-first → spec).** Entry is
   `traceVariables(code, {seconds}?, createTransport?)`; the instrumenter is
   AST-first and never parses, with two hard constraints on any reshape: the AST
   must be parsed `preserveParens` (paren'd read node paths change otherwise)
   and the ORIGINAL source string must reach the instrumenter intact
   (offset-slice copying); the worker seam is raw engine `WorkerSetup` with the
   ScopeTable cast from `unknown` and a hand-written entry module; the
   projector's Validate→Project phase placement is caller-owned; every doc
   commitment is authored against the code-first signature; and
   PHASE-1-HANDOFF's return half (`EvaluationStream` + `settled`) is already
   superseded by HR-3's handle — its input half
   (`EvaluationSpec = {facts, execution, iterations?}`) stands as design intent.
3. **The eager-throw JEJ gate arm.** Phase 1 throws a plain untyped `Error`
   synchronously at the call; Phase 3's instrument boundary throws typed
   (`InstrumentBoundaryError`); the docs make the eager throw a STRUCTURAL
   commitment with the two throw boundaries split across phases. The handoff's
   refuse-as-data posture: its environment-stage caveat is about embody defects,
   NOT JEJ admission — the genuine handoff basis is the
   stream-or-`EvaluatorRefusal` return contract with no throwing arm named;
   ratified forward-compat requirement 12 keeps typed sync throws at the gate
   KIND-LEGAL. The consumer side has ALREADY BUILT on the throw (embody DOCS
   instructs try/catch; the deprecated trace-debugging lens parses the thrown
   message families) — flipping to refuse-as-data is a breaking consumer change,
   not cosmetic.
4. **The `createTransport` test-only seam.** Third param typed from the engine's
   PRIVATE `worker/types.js` (variables is a measured second cross-module
   consumer of that surface — the 19-symbol drop row's FLAG anticipated exactly
   one); invisible in the public type; the ENTIRE non-browser suite drives
   through it; UNDOCUMENTED in README/DOCS (a docs-led rebuild would not know it
   exists); propagated through embody's facade by wider-function assignability,
   no cast. Where the rebuild's injection point lives — private back-channel vs
   first-class spec field — is P0-V's.
5. **ScopeAnalysis re-homing.** FOUR quarry-internal imports with no greenfield
   counterpart (`scope/build-scope`, `scope/types`,
   `parse-old/build-node-path-map`, `parse-old/get-child-nodes` — the last with
   its documented property-enumeration-order dependency); the projector consumes
   `ScopeInfo` STRUCTURALLY (scope tree with per-scope declaration order,
   per-scope AST node, per-declaration `id` node — a flat or name-keyed
   replacement silently breaks shadowing and the sibling-loop fix); the
   `program`→`script` top-scope rename is the single vocabulary translation
   point; the correctness argument "the two never diverge because both are pure
   functions of the same program" survives ONLY if table and transform read one
   analysis; the handoff re-homes resolution onto
   `facts.entwined`/`facts.environment` — two independent analyses vs
   collapse-onto-facts is the decision.

Standalone flags:

- **Deprecated-engine projection (r1).** All settlement/handle presence rules
  rowed here project the DEPRECATED engine's contract
  (`trace-variables.ts:17-25`); each must re-verify against the live engine's
  Settlement on retarget.
- **Classic-`for` one-scope-per-loop (r2).** A KNOWN, documented divergence from
  the NM's fresh-environment-per-iteration, justified as value-equivalent in
  closure-free JEJ — needs a ruling under HR-4 before P0-V builds against either
  shape (it changes the event stream of every classic for loop).
- **The `with` contradiction (r2, narrowed by v4).** JEJ ADMITS WithStatement
  (`just-enough-js.ts:582`, easter-egg block); the entry-file comment claiming
  the gate rejects it is FALSE; tests pin instrument-then-runtime-SyntaxError.
  Default resolution under HR-4 + the two pins: restore the README behavior and
  bar the false comment; gating `with` at validation would edit the SHARED JEJ
  ruleset — a named-strength supersede proposal, not a free P0-V choice.
- **Engine-ended runs produce no closing pops (r3, corrected by v4).** Only
  normal/break/continue/error pops are producible; `'limit'` is in the type with
  no producer. The deprecated engine owns NO iteration limit (time budget only)
  — the question becomes live exactly when the kind's `iterations?` cap lands.
  Timeout/cancel kills the worker and open scopes never pop; P0-V decides what
  the rebuilt tracer emits when the ENGINE ends the run.
- **Snapshot-at-pop liveness (r3).** The registry stores live references for
  clone-safe values, so a `scope-pop` reports state AT POP — sound only while
  the tier tracks no object mutation; widening the value domain makes this a
  semantics decision.
- **Doc cross-reference targets (r5).** The README's authority stance is
  relative links into sibling tracers' types, embody NM-event types, and the JEJ
  notional-machine doc — whether those targets exist at the rebuild's
  destination decides how much of Vocabulary-pinning/Glossary transports
  verbatim vs re-authors.
- **Module-doc ownership naming (r5).** README excludes "the `trace/`
  module-level documentation" while DOCS calls the same thing "the `tracers/`
  module docs" — one name is stale; ownership after the rebuild is unresolved.
- **Phase-count discrepancy (r5+r1, counts corrected by v4).** DOCS enumerates
  SIX phases (headers :14/:21/:29/:45/:57/:61); the entry body comments FOUR;
  README:196 lists FIVE verb phrases. The rebuild needs ONE canonical phase
  count; Narrow and Settle live outside the entry function, which explains the
  four.
- **HR-16 cross-lineage config (r6, narrowed by v4).** A pre-quarry Aran-advice
  variables tracer (born `5aca3672`, 312 lines, DELETED at `f1f8b568`) carried
  per-variable allowlist + per-form gates
  (`variablesList`/`variablesDeclare`/`variablesAssign`/`variablesRead` plus
  `range`, consumed as real control flow; the student-configurable intent
  recorded at its `data/config.js:7-8`). HR-16's sweep obligation and
  non-foreclosure constraint BIND variables now; the one open question is
  whether cross-lineage Aran config is a restore-grade fidelity TARGET or
  citable reference only, given the quarry's explicit no-Aran posture. Converges
  with forward-compat requirement 10.
- **A REAL consumer lens exists (r6).** The research digest's "no consumer lens"
  is stale: the deprecated `trace-debugging` lens consumes
  `traceVariableLifecycle` end-to-end with four pinned settlement classes, and a
  live smoke page exists (below). Whether the lens is a fidelity target for the
  rebuild's consumer surface — the way HR-15 makes sandbox pages targets — is
  P0-V's question, and it currently binds the eager-throw decision.
- **De-facto sandbox exists (r6).** `src/pages/trace-debugging-smoke.tsx` is a
  real checkpoint surface (real `embody(code)`, seconds budget, 4-scenario
  script — completed/cancelled/admission-error/timed-out, COOP/COEP requirement,
  raw-event console tee) despite no `sandbox.html` under the variables tree.
  Whether HR-15's both-lineages union includes this React-harness page's feature
  inventory is P0-V's question; B5's sandbox ADDITION row stands either way.

<!-- markdownlint-disable MD037 MD038 — rows quote generated JS (template-literal backticks, __$vr names, edge-spaced snippets) inside table cells; both rules false-positive on those quotes -->

### B1 — handle, options, result, event types (the public surface)

| member                                                                                                                                     | classification                                                                    | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | evidence                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| traceVariables(code, options?, createTransport?) — default export                                                                          | restore (substance; entry SHAPE governed by FLAG 2/P0-V)                          | The tier's single end-to-end entry: code-first, options-second, returns a lazy typed handle synchronously. Validate and instrument run EAGERLY inside the call; everything else is lazy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | trace-variables.ts:54-58 — "export default function traceVariables(code: string, options: TraceVariablesOptions = {}, createTransport?: CreateTransport,): VariablesTraceHandle"; docblock trace-variables.ts:7-14 — "Validate and instrument run EAGERLY and are the only throwing boundaries"                                                                                                                     |
| options parameter defaults to `{}` (call with no options is legal)                                                                         | restore                                                                           | A 1-arg call is a supported public shape; the rebuild must not make options required.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | trace-variables.ts:56 — "options: TraceVariablesOptions = {}"; pinned by tests/trace-variables.test.ts:46 — "traceVariables('@@@')" (1-arg)                                                                                                                                                                                                                                                                         |
| option `seconds` — the ONLY option; omitted-vs-present is a spread, not a default                                                          | restore                                                                           | The tier owns no default. When `seconds` is undefined the key is OMITTED from the engine spec entirely (spread-if-defined), so the engine's own default (5) applies. A rebuild that writes `seconds: options.seconds ?? 5` would move ownership of the default and is a fidelity loss.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | trace-variables.ts:91 — "...(options.seconds === undefined ? {} : { seconds: options.seconds })"; types.ts:217-220 — "Time budget in seconds; the engine defaults to 5 when omitted"; engine default confirmed at src/lib/study-lenses--deprecated-architecture/lib/engine/types.ts:50 — "Default 5"                                                                                                                |
| `strict` is deliberately LEFT UNSET on the engine spec (engine default true)                                                               | restore                                                                           | The spec object never sets `strict`; the run is strict because the engine defaults it. The unit test pins that the assembled TransportInit carries strict === true.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | trace-variables.ts:75-92 (spec literal has no `strict` key) + trace-variables.ts:75-77 comment — "`strict` is left unset (engine default true)"; pinned by tests/trace-variables.test.ts:189 — "expect(init?.strict).toBe(true)"                                                                                                                                                                                    |
| CORRECTION — the `strict`-rationale comment is FALSE: JEJ ADMITS `with`                                                                    | restore                                                                           | trace-variables.ts:76-77 claims "the only sloppy-mode construct, `with`, fails the JEJ gate above and never reaches here". It does not: `WithStatement: true` is an admitted 'easter egg' in the JEJ ruleset, and the unit test shows a `with` program passing the gate, reaching the worker, and settling `errored` with a SyntaxError halt (nodePath null) — i.e. the strictness is enforced by the WORKER's strict-mode parse, not by the gate. Restore the BEHAVIOR (with-programs degrade to an errored SyntaxError halt, not a synchronous gate throw); do NOT carry the comment's reasoning forward, and do not 'fix' the rebuild by gating `with`.                                                                                                                                                                                                                                                                | trace-variables.ts:76-77 (the false claim); src/lib/embody/lib/validating/just-enough-js.ts:582 — "WithStatement: true" under the "easter eggs — not in reference.md" block (line 579); pinned by tests/trace-variables.test.ts:116-132 — "degrades a `with` program to an errored SyntaxError halt (a named boundary)", expecting outcome 'errored', errorName 'SyntaxError', nodePath null, engineError undefined |
| Handle is an `AsyncIterable<VariablesTraceEvent>` (streaming surface)                                                                      | restore                                                                           | The handle's primary consumption mode is `for await`; the wrapper's iterator only narrows the engine's `unknown` items by cast — no filtering, no re-wrapping (the thread logic already dropped malformed messages).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | types.ts:281-285 — `type VariablesTraceHandle = AsyncIterable<VariablesTraceEvent> & {...}`; trace-variables.ts:126-131 — `return (await inner.next()) as IteratorResult<VariablesTraceEvent>`                                                                                                                                                                                                                      |
| Iterator `return()` FORWARDS to the engine iterator (break === cancel)                                                                     | restore                                                                           | Breaking out of a `for await` routes through the engine's cancel and settles 'cancelled', keeping events already yielded. The guard (`if (inner.return)`) exists only because `return` is optional on the AsyncIterator type; the fallback returns `{done:true, value:undefined}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trace-variables.ts:132-144 — "the engine's iterator always provides it, which is what makes breaking a `for await` route through the engine's cancel"; pinned by tests/trace-variables.test.ts:136-154 — break after 1 event → outcome 'cancelled', events length 1, halt null                                                                                                                                      |
| The handle object is `Object.freeze`d (shallow)                                                                                            | restore                                                                           | Consumers cannot monkey-patch or replace handle members; a rebuild returning a plain object loses this guarantee.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | trace-variables.ts:125 — "return Object.freeze({"                                                                                                                                                                                                                                                                                                                                                                   |
| `.result` is a GETTER that builds a NEW promise on every access                                                                            | supersede (the kind's ratified memoized `.result` wins — shared-handle row, HR-3) | SUBSTANCE restores (every access resolves to the run's single {events, settlement}); the per-access promise IDENTITY yields to the kind. `get result()` calls `engine.result.then(...)` per access, so `handle.result !== handle.result` by identity (each is a distinct Promise resolving to a distinct freshly-mapped result object). Behaviorally safe because the engine's own `result` is one promise, but a rebuild caching the mapped promise (or exposing a plain property) changes observable identity. Flagging so the reshape decides deliberately rather than by accident.                                                                                                                                                                                                                                                                                                                                    | trace-variables.ts:147-154 — `get result(): Promise<VariablesTraceResult> { return engine.result.then(...) }`                                                                                                                                                                                                                                                                                                       |
| `.result` resolves to `{ events, settlement }`                                                                                             | restore                                                                           | The result record is the run's archive: every event that was yielded, plus how the run ended.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | types.ts:269-273 — "What `result` resolves with: every event, then how the run ended"; trace-variables.ts:149-152                                                                                                                                                                                                                                                                                                   |
| `result.events` is the engine's `items` array BY REFERENCE (cast, not copied)                                                              | restore                                                                           | No mapping, filtering, or copying happens at the facade — the engine froze the items array at settlement and the facade re-labels its element type. A rebuild that maps/copies loses the frozen-array identity and adds a per-run allocation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | trace-variables.ts:150 — `events: engineResult.items as ReadonlyArray<VariablesTraceEvent>`; engine contract src/lib/study-lenses--deprecated-architecture/lib/engine/types.ts:138-139 ("EngineResult.items")                                                                                                                                                                                                       |
| `.cancel(): void`                                                                                                                          | restore                                                                           | Zero-arg consumer stop, delegated straight to the engine handle; settles 'cancelled' with `halt: null`, and works BEFORE any pull.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trace-variables.ts:155 — "cancel: () => engine.cancel()"; types.ts:283; pinned by tests/trace-variables.test.ts:156-164 — "cancels before any pull" → outcome 'cancelled', halt null                                                                                                                                                                                                                                |
| `.fail(reason?: unknown): void` — reason passed BY REFERENCE                                                                               | restore                                                                           | The structured consumer stop for the prediction-quiz UX. The reason is not cloned, serialized, or normalized: the test asserts identity (`toBe`), not equality. Settles 'failed' with `halt: null`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | trace-variables.ts:156 — "fail: (reason?: unknown) => engine.fail(reason)"; types.ts:275-285 — "`fail(reason)` is the structured consumer stop the prediction-quiz UX needs"; pinned by tests/trace-variables.test.ts:166-176 — "expect(settlement.failReason).toBe(reason)"                                                                                                                                        |
| Full laziness: nothing runs until the first pull or `.result` access                                                                       | restore                                                                           | The factory call is synchronous and lazy; a cancel/fail before any pull settles without spawning a worker. This is inherited from the engine handle but is a stated commitment of THIS tier's type.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | types.ts:275-280 — "Fully lazy (nothing runs until the first pull or `result` access)"; trace-variables.ts:9-11 — "The engine RUN stays fully lazy"; engine handle contract src/lib/study-lenses--deprecated-architecture/lib/engine/types.ts:109-112                                                                                                                                                               |
| settlement.halt — `settlement.halt ?? null`, typed `VariablesHalt \| null`                                                                 | restore                                                                           | The tier NARROWS the engine's optional-`unknown` halt into a required nullable field: absent becomes explicit `null`. Present on every worker-side stop (natural end AND throw); null on cancel/fail/timeout/worker-crash. The worker's serializeHalt authored the shape verbatim — the facade only casts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | trace-variables.ts:164-167 — "halt: (settlement.halt ?? null) as VariablesHalt \| null" with comment "Null on non-worker stops (cancel/fail/timeout/worker-crash)"; types.ts:261-267; pinned by tests/trace-variables.test.ts:81-86 (natural halt object on completion) and :153/:163/:175 (halt null on cancel/fail)                                                                                               |
| settlement.engineError — key PRESENT only when the engine ended the run                                                                    | restore                                                                           | Conditional spread: absent (key omitted, not undefined-valued) when `settlement.error === undefined`. Distinguishes engine-made stops (timeout, worker crash) from worker-side ones.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | trace-variables.ts:169-171 — "...(settlement.error === undefined ? {} : { engineError: toVariablesEngineError(settlement.error) })"; pinned negatively by tests/trace-variables.test.ts:113 and :131 — "expect(settlement.engineError).toBeUndefined()"                                                                                                                                                             |
| settlement.failReason — key present iff `outcome === 'failed'`                                                                             | restore                                                                           | Gated on the OUTCOME, not on the value's presence, so a `fail(undefined)` still yields the key. Passed through by reference from the engine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | trace-variables.ts:172-174 — "...(settlement.outcome === 'failed' ? { failReason: settlement.failReason } : {})"; types.ts:265                                                                                                                                                                                                                                                                                      |
| settlement.durationMs — always present, pass-through                                                                                       | restore                                                                           | Consumed budget in ms, copied verbatim from the engine settlement; never recomputed at the facade.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trace-variables.ts:168 — "durationMs: settlement.durationMs"; types.ts:266                                                                                                                                                                                                                                                                                                                                          |
| The engine's `refinement` field is DELIBERATELY DROPPED (no `refineError` hook installed)                                                  | restore                                                                           | The engine settlement carries an optional `refinement` (the `refineError` annotation, present only on errored halts). This tier installs no `refineError` hook on the spec and its VariablesSettlement type has no `refinement` member — the worker already stamps nodePath/errorName into the halt, so refinement is redundant here. The omission is a deliberate contract, not an oversight; restore it as an omission and do not add a refinement channel in the reshape without a ruling.                                                                                                                                                                                                                                                                                                                                                                                                                             | trace-variables.ts:78-92 (spec literal — no `refineError` key); types.ts:261-267 (VariablesSettlement — no `refinement` member); engine field at src/lib/study-lenses--deprecated-architecture/lib/engine/types.ts:171-172 — "`refineError`'s annotation — present only on errored halts" and :97 "readonly refineError?"                                                                                           |
| `toVariablesEngineError` — projects EXACTLY `{ cause, name, message }`                                                                     | restore                                                                           | A field-by-field mirror of the engine's structured cause; the discriminant is `cause` (never string-matched from name/message). Explicit re-construction (not a spread), so any future engine field is dropped by design.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | trace-variables.ts:178-181 — "return { cause: error.cause, name: error.name, message: error.message }"; types.ts:242-246 (cause union 'timeout' \| 'worker-error' \| 'call-error' \| 'hook-error')                                                                                                                                                                                                                  |
| Gate throw #1 — parse failure message format                                                                                               | restore                                                                           | A plain `Error` (NOT a typed/tagged error) whose message is prefixed with the entry-point name: `traceVariables: not valid JavaScript — <parseError.message>`. Consumers and the unit test match on this prose. A rebuild renaming the entry point silently breaks the message contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | trace-variables.ts:106-110 — "`traceVariables: not valid JavaScript — ${report.parseError.message}`"; pinned by tests/trace-variables.test.ts:45-47 — "toThrow(/not valid JavaScript/u)"                                                                                                                                                                                                                            |
| Gate throw #2 — JEJ violation message format, with a fallback string                                                                       | restore                                                                           | `traceVariables: not Just-Enough-JavaScript — <first violation's message>`, falling back to the literal `'rejected construct'` when the violations array is empty or the first entry lacks a message. Only the FIRST violation is reported; the rest of the report is discarded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | trace-variables.ts:111-116 — "firstViolation?.message ?? 'rejected construct'"; pinned by tests/trace-variables.test.ts:49-53 — `traceVariables('var x = 1;')` → /not Just-Enough-JavaScript/                                                                                                                                                                                                                       |
| Gate condition includes `report.ast === undefined` (not just `!report.isValid`)                                                            | restore                                                                           | Two-part guard: an invalid report OR a valid-but-astless report throws. This is what lets the rest of the pipeline treat `ast` as non-optional.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | trace-variables.ts:62-65 — "if (!report.isValid \|\| report.ast === undefined) { throw gateError(report); }" then "const { ast } = report;"                                                                                                                                                                                                                                                                         |
| InstrumentBoundaryError propagates UNCAUGHT from the facade (typed, structurally identifiable)                                             | restore                                                                           | Phase 3's throw is not caught, wrapped, or re-messaged by the entry — it reaches the caller as-is, carrying `instrumentBoundary: true` and a `reason` so callers discriminate structurally without parsing prose. NOTE a doc defect: types.ts:428-429 says the error is one "the facade re-throws on the validate path" — it is neither caught nor thrown on the validate path; it originates in Phase 3 (instrument) and simply propagates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | trace-variables.ts:71-73 (bare call, no try/catch) — "Phase 3 — Instrument (throws InstrumentBoundaryError on labels / expression-target for-of)"; types.ts:435-438 (the type) and types.ts:428-429 (the inaccurate prose); pinned by tests/trace-variables.test.ts:55-68 — asserts `.instrumentBoundary === true` and `.reason === 'labeled-statement'`                                                            |
| `workerFactory` — the INLINE `new Worker(new URL('./variables-worker-entry.ts', import.meta.url), { type: 'module' })` bundling constraint | restore                                                                           | Load-bearing build constraint, not style: the URL construction MUST stay syntactically inline inside `new Worker(...)` or webpack's static worker detection emits a raw .ts asset and the run crashes. Also carries an eslint-disable for `unicorn/relative-url-style` — the `./` form is required for worker-bundling resolution. A rebuild that extracts the URL to a constant reintroduces the crash.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | trace-variables.ts:80-88 — "Inline `new Worker(new URL(...))` so webpack's static worker detection emits a real worker chunk; splitting it across modules (URL apart from `new Worker`) emits a raw .ts asset that crashes"                                                                                                                                                                                         |
| `workerConfig` = the projected ScopeTable; `threadLogic` = variablesThreadLogic                                                            | restore                                                                           | The spec's wiring: the static scope table crosses to the worker as `workerConfig` (clone-safe plain data), and thread-side narrowing is supplied as `threadLogic`. The unit test pins that the assembled TransportInit's workerConfig has the `$` (script-scope) key and that the code carries `__$vr.open` splices.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | trace-variables.ts:89-90 — "workerConfig: scopeTable, threadLogic: variablesThreadLogic"; pinned by tests/trace-variables.test.ts:187-188 — "expect(init?.code).toContain('\_\_$vr.open')" and "expect(init?.workerConfig).toHaveProperty('$')"                                                                                                                                                                     |
| Phase 2 runs ONE `buildScope(ast)`, fed only to the table — the instrumenter re-derives its own scope analysis                             | restore                                                                           | A deliberate non-sharing decision at the entry: the scope analysis is NOT threaded into instrumentation. A rebuild that 'optimizes' by sharing one analysis changes the instrumenter's contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | trace-variables.ts:67-69 — "One buildScope, fed only to the table; the instrumenter re-derives its own scope analysis internally"                                                                                                                                                                                                                                                                                   |
| PIPELINE-PHASE DISCREPANCY — the entry body enumerates FOUR phases, its header docblock SIX steps, DOCS.md six DIFFERENT ones              | restore-as-doc                                                                    | Three enumerations disagree and none is a subset-relabel of another. (a) The body's numbered comments: Phase 1 Validate, Phase 2 Project, Phase 3 Instrument, Phase 4 assemble-spec-and-call-factory (the handle wrap is unnumbered). (b) The header docblock: six arrow steps — validate → project → instrument → assemble spec → call factory → wrap handle — while citing "DOCS.md § Architectural Sketch, phases 1-6". (c) DOCS.md § Execution phases: 1 Validate, 2 Project, 3 Instrument, 4 Run and emit, 5 Narrow, 6 Settle. So the docblock's 5/6 (call factory / wrap handle) are NOT DOCS's 5/6 (Narrow / Settle), and the citation is therefore wrong. Reporting rather than picking: P0-V must choose ONE canonical phase vocabulary. DOCS's six is the pipeline of the TIER (it includes the worker/thread phases the entry does not itself perform); the entry's four are the phases of the ENTRY FUNCTION. | trace-variables.ts:59/67/71/75 (the four numbered body comments); trace-variables.ts:2-5 — "(DOCS.md § Architectural Sketch, phases 1-6) ... Validate (JEJ gate) → project the scope table → instrument → assemble the engine spec → call the engine factory → wrap the engine handle"; DOCS.md:12-66 § Execution phases items 1-6 — "4. **Run and emit**" (:44), "5. **Narrow**" (:58), "6. **Settle**" (:60)      |
| TYPE: `VariablesEventBase` — `step`, `nodePath`, `scopeInstanceId`, and the HOME-SCOPE rule                                                | restore                                                                           | Every event carries a monotonic worker-assigned `step`, a source-attributing `nodePath`, and a `scopeInstanceId`. The load-bearing rule: on scope events the id is that scope's own instance; on BINDING events it is the instance of the scope that DECLARES the binding (its home scope), not the innermost live scope — giving a stable per-binding-life key. The two readings differ only when an outer binding is touched from an inner scope. Losing this rule silently breaks any consumer keying on binding identity.                                                                                                                                                                                                                                                                                                                                                                                             | types.ts:118-136 — "it is the instance of the scope that DECLARES the named binding (its home scope), not the innermost live scope — so a binding's `initialize` and every later read/assign/increment of it carry one id, giving a stable per-binding-life key"                                                                                                                                                    |
| TYPE: `ScopePushEvent` — `{ event:'scope-push', scopeKind, variables: DeclaredVariable[] }`                                                | restore                                                                           | Folds the notional machine's `scope:push` plus per-binding `declare` into ONE event: every variable arrives in the burst, in source order, all in implicit TDZ. A rebuild emitting separate declare events changes the stream shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | types.ts:138-147 — "folding the NM's `scope:push` + per-binding `declare` into one event (every variable in implicit TDZ)"; pinned by tests/trace-variables.test.ts:78 and :97                                                                                                                                                                                                                                      |
| TYPE: `ScopePopEvent` — `{ event:'scope-pop', scopeKind, reason: ScopePopReason, variables: FinalVariable[] }`                             | restore                                                                           | The scope's cleanup census: final variables with status and (iff initialized) value, plus WHY control left, authored from the abrupt flag (seam 4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | types.ts:149-155; reason vocabulary types.ts:66-71; pinned by tests/trace-variables.test.ts:97 — empty program yields exactly ['scope-push','scope-pop']                                                                                                                                                                                                                                                            |
| TYPE: `InitializeEvent` — `{ event:'initialize', name, value, explicit }`                                                                  | restore                                                                           | First value for a binding. `explicit` distinguishes `let x = 5` (true) from `let x;` (false, value undefined) — a distinction the value alone cannot carry (`let x = undefined` is explicit).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | types.ts:157-166 — "`explicit` is `true` for `let x = 5`, `false` for `let x;` (value `undefined`)"; pinned by tests/trace-variables.test.ts:79                                                                                                                                                                                                                                                                     |
| TYPE: `ReadEvent` — `{ event:'read', name, value }`                                                                                        | restore                                                                           | Emitted only in a STANDALONE read position (the qualifier matters — the read inside an assignment/increment is carried as `priorValue` on that event, not as a separate read).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | types.ts:168-173 — "A binding's value is read in a standalone read position"; pinned by tests/trace-variables.test.ts:80                                                                                                                                                                                                                                                                                            |
| TYPE: `AssignEvent` — `{ event:'assign', name, operator, priorValue, nextValue?, wrote }`                                                  | restore                                                                           | `wrote` is false ONLY when a logical assign (`??=`/`\|\|=`/`&&=`) short-circuits, and is derived from the operator plus the PRIOR VALUE — never by comparing before/after values (so `x = x` still reports wrote true). `nextValue` is ABSENT (key omitted) when `wrote` is false. This derivation rule is the row's substance; a rebuild computing `wrote` by value comparison is a semantic regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | types.ts:175-188 — "`wrote` is `false` only when a logical assign (`??=` / `\|\|=` / `&&=`) short-circuits — derived from the operator and prior value, never from comparing values; `nextValue` is absent when `wrote` is `false`"                                                                                                                                                                                 |
| TYPE: `IncrementEvent` — `{ event:'increment', name, operator, form, priorValue, nextValue, returnedValue }`                               | restore                                                                           | THREE distinct values, all required: `priorValue` is the UNCOERCED old value; `nextValue` is the stored result; `returnedValue` is the COERCED value the expression yields (old for postfix, new for prefix). Collapsing returnedValue into prior/next loses the coercion story (`let s='1'; s++` → prior '1', returned 1, next 2) that the tier exists to teach.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | types.ts:190-203 — "`priorValue` is the uncoerced old value; `returnedValue` is the coerced value the expression yields (old for postfix, new for prefix); `nextValue` is the stored result"; form semantics types.ts:113-114                                                                                                                                                                                       |
| TYPE: `VariablesTraceEvent` — a FLAT six-member union (no nesting, no envelope)                                                            | restore                                                                           | Six members exactly: scope-push, scope-pop, initialize, read, assign, increment; discriminated by the `event` string field (not `type`, not `kind`). Flat — no wrapper envelope around a payload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | types.ts:205-212 — "The flat lifecycle-event union this tier streams"                                                                                                                                                                                                                                                                                                                                               |
| TYPE: `ValueSnapshot` = `unknown`, and `OpaqueValue` = `{ opaqueValue: true, typeOf }`                                                     | restore                                                                           | Clone-safe values (string, number, boolean, null, undefined, bigint, Date, RegExp) ride AS THEMSELVES, never wrapped; only non-cloneable values (a function bound to a variable, a realm object) become the tagged placeholder, so an emit never crashes the run. Explicitly NO `ValueRepresentation` tagging layer in MVP — a deliberate design refusal, not an omission.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | types.ts:34-53 — "Clone-safe values ... ride as themselves, never wrapped. There is deliberately no `ValueRepresentation` tagging layer in MVP: JEJ values are overwhelmingly primitives"                                                                                                                                                                                                                           |
| TYPE: `ScopeKind` = `'script' \| 'block' \| 'for' \| 'for-of'`                                                                             | restore                                                                           | Four kinds. Two renames/synthesis decisions ride here: the package scope analysis's top scope named `program` is RENAMED `script` to match the notional machine, and `for` is a SYNTHESIZED loop-head scope for `for (let i …)` with no direct counterpart in the upstream analysis. `for-of` is a genuine per-iteration NM scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | types.ts:56-64 — "(The package scope analysis names the top scope `program`; this tier renames it `script` to match the NM.)"                                                                                                                                                                                                                                                                                       |
| TYPE: `ScopePopReason` = `'normal'\|'break'\|'continue'\|'error'\|'limit'` — `'limit'` structurally present, NEVER produced                | restore                                                                           | Five reasons mirroring the NM. `'limit'` is deliberately reserved-but-dead in MVP because this tier owns no instrumentation limit. Keep the member (forward-compat with a limit-owning tier) and keep it unproduced; a rebuild that either drops it or starts emitting it changes the contract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | types.ts:66-71 — "`'limit'` is structurally present but never produced in MVP (this tier owns no instrumentation limit — README § Bounded context)"                                                                                                                                                                                                                                                                 |
| TYPE: `BindingKind` = `'let' \| 'const'` (no `var`)                                                                                        | restore                                                                           | `var` is absent by design — it is not JEJ (the gate rejects it, pinned by the unit test's `var x = 1;` violation case). The rebuild must not widen this union 'for completeness'.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | types.ts:73; pinned by tests/trace-variables.test.ts:49-53 — `traceVariables('var x = 1;')` throws /not Just-Enough-JavaScript/                                                                                                                                                                                                                                                                                     |
| TYPE: `BindingStatus` / `DeclaredVariable` / `FinalVariable`                                                                               | restore                                                                           | Two distinct binding shapes for two moments: DeclaredVariable `{name, kind}` on push (no value — everything is in TDZ); FinalVariable `{name, kind, status, value?}` on pop, where `value` is present IFF status is 'initialized'. Collapsing them into one shape loses the TDZ-at-entry invariant.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | types.ts:75-90 — "A binding as it enters a scope: declared into TDZ, not yet initialized" / "`value` is present iff initialized"                                                                                                                                                                                                                                                                                    |
| TYPE: `AssignOperator` — the exact 16-member JEJ operator list                                                                             | restore                                                                           | `=`, `+=`, `-=`, `*=`, `/=`, `%=`, `**=`, `&=`, `\|=`, `^=`, `<<=`, `>>=`, `>>>=`, `??=`, `\|\|=`, `&&=`. The union is pinned to the README pinning table, and the three logical forms at the tail are the ones that drive `wrote:false`. Widening or narrowing this list decouples the type from the JEJ validator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | types.ts:92-109                                                                                                                                                                                                                                                                                                                                                                                                     |
| TYPE: `IncrementOperator` (`'++'\|'--'`) and `IncrementForm` (`'prefix'\|'postfix'`)                                                       | restore                                                                           | Operator and form are ORTHOGONAL fields, not a fused four-member union — so a consumer can key on direction independently of return semantics (prefix returns new, postfix returns old).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | types.ts:111-114 — "Prefix returns the new value; postfix returns the old (README pinning)"                                                                                                                                                                                                                                                                                                                         |
| TYPE: `VariablesHalt` — `{ natural, errorName, message, nodePath: NodePath \| null }`                                                      | restore                                                                           | The worker-authored stop, present on EVERY worker-side stop — natural end AND throw alike (not an error-only channel). `natural:true` with `errorName:''` marks the clean end; `nodePath` is the stamped attribution (the exact node of a TDZ read or const reassignment) and is null on a natural end OR an unstamped throw (e.g. an undeclared-identifier ReferenceError that reaches the worker unwrapped). The unit test pins the exact natural-end object.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | types.ts:222-236; pinned by tests/trace-variables.test.ts:81-86 — "toStrictEqual({ natural: true, errorName: '', message: '', nodePath: null })" and :108-112 — const reassignment → natural false, errorName 'TypeError', typeof nodePath === 'string'                                                                                                                                                             |
| TYPE: `VariablesTraceResult` = `{ events: ReadonlyArray<VariablesTraceEvent>, settlement }`                                                | restore                                                                           | Exactly two members; readonly array. The result is the run's complete record — iterating after settlement yields nothing, so `events` is the only archive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | types.ts:269-273                                                                                                                                                                                                                                                                                                                                                                                                    |
| TYPE: `VariablesTraceHandle` = `AsyncIterable<VariablesTraceEvent> & { readonly result; readonly cancel; readonly fail }`                  | restore                                                                           | Four public members and no more — deliberately a THIN typed facade over the engine handle, adding no state, no replay, no pause. Every member is readonly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | types.ts:275-285 — "The tier's primary handle: a thin typed facade over the engine handle"                                                                                                                                                                                                                                                                                                                          |
| TYPE: `TraceVariables` — the exported call signature is TWO-ARG                                                                            | restore (substance; entry SHAPE governed by FLAG 2/P0-V)                          | The public type is `(code: string, options?: TraceVariablesOptions) => VariablesTraceHandle`; the implementation's third `createTransport` parameter is deliberately INVISIBLE to it. Restore the two-arg public type as the contract; the third-parameter seam itself is a FLAG (see flags).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | types.ts:287-291; trace-variables.ts:49-51 — "test-only engine seam (invisible to the public ... TraceVariables type a 2-arg caller uses)"                                                                                                                                                                                                                                                                          |
| TYPE/SEAM 1: `VariablesMessage` = `VariablesTraceEvent` (an ALIAS, not a distinct wire type)                                               | restore                                                                           | Load-bearing architecture: the WORKER authors the COMPLETE event (step, scopeInstanceId, value snapshots), so the wire message IS the public event and thread logic stays pure and stateless. The thread narrows and yields BY REFERENCE (the engine freezes at yield); a message failing the narrowing is DROPPED, not thrown on. A rebuild that moves step/id assignment thread-side breaks this alias and the statelessness it buys.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | types.ts:293-303 — "the message is therefore the clone-safe wire form of a VariablesTraceEvent ... yields it BY REFERENCE ... a message that fails the narrowing is dropped"                                                                                                                                                                                                                                        |
| TYPE/SEAM 2: `ScopeTable` = `Readonly<Record<NodePath, ScopeTableEntry>>`, entry = `{ scopeKind, variables }`                              | restore                                                                           | Keyed by scope nodePath; PLAIN DATA ONLY — explicitly no `Map`s, with the stated reasons that `Object.freeze` does not freeze a Map and a Map does not structured-clone to the same shape. Variables are in SOURCE ORDER, which is the order of the declare burst on scope-push. Built once, before instrumentation. (Per the brief's scope rule this TYPE is r1's row; r4 owns the projection behavior.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | types.ts:305-325 — "plain data only (no `Map`s — `Object.freeze` does not freeze a Map and a Map does not structured-clone to the same shape)"; entry types.ts:307-314                                                                                                                                                                                                                                              |
| TYPE/SEAM 3: `VariablesHelpers` — the `__$vr` protocol, 8 members + the thunk-ordering rule                                                | restore                                                                           | Eight members: `open`, `close`, `abrupt`, `landed`, `initialize`, `read`, `assign`, `increment` (the namespace's global name `__$vr` is its address, not a member). The pinned design rule: value-bearing helpers take THUNKS, not eager values, so a TDZ or const throw fires at the spec-correct moment INSIDE the helper where the nodePath can be stamped before propagation. Per-form ordering is also pinned: simple `=` evaluates its RHS FIRST (passed eagerly as `incoming`, matching the spec's RHS-before-PutValue order) while compound and logical forms read the target first then run the original expression inside `writerThunk` (no `incoming`). Member set, signatures and ordering are the stable seam; only the splice strings are increment-2's business.                                                                                                                                           | types.ts:327-392 — "Value-bearing helpers take **thunks**, not eager values, so a TDZ or const throw fires at the spec-correct moment INSIDE the helper, where the node path can be stamped"; assign signature types.ts:374-382; global name types.ts:330-331 — "addressed as the global `__$vr`"                                                                                                                   |
| TYPE/SEAM 4: `AbruptReason` = `'break'\|'continue'\|'error'` + the set/clear/read protocol                                                 | restore                                                                           | A worker-side flag whose CLEAR state means 'normal'. The protocol is the substance: SET to break/continue by `__$vr.abrupt(...)` spliced immediately before the statement, SET to 'error' by the scope wrap's catch (which rethrows); CLEARED by every value-bearing helper, by every scope `open`, and by `__$vr.landed()` after a loop; READ by `close` to author the pop reason — and closes NEVER clear it, because one break unwinds several scopes that must all report 'break'. That last clause is the easiest thing for a rebuild to lose.                                                                                                                                                                                                                                                                                                                                                                       | types.ts:394-413 — "Closes never clear the flag: a single break unwinds several scopes, all of which must report `'break'`"                                                                                                                                                                                                                                                                                         |
| TYPE: `InstrumentBoundaryReason` = `'labeled-statement' \| 'expression-target-for-of'`                                                     | restore                                                                           | Exactly two constructs that pass the JEJ gate but cannot be faithfully spliced, so the transform throws rather than mistrace. A labeled break/continue needs no separate reason — it cannot appear without an enclosing labeled statement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | types.ts:415-426 — "A labeled break/continue is covered by `'labeled-statement'` — it cannot appear without an enclosing labeled statement"; pinned by tests/trace-variables.test.ts:58-67                                                                                                                                                                                                                          |
| TYPE: `InstrumentBoundaryError` = `Error & { instrumentBoundary: true, reason }`                                                           | restore                                                                           | A REAL thrown Error (carries a stack, is `instanceof Error`) augmented with a discriminant tag — deliberately the same tag idiom as OpaqueValue — so callers identify it structurally without parsing the message. A rebuild using a plain object or a custom subclass loses one half or the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | types.ts:428-438 — "A real thrown `Error` — so it carries a stack and is `instanceof Error` — augmented with a discriminant tag ... so callers can identify it structurally without parsing the message"                                                                                                                                                                                                            |
| types.ts imports NOTHING — the contract module is dependency-free                                                                          | restore                                                                           | An explicit architectural property: `NodePath` is a local string alias and values are `unknown` precisely so the tier's contract depends on neither the engine's nor embody's types. This is what makes the type surface portable to the reshape; a rebuild that imports the kind/handle types INTO the contract module inverts the dependency the quarry deliberately avoided.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | types.ts:19-21 — "This module imports nothing: the tier is self-contained, and string-typed node paths and `unknown` values keep it decoupled from the engine and embody" (file has no import statements)                                                                                                                                                                                                           |
| The 35-name public export list of types.ts, grouped in six labelled bands                                                                  | restore                                                                           | A single `export type { ... }` block at the file tail exports 35 names in labelled groups (source+values, scope+binding vocabulary, event union, facade, seams, instrumentation boundary error). Everything in the file is public — there are no module-private types — so the reshape's contract surface is exactly this list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | types.ts:440-484 — the `export type {}` block, bands "// source + values" (:443), "// scope + binding vocabulary" (:447), "// event union" (:457), "// facade" (:466), "// seams" (:475), "// instrumentation boundary error" (:481)                                                                                                                                                                                |
| TEST-PINNED: two admission channels vs one settlement channel (the consumer's guard shape)                                                 | restore                                                                           | The documented consumer contract: a `try/catch` around the CALL catches admission errors (parse fail, JEJ violation, instrument boundary); the SETTLEMENT carries runtime errors (TDZ read, const reassignment) as `outcome:'errored'` with a stamped halt. Two channels, never merged. The unit test file is structured as the file's four inner describe blocks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trace-variables.ts:11-14 — "A consumer therefore guards two channels: a `try/catch` around the call (admission errors) and the settlement (runtime errors ... which arrive as `outcome: 'errored'` with a stamped halt)"; tests/trace-variables.test.ts:44 ("admission throws synchronously at the call (never builds a handle)") vs :100-114 (errored settlement)                                                  |
| TEST-PINNED: an EMPTY program still frames a script scope                                                                                  | restore                                                                           | `traceVariables('')` completes with exactly `['scope-push','scope-pop']` — the script scope is always present even with no declarations. An easy behavior for a rebuild to optimize away.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | tests/trace-variables.test.ts:89-98 — "frames an empty program with just a script push and pop", "expect(eventTypes(events)).toStrictEqual(['scope-push', 'scope-pop'])"                                                                                                                                                                                                                                            |

### B2 — instrumentation

| member                                                                                                                                                     | classification | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | evidence                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| instrumentVariables(program, source, scopeTable) → string (default export, 3-arg, pure)                                                                    | restore        | The instrumentation entry's arity and purity contract: takes the validated acorn Program, the ORIGINAL source string, and I1's clone-safe ScopeTable, returns instrumented source. Pure given an instrumentable program — no I/O, no mutation of inputs. The 3-arg shape is what makes the transform re-derivable and testable without the worker.                                                                                                                                                                                                | instrument-variables.ts:51-55 — "export default function instrumentVariables(program: Program, source: string, scopeTable: ScopeTable): string"; :5-7 — "Pure given an instrumentable program; it throws first, then splices."                                                                                                                               |
| Throw-first ordering: full boundary scan completes before ANY splicing                                                                                     | restore        | rejectUnsupported(program) runs as step 1, before the nodePathMap/scope derivation and before any rewrite. A rejected program never yields partial instrumented output. Lose this and a boundary program can emit half-spliced source.                                                                                                                                                                                                                                                                                                            | instrument-variables.ts:56-58 — "// 1. Boundary scan — throw before any splicing"; rejectUnsupported called at :58, rewriteNode not reached until :69                                                                                                                                                                                                        |
| Boundary rejection #1 — any LabeledStatement (subsumes every labeled break/continue)                                                                       | restore        | The pre-order scan hits the enclosing LabeledStatement before any labeled jump, so rejecting the label rejects labeled break/continue transitively — a deliberate single-check design, not two checks. Reason tag 'labeled-statement'.                                                                                                                                                                                                                                                                                                            | instrument-variables.ts:79-87 — "A labeled break/continue cannot exist without an enclosing LabeledStatement (a parse requirement), which this pre-order scan reaches first"; tests/instrument-variables.test.ts:296-306 pins both the bare label and `break loop;`                                                                                          |
| Boundary rejection #2 — expression-target for-of (`for (x of …)`, left not a VariableDeclaration)                                                          | restore        | A for-of whose left is an existing binding rather than a declaration cannot be faithfully spliced (the head initialize splice assumes a declarator id). Reason tag 'expression-target-for-of'.                                                                                                                                                                                                                                                                                                                                                    | instrument-variables.ts:89-97 — "if (left.type !== 'VariableDeclaration')" → 'expression-target-for-of'; tests:308-310 — instrument('let x; for (x of "ab") {}') throws 'for-of'                                                                                                                                                                             |
| InstrumentBoundaryError construction — a real Error augmented with `instrumentBoundary: true` + `reason`                                                   | restore        | createBoundaryError uses Object.assign(new Error(msg), …) so the thrown value keeps a stack and passes `instanceof Error`, while carrying a structural discriminant callers can test without string matching. A rebuild that throws a plain object or a bare Error loses both halves.                                                                                                                                                                                                                                                             | instrument-variables.ts:108-116 — "Object.assign(new Error(message), { instrumentBoundary: true as const, reason })"; :105-107 — "a real `Error` (stack + `instanceof Error`) augmented with the discriminant tag and reason"                                                                                                                                |
| Deliberate NON-rejections — `with`, sequence/comma expressions, optional chaining                                                                          | restore        | Named boundaries that are deliberately let through, not oversights: `with` and comma are explicitly excluded from the throw set, and optional chaining instruments normally (the object read still wraps). NOTE a contradiction worth resolving at P0-V: trace-variables.ts:76-77 claims `with` "fails the JEJ gate above and never reaches here", but JEJ ALLOWS WithStatement, so `with` does reach instrumentation and passes it — the README's account (runtime SyntaxError under the strict instrumented output) is the accurate one.        | instrument-variables.ts:8-10 — "`with` and comma/sequence are NOT thrown (README § Bounded context)"; tests:313-330 (three not.toThrow cases + the optional-chain read assertion); [measured: grep -n 'WithStatement' src/lib/embody/lib/validating/just-enough-js.ts → 582: "WithStatement: true,"]; trace-variables.ts:75-77 for the contradicting comment |
| Line-preserving transform — instrumented source has the original line count                                                                                | restore        | No splice inserts or removes a newline; every helper call is emitted on one line and inter-node whitespace is copied verbatim. This is what keeps runtime line/column attribution and editor highlighting honest against the ORIGINAL source. A rebuild that pretty-prints or emits multi-line wrappers silently breaks every downstream line-based consumer.                                                                                                                                                                                     | instrument-variables.ts:10-13 — "The transform is line-preserving: no splice inserts or removes a newline, so the instrumented source keeps the original line count"; tests:145-155 pin both the multi-line count and same-line statements                                                                                                                   |
| Original-source fidelity — every non-instrumented character is copied by offset slice from `source`, never regenerated from the AST                        | restore        | Each rewriter emits context.source.slice(...) for the gaps between rewritten children, so comments, formatting, original spacing, and parens survive byte-identically. There is no codegen/escodegen step anywhere. This is the mechanism behind line preservation and the reason offsets in the original source stay meaningful.                                                                                                                                                                                                                 | instrument-variables.ts:429-444 (rewriteChildren: "result += context.source.slice(index, child.start)" … trailing "slice(index, node.end)"); :451-467 (rewriteRange, same discipline between statements); :305, :309, :370, :391-396 slice uses inside the specific handlers                                                                                 |
| Bottom-up dispatch — REWRITERS table keyed by node.type, default fall-through to rewriteChildren                                                           | restore        | 13 node types get dedicated handlers (Program, BlockStatement, ForStatement, ForOfStatement, WhileStatement, DoWhileStatement, VariableDeclaration, AssignmentExpression, UpdateExpression, MemberExpression, Identifier, BreakStatement, ContinueStatement); everything else is a faithful rebuild with reads wrapped. Handlers embed children's already-rewritten text, so there is NO offset bookkeeping across the tree — the property the design explicitly claims generalizes intercept/wrap-call-expressions.ts.                           | instrument-variables.ts:537-552 (the REWRITERS table, all 13 entries); :136-141 — "Bottom-up: every handler embeds its children's already-rewritten text, so there is no offset bookkeeping across the tree"; :147 — "REWRITERS[node.type] ?? rewriteChildren"                                                                                               |
| rewriteChildren sorts direct children by `.start` before splicing                                                                                          | restore        | getChildNodes returns children in PROPERTY-ENUMERATION order, which is not source order for nodes with several node-valued properties. The `.toSorted((a,b) => a.start - b.start)` is therefore load-bearing: without it the running `index` offset walks backwards and the rebuild emits duplicated/negative slices. Easy to drop as 'redundant' in a rebuild.                                                                                                                                                                                   | instrument-variables.ts:434 — "getChildNodes(node).toSorted((a, b) => a.start - b.start)"; parse-old/get-child-nodes.ts:26 — "A flat array of all direct child nodes, in property enumeration order"                                                                                                                                                         |
| Addressing re-derivation — nodePathMap via buildNodePathMap(program), read-resolution chain via buildScope(program).root                                   | restore        | The flat ScopeTable can place scope wraps but cannot answer "is this identifier a read of a declared, in-scope binding?", which needs the scope CHAIN. So the transform re-derives BOTH the node paths and the scope tree from the same `program`. The convergence argument is explicit: both are pure functions of the same input, so the table's keys and the transform's addresses cannot diverge. A rebuild that passes a pre-computed path map from elsewhere must preserve that same-input purity or the addresses drift.                   | instrument-variables.ts:15-22 — "Read resolution … needs the scope CHAIN at a node, which the flat table cannot give, so the scope analysis is re-derived here via `buildScope` … The two never diverge because both are pure functions of the same `program`"; :62-69                                                                                       |
| Program scope wrap — the script scope is ALWAYS wrapped, even for an empty program                                                                         | restore        | rewriteProgram unconditionally wraps at the program's path ("$") — no scope-table lookup, unlike blocks. So every run emits at least one open/close pair at "$". The empty-program test pins the exact emitted string, making this the tightest single characterization of the wrap grammar.                                                                                                                                                                                                                                                      | instrument-variables.ts:153-162 — "Program: always the script scope — wrap the whole body"; tests:102-106 — instrument('') === '**$vr.open("$"); try {} catch (**$e) { __$vr.abrupt("error"); throw **$e; } finally { **$vr.close("$"); }'                                                                                                                   |
| Declaring vs non-declaring block — scope wrap only when the block's path is a ScopeTable key                                                               | restore        | A block that declares nothing keeps its braces and gets NO open/close: the notional machine elides empty scopes. The ScopeTable membership test is the sole predicate, which is exactly how the transform stays in lockstep with I1's projection. The braces are re-emitted by the handler itself (the wrap grammar carries no braces of its own).                                                                                                                                                                                                | instrument-variables.ts:166-187 — "if (context.scopeTable[path]) { return `{${wrapScope(path, inner)}}`; } return `{${inner}}`;"; :167 — "the NM elides empty scopes"; tests:236-241 pin both arms                                                                                                                                                           |
| Classic `for` — ONE synthesized for-scope wrapping the whole loop, not per-iteration                                                                       | restore        | A declaring head (`for (let i …)`) gets a single open-before / close-after wrap around the entire loop statement. This is a KNOWN, documented divergence from the notional machine (which models a fresh environment per iteration), justified as value-equivalent in closure-free JEJ. Any rebuild that 'fixes' this to per-iteration changes the event stream shape and must do so as a deliberate ruling, not a bugfix.                                                                                                                        | instrument-variables.ts:190-205 — "a declaring head (`for (let i …)`) gets ONE synthesized for-scope wrapping the whole loop (open before, close after — not per-iteration)"; README.md:166-172 — "Per-iteration environment copies for classic `for` … this tier models one synthesized for-scope per loop"; tests:244-256 (own path, nested path)          |
| for-of — per-iteration scope carried INSIDE the body block, with an initialize prelude                                                                     | restore        | The wrap lives inside the body's braces so it re-runs each iteration; the prelude emits an `initialize` of the loop binding with explicit=true (the binding is never TDZ at that point). Crucially the initialize is attributed to the BINDING ID's path, not the declarator's, because a for-of head has no initializer expression so the id is the highlight target — a deliberate attribution choice a rebuild would naturally get wrong by copying the declarator rule.                                                                       | instrument-variables.ts:207-254, esp. :227-231 — "The binding id's path (not the declarator's) is the initialize attribution: a for-of head has no initializer expression, so the id is the highlight target" and the emitted `initialize(path, name, name, true)`; tests:258-262 — '\_\_$vr.initialize("$.body.0.left.declarations.0.id", "c", c, true)'    |
| for-of head reconstruction — only the `right` (iterable) expression is rewritten; the head text left/between is copied verbatim                            | restore        | rewriteForOf hand-assembles the head from three verbatim slices plus a rewritten `right`, so the declaration target is never read-wrapped (it is a binding site, not a read) while the iterable's reads DO wrap. This is an exclusion that no generic descent would produce.                                                                                                                                                                                                                                                                      | instrument-variables.ts:245-251 — head = source.slice(node.start, left.end) + source.slice(left.end, right.start) + rewriteNode(right, …) + source.slice(right.end, body.start)                                                                                                                                                                              |
| while / do-while — no synthesized scope, but the loop landing clear still follows                                                                          | restore        | Unscoped loops declare nothing in their head, so no push/pop; they still need the post-loop `landed()` because a break/continue inside them sets the abrupt flag. Dropping the landed() on unscoped loops leaves a stale abrupt flag that mis-authors the reason on the NEXT scope pop.                                                                                                                                                                                                                                                           | instrument-variables.ts:256-263 — "while / do-while: no synthesized scope, but the loop landing clear follows"; tests:282-286 pin `__$vr.landed();` after a while loop                                                                                                                                                                                       |
| VariableDeclaration — per-declarator rewrite preserving all inter-declarator source                                                                        | restore        | The handler walks declarations in order, copying the `let `/`const ` keyword, the commas, and the trailing semicolon verbatim, and rewriting only the declarator spans. Multi-declarator statements therefore keep one distinct path per declarator.                                                                                                                                                                                                                                                                                              | instrument-variables.ts:267-285 (the index-walking loop over `declarations`); tests:131-135 — 'let a = 1, b = 2;' contains initialize("$.body.0.declarations.1", "b", (2), true)                                                                                                                                                                             |
| Explicit declarator → `__$vr.initialize(<declarator path>, <name>, (<rewritten RHS>), true)` — RHS evaluated EAGERLY, parenthesized                        | restore        | The initializer value is passed as an argument, not a thunk, so it is evaluated before the helper runs (the real evaluation order). The extra parens around the value are hygiene: they neutralize comma/low-precedence RHS forms. Path is the DECLARATOR's, not the id's (contrast the for-of rule above).                                                                                                                                                                                                                                       | instrument-variables.ts:304-308 — "return `${prefix}${HELPER}.initialize(${path}, ${nameLiteral}, (${value}), true)`"; tests:118-121 — initialize("$.body.0.declarations.0", "x", (1), true)                                                                                                                                                                 |
| Implicit declarator (`let x;`) → an initializer is INSERTED: `x = __$vr.initialize(path, name, undefined, false)`                                          | restore        | The transform synthesizes an assignment so the uninitialized declaration still emits a lifecycle event, tagged `explicit: false` so the consumer can distinguish 'declared undefined' from 'assigned undefined'. This changes the program text shape (adds an `= …`) while preserving semantics; a rebuild that skips it loses every implicit-declaration event.                                                                                                                                                                                  | instrument-variables.ts:288-311, esp. :309-310 — "return `${idText} = ${HELPER}.initialize(${path}, ${nameLiteral}, undefined, false)`"; tests:124-128                                                                                                                                                                                                       |
| Simple `=` assignment → `assign(path, name, "=", () => name, (__$vrIncoming) => (name = __$vrIncoming), (RHS))` — RHS eager, prior read thunked            | restore        | The RHS is passed as the TRAILING argument so it is evaluated before the helper body runs the prior-value read — matching real JS evaluation order (RHS first). Prior read and write are separate thunks, letting the worker interleave a priorValue/nextValue pair around the real write. The ordering is pinned by an execution oracle, not just a string match.                                                                                                                                                                                | instrument-variables.ts:339-342; :315-316 — "Simple `=` passes the RHS eagerly as the trailing `incoming` (it evaluates before the prior read)"; tests:202-206 (exact string) and tests:334-339 (executed: read:y precedes assign-enter:x:=)                                                                                                                 |
| Compound / logical assignment (`+=`, `\|\|=`, `&&=`, `??=`) → the ORIGINAL expression runs inside the writer thunk; no incoming param                      | restore        | Rather than decomposing the operator, the transform re-emits `name op value` inside a zero-arg writer thunk, which preserves real coercion (`+=` on strings) AND real short-circuit: for `\|\|=`/`&&=`/`??=` the RHS thunk is never invoked when the prior value short-circuits. Four separate executed-ordering tests pin the short-circuit arms. A rebuild that normalizes compound to read-modify-write breaks short-circuit observably.                                                                                                       | instrument-variables.ts:343-347 — "() => (${name} ${operator} ${value})"; :316-318 — "compound/logical forms run the original expression inside the writer thunk (real coercion / real short-circuit)"; tests:208-218 and tests:341-364 (prior-before-RHS, \|\|=/??=/&&= no read:y, &&= truthy does read)                                                    |
| Writer parameter is named `__$vrIncoming` (reserved-prefix hygiene)                                                                                        | restore        | An obvious short name like `v` would shadow the assignment target when the target is itself named `v`, silently mistracing. The reserved `__$vr` prefix guarantees no collision with any JEJ-legal identifier. This is a one-line hygiene decision that a rebuild will lose unless it is written down as a rule, and there is a negative test asserting the compound arm carries NO such param.                                                                                                                                                   | instrument-variables.ts:340-342 — "The writer param uses the reserved `__$vr` prefix (not `v`) so it cannot shadow the target when the target itself is named `v`"; tests:214-218 — compound does not contain '(\_\_$vrIncoming)'                                                                                                                            |
| Assignment to a NON-declared target falls to generic descent (RHS reads still wrap)                                                                        | restore        | isBindingTarget gates the assign splice on the left being an Identifier resolving to a declared let/const. Assignments to globals/undeclared names and to member expressions are passed through unchanged, but their sub-reads still instrument. This is what keeps realm/global mutation out of the stream (README's explicit exclusion) without losing the reads inside the RHS.                                                                                                                                                                | instrument-variables.ts:330-332 — "if (!isBindingTarget(left, scope)) { return rewriteChildren(node, context, scope); }"; :317-318 — "A non-declared target is left to the generic descent (the RHS reads still wrap)"; README.md:163-165 — "Only declared let/const lifecycles are traced"                                                                  |
| UpdateExpression on a declared binding → `increment(path, name, operator, form, () => name, () => <ORIGINAL x++ text>)`                                    | restore        | The writer thunk re-emits the ORIGINAL source text of the update (sliced by offsets), so real `ToNumeric` semantics — including BigInt and valueOf coercion — happen exactly as they would unwrapped. `form` distinguishes 'prefix' from 'postfix', which the consumer needs because the two differ in the value the expression yields. Any rebuild that desugars `x++` into `x = x + 1` is semantically wrong.                                                                                                                                   | instrument-variables.ts:349-376, esp. :370 — "const original = context.source.slice(node.start, node.end);" and :375 — "() => ${original}"; :350-352 — "the writer thunk runs the original `x++` / `++x` (real `ToNumeric`)"; tests:221-232 (both forms)                                                                                                     |
| Non-identifier update argument falls to generic descent                                                                                                    | restore        | `obj.count++` and updates of undeclared names are not increment-wrapped; the object read inside still wraps. Same exclusion principle as the assignment target rule.                                                                                                                                                                                                                                                                                                                                                                              | instrument-variables.ts:364-366 — "if (!isBindingTarget(argument, scope)) { return rewriteChildren(node, context, scope); }"; :351-352 — "Any other argument falls to the generic descent"                                                                                                                                                                   |
| MemberExpression — the OBJECT is read-wrapped; a non-computed property name is NEVER wrapped; a computed property IS wrapped                               | restore        | Three distinct behaviors in one handler. `obj.length` must not emit a read of a binding named `length` even when such a binding exists in scope — there is a dedicated test with exactly that shadowing fixture. `obj[k]` must emit a read of `k`. Getting this wrong produces phantom reads on every property access.                                                                                                                                                                                                                            | instrument-variables.ts:378-397 (computed vs non-computed arms); tests:157-175, esp. :164-168 — instrument('let length = 1; let obj = "x"; obj.length;') contains '() => obj).length' (no read of `length`)                                                                                                                                                  |
| Standalone read of a declared binding → `__$vr.read(path, name, () => name)`; undeclared identifiers emitted VERBATIM                                      | restore        | The read is thunked so the helper controls when the binding is actually touched (this is how a TDZ ReferenceError is observed rather than triggered eagerly by the instrumentation). Undeclared names pass through unwrapped, which is exactly why their ReferenceError arrives WITHOUT node-path attribution — a documented, named boundary rather than a bug.                                                                                                                                                                                   | instrument-variables.ts:399-412; README.md:180-183 — "undeclared identifiers reach the worker unwrapped, so their `ReferenceError` arrives without node-path attribution"; tests:109-114                                                                                                                                                                     |
| Read-wrap exclusions — an assignment target and an update argument are never read-wrapped                                                                  | restore        | Because the parent handlers (rewriteAssignment / rewriteUpdate) consume the identifier themselves and emit `() => name` thunks directly, the Identifier rewriter is never reached for those positions. The tests assert the ABSENCE of any `__$vr.read` in `let x = 0; x++;` and `let x = 0; x = 1;` — a strong negative pin that a rebuild routing through a generic visitor would violate immediately.                                                                                                                                          | tests/instrument-variables.test.ts:177-185 — describe('exclusions'), both assertions `.not.toContain('__$vr.read')`; instrument-variables.ts:342/:346 and :368-375 (the parents emit their own `() => name`)                                                                                                                                                 |
| Unlabeled break / continue → `__$vr.abrupt("break"\|"continue");` emitted BEFORE the original statement                                                    | restore        | The marker must precede the jump or it never executes. It sets the abrupt flag that the enclosing scope's `finally { close(…) }` reads to AUTHOR the scope-pop reason — so the ordering abrupt → close → landed is a hard requirement, pinned by two executed-ordering tests. Labeled forms are unreachable here (rejected at the boundary scan).                                                                                                                                                                                                 | instrument-variables.ts:414-421 — "`${HELPER}.abrupt(${JSON.stringify(reason)}); ${context.source.slice(node.start, node.end)}`"; types.ts:149 — "A scope is left. `reason` is authored from the abrupt flag (seam 4)"; tests:269-280 (string) and tests:387-397 (executed: abrupt < close < landed)                                                         |
| The scope-wrap grammar — `open(path); <prelude> try {…} catch (__$e) { abrupt("error"); throw __$e; } finally { close(path); }`                            | restore        | Five commitments in one template: (1) open before anything; (2) an optional prelude slot used only by for-of's binding initialize; (3) the catch RETHROWS the original error object unchanged (`throw __$e`) so the engine's error path sees the real error, having only recorded the reason; (4) close in `finally`, so it runs on normal completion, break/continue, and throw alike; (5) no newlines anywhere, preserving the line count. The catch-variable is `__$e`, again reserved-prefixed. The empty-program test pins the exact string. | instrument-variables.ts:471-480 (the full template at :479); tests:102-106 (exact emitted string for the empty program)                                                                                                                                                                                                                                      |
| `landed()` post-loop abrupt-flag clear emitted after EVERY loop form (for / for-of / while / do-while)                                                     | restore        | All four loop handlers append the LANDED constant unconditionally — including the non-declaring arms that get no scope wrap. Without it a break's abrupt flag survives the loop and mis-authors the reason on a later, unrelated scope pop. The 'unconditionally, even without a wrap' part is the easy thing to lose.                                                                                                                                                                                                                            | instrument-variables.ts:534-535 — "const LANDED = `${HELPER}.landed();`" described as "The post-loop abrupt-flag clear (types.ts seam 4)"; emitted at :202, :204 (both for arms), :253 (for-of), :262 (while/do-while)                                                                                                                                       |
| isDeclared walks the full scope CHAIN via parent links; only let/const registrations count                                                                 | restore        | Read resolution is lexical: it climbs scope.parent until a declarations Map contains the name. Since build-scope registers only 'let' \| 'const', var/function/global names never resolve, which is the mechanism that implements README's 'only declared let/const lifecycles are traced' exclusion. Shadowing works correctly because the innermost scope is consulted first.                                                                                                                                                                   | instrument-variables.ts:509-519 (the while(current) climb over `current.declarations.has(name)` / `current.parent`); scope/build-scope.ts:65 and :127 — "kind: 'let' \| 'const'"                                                                                                                                                                             |
| findChildScope — for-of body block merges into the for-of scope (fallback to parent)                                                                       | restore        | When no child ScopeInfo matches the node, the parent scope is reused. This mirrors build-scope's merge of a for-of body block into the per-iteration for-of scope, which is why a for-of body does NOT push/pop separately while a classic-for body block does. The comment names validating/check-undeclared-globals.ts as the sibling that must stay consistent — a cross-module convention a rebuild can silently break.                                                                                                                       | instrument-variables.ts:490-499 — "Falls back to the parent for a for-of body block, which build-scope merges into the for-of scope (mirrors `validating/check-undeclared-globals.ts`)"; README.md:170-172                                                                                                                                                   |
| preserveParens contract — ParenthesizedExpression nodes are present and ACCUMULATE `.expression` path segments                                             | restore        | The entry's contract states the AST is parsed with preserveParens. That is not cosmetic: each paren layer adds an `.expression` segment to the node path, so `(y)` and `((y))` produce different addresses for the same read. Two tests pin one- and two-layer paths. A rebuild parsing without preserveParens changes every parenthesized read's address AND loses paren text fidelity.                                                                                                                                                          | instrument-variables.ts:40-41 — "The validated JEJ `Program` AST (parsed with `preserveParens`, so `ParenthesizedExpression` nodes are present)"; tests:187-199 — '$.body.1.declarations.0.init.expression' and '…init.expression.expression'                                                                                                                |
| Instrumentation stamps STATIC node paths only — `scopeInstanceId` is never allocated at instrument time                                                    | restore        | Charter-relevant negative fact: every splice carries a `$`-rooted node path (the ScopeTable key and the worker's address) and nothing else identifying a runtime instance. Instance identity is allocated at runtime, worker-side. Preserving this split matters — an instrument-time instance id is impossible for loops anyway, and moving the allocation would change the event payload contract r3 audits.                                                                                                                                    | instrument-variables.ts:484-488 — scopePath: "The `$`-rooted node path — the scope-table key and the worker's address"; [measured: grep -c 'scopeInstanceId' src/lib/embody/lib/evaluating/trace/variables/instrument-variables.ts → 0]                                                                                                                      |
| Strict/sloppy — the transform emits NO 'use strict' directive; strictness comes from outside                                                               | restore        | Instrumentation is strictness-agnostic: it adds no directive prologue and its splices are strict-safe (no with, no octal, all helper names reserved-prefixed). Strictness is imposed by the engine default plus the worker's `type: 'module'`. Consequence: a `with` program instruments cleanly and then fails as a RUNTIME SyntaxError — the behavior README documents. A rebuild must not 'helpfully' prepend a directive here; that would change where the with failure surfaces.                                                             | [measured: grep -c 'use strict' src/lib/embody/lib/evaluating/trace/variables/instrument-variables.ts → 0]; trace-variables.ts:74-75 — "`strict` is left unset (engine default true)"; README.md:179-180 — "`with` runs as a runtime SyntaxError under the strict instrumented output"                                                                       |
| The emitter-side helper seam — exactly 8 `__$vr` members with fixed arities (open/close/abrupt/landed/initialize/read/assign/increment)                    | restore        | instrument-variables.ts is one half of a two-sided contract; the arities it emits ARE the contract as seen from the emitter. The test file's RecordingDouble type is the machine-readable statement of it (initialize/4, read/3, assign/6 with optional incoming, increment/6, open\|close/1, abrupt/1, landed/0). The `assign` signature's OPTIONAL trailing `incoming` is the load-bearing subtlety: present for `=`, absent for compound. r3 owns the worker-side implementation; this row pins what the instrumenter promises to call.        | tests/instrument-variables.test.ts:20-49 (the RecordingDouble type, all 8 members with arities); instrument-variables.ts:531-535 — "const HELPER = '\_\_$vr'" "(types.ts seam 3)"                                                                                                                                                                            |
| Executed-ordering oracle — the test suite EXECUTES instrumented output against a recording double via `new Function`                                       | restore        | Eleven of the tests do not assert on strings at all: they run the instrumented source with a logging double and assert on the ORDER of the resulting log (RHS-before-prior, short-circuit non-invocation, open-before-init, abrupt-before-close-before-landed). String assertions alone cannot catch an ordering regression in the thunk protocol. This test STRATEGY — not merely the individual cases — is a restorable asset; a rebuild verified only by snapshotting emitted text would be strictly weaker.                                   | tests/instrument-variables.test.ts:93-98 (the `run` helper: "new Function('\_\_$vr', instrument(source))(double)") with :95 — "executing the instrumented output against a recording double is the ordering oracle"; the describe('executed ordering') block at :333-398                                                                                     |
| Test-harness production mirror — fixtures go through validateProgram(source, justEnoughJs) → projectScopeTable(ast, buildScope(ast)) → instrumentVariables | restore        | The suite does not hand-build ASTs or scope tables; it reproduces the real phase 1→2→3 chain, so the tests exercise the actual ScopeTable the facade would pass, including the module-mode-with-script-fallback parse. That fidelity is why the with/sequence/optional-chaining non-rejection tests are meaningful. A rebuild whose unit tests fabricate their scope table would lose the coupling these tests verify.                                                                                                                            | tests/instrument-variables.test.ts:9-18 — "// Mirror production: the facade hands instrumentVariables the AST that validateProgram produced (module mode, falling back to script for `with`)"                                                                                                                                                                |

### B3 — worker split and event emission

| member                                                                                         | classification | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| variablesWorkerSetup (default export) — engine WorkerSetup shape                               | restore        | The tier's worker logic is a single default-exported `(api, workerConfig) => WorkerSetupResult` returning a FROZEN `{ globals, serializeHalt }`. It uses only `api.emit` (never `api.call`) — the tier has no call channel. Losing the frozen return or the single-function shape breaks the engine bootstrap contract.                                                                                                                                                                                                                                                                      | variables-worker-setup.ts:83-86 `export default function variablesWorkerSetup(api: WorkerApi, workerConfig: unknown): WorkerSetupResult`; :286-295 `return Object.freeze({ globals: Object.freeze({…}), serializeHalt: serializeVariablesHalt })`; :79 "this tier uses only `api.emit`"; engine contract at study-lenses--deprecated-architecture/lib/engine/types.ts:258-277                                                                                                                                                                                               |
| workerConfig IS the ScopeTable (clone-transported, cast by use, `?? {}` fallback)              | restore        | The engine hands `workerConfig` as contractually `unknown`; this tier's is the ScopeTable, cast at the boundary with an empty-object fallback so a missing config degrades to all-defaults rather than throwing. This is the worker's ONLY input besides `api` — the whole static projection arrives here.                                                                                                                                                                                                                                                                                   | variables-worker-setup.ts:87-89 `// WHY the cast: workerConfig is clone-transported and contractually unknown` / `const scopeTable = (workerConfig ?? {}) as ScopeTable`; :5-8 "the spec's `workerConfig` — which for this tier IS the {@link ScopeTable}"                                                                                                                                                                                                                                                                                                                  |
| Per-run mutable state, closed over per setup() call                                            | restore        | The run's ONLY mutable state lives here: the frame stack, a monotonic step counter, a scope-instance counter, and the abrupt flag — all created fresh inside `setup()` (a worker module is one disposable run), which is what keeps the thread side stateless. A rebuild that hoists any of this to module scope silently breaks run isolation.                                                                                                                                                                                                                                              | variables-worker-setup.ts:91-95 `const stack: Frame[] = []; let stepCounter = 0; let instanceCounter = 0; let abruptReason: AbruptReason \| null = null;`; :18-26 "This module holds the run's only mutable state … State is closed over per `setup()` call"                                                                                                                                                                                                                                                                                                                |
| `step` — monotonic, gap-free emission order starting at 1                                      | restore        | Every event carries `step` from a single counter incremented at emit time, so steps are exactly 1..N in emission order with no gaps. The unit tests assert this as a global invariant across several fixtures, so it is a pinned guarantee, not an accident of ordering.                                                                                                                                                                                                                                                                                                                     | variables-worker-setup.ts:97-100 `function nextStep(): number { stepCounter += 1; return stepCounter; }`; tests/variables-worker-setup.test.ts:73-77 `expect(steps).toStrictEqual(events.map((_event, index) => index + 1))` under the comment "Steps are the consecutive emission order 1, 2, …, N (monotonic, gap-free)"; asserted at :104,:153,:263,:341,:397,:522                                                                                                                                                                                                       |
| Injected globals: `__$vr` helper namespace + inert dialog stubs                                | restore        | The setup injects a frozen `__$vr` namespace (the seam-3 helper protocol the instrumentation emits calls against) PLUS three inert dialog stubs with pinned return values — `prompt() => null`, `confirm() => false`, `alert() => undefined`. The stubs are a behavior, not decoration: a test fixture (`let f = prompt`) relies on `prompt` existing as a function value in the injected globals.                                                                                                                                                                                           | variables-worker-setup.ts:287-293 `globals: Object.freeze({ __$vr: Object.freeze(helpers), prompt: () => null, confirm: () => false, alert: () => undefined })`; tests/variables-worker-setup.test.ts:575 `runWorker('let f = prompt; f;')`                                                                                                                                                                                                                                                                                                                                 |
| `__$vr.open(scopePath)` — scope-push emission                                                  | restore        | open() reads the scope table entry for the path, defaults `scopeKind` to `'block'` when the path is absent, seeds every declared binding into status `'tdz'`, allocates the next instance id, pushes the frame, CLEARS the abrupt flag, and emits `scope-push` carrying `{ nodePath, scopeInstanceId, scopeKind, variables:[{name,kind}] }` in scope-table (source) order. Each of those five effects is separately observable.                                                                                                                                                              | variables-worker-setup.ts:123-146 (`const scopeKind: ScopeKind = entry?.scopeKind ?? 'block'`; `bindings.set(variable.name, { kind: variable.kind, status: 'tdz' })`; `abruptReason = null`; `emit({ event: 'scope-push', … })`); tests/variables-worker-setup.test.ts:89-94 empty program pushes `scopeKind:'script', nodePath:'$', variables: []`; :191-196 two declarators push `[{a,let},{b,let}]` in source order                                                                                                                                                      |
| `__$vr.close(scopePath)` — scope-pop emission with final variable states                       | restore        | close() pops the frame and emits `scope-pop` with the POPPED frame's `scopeInstanceId`/`scopeKind` (not the current top), the reason, and each binding's FINAL state read out of the registry — `value` present iff `status === 'initialized'`, absent entirely on a still-TDZ binding. It is also defensively a no-op on an empty stack.                                                                                                                                                                                                                                                    | variables-worker-setup.ts:148-166; :299-303 `finalVariable` — `state.status === 'initialized' ? { name, kind, status:'initialized', value: state.value } : { name, kind, status:'tdz' }`; :150-152 "defensive: an unbalanced close cannot occur with LIFO wraps"; tests/variables-worker-setup.test.ts:560-569 pop reports `[{y,tdz},{x,tdz}]` and `expect('value' in variable).toBe(false)`                                                                                                                                                                                |
| Abrupt-flag protocol: `abrupt(reason)` sets, `landed()` clears, `close` READS but never clears | restore        | The flag is the SOLE authority for a scope-pop reason. Because `close` reads without clearing, ONE break unwinds several scopes and every one of them pops the same reason; `landed()` exists to kill a flag left set by a final-iteration jump so a later sibling scope pops `'normal'`. This three-part protocol (set / read-don't-clear / explicit land) is the subtle part a rebuild would most easily lose.                                                                                                                                                                             | variables-worker-setup.ts:168-174 `abrupt(reason) { abruptReason = reason; }` / `landed() { abruptReason = null; }`; :153 `const reason = abruptReason ?? 'normal'; // close READS, never clears`; :30-35 "read but NEVER cleared by `close` (one break unwinds several scopes, all reporting the same reason)"; tests/variables-worker-setup.test.ts:436-458 break pops block+for-of as `'break'` while script pops `'normal'`; :494-507 stale flag cleared by `landed` → sibling block pops `'normal'`                                                                    |
| Every value-bearing helper clears the abrupt flag on success                                   | restore        | initialize / read / assign / increment each set `abruptReason = null` before emitting, so ordinary progress after a jump cannot leave a stale reason attached to the next pop. Together with `open`'s clear this is the flag's full lifecycle; dropping any one clear produces wrongly-attributed pop reasons.                                                                                                                                                                                                                                                                               | variables-worker-setup.ts:184 (initialize), :200 (read), :224 (assign), :269 (increment) each `abruptReason = null;`; :32-33 "cleared by every value-bearing helper on success and by every `open` and `landed`"                                                                                                                                                                                                                                                                                                                                                            |
| HOME-scope attribution of binding events (`scopeInstanceId` = declaring frame)                 | restore        | initialize/read/assign/increment stamp the instance id of the frame that DECLARES the name — found by scanning the stack for the innermost match — not the frame the statement textually executes in, so a binding's whole life shares one `scopeInstanceId`. The fallback when no frame declares the name is the innermost live frame (`?? 0` if the stack is empty). Losing this silently re-attributes every cross-scope read/write.                                                                                                                                                      | variables-worker-setup.ts:106-115 `homeFrame` — `match = frame; // later == deeper, so the last match is innermost`; :117-120 `currentInstanceId(): stack.at(-1)?.instanceId ?? 0`; used at :189,:205,:222,:274; :27-30 "carry the HOME scope instance of the named binding"; tests/variables-worker-setup.test.ts:221-224 "HOME: the read carries the SCRIPT instance … not the inner block instance"                                                                                                                                                                      |
| `__$vr.initialize(nodePath, name, value, explicit)`                                            | restore        | Records the snapshot into the home frame (status → `'initialized'`), emits `initialize` with `{ name, value, explicit }`, and RETURNS THE ORIGINAL live value so program semantics are untouched. `explicit` distinguishes `let x = 1` (true) from `let x;` (false, value `undefined`) — a pedagogically load-bearing bit, not a redundant flag.                                                                                                                                                                                                                                             | variables-worker-setup.ts:176-195 (`binding.status = 'initialized'; binding.value = snap;` … `return value;`); tests/variables-worker-setup.test.ts:130-135 `{ event:'initialize', name:'x', value:1, explicit:true }`; :166-171 `let x;` → `value: undefined, explicit: false`                                                                                                                                                                                                                                                                                             |
| `__$vr.read(nodePath, name, thunk)` — thunked read, TDZ throws BEFORE any event                | restore        | The read runs the real identifier reference inside a thunk under `stampOnThrow`; a TDZ access therefore throws stamped and rethrown with NO `read` event emitted at all (the emit is downstream of the thunk). The trace shows the absence, and the halt carries the exact node path. Reordering emit before the thunk would fabricate a read that never happened.                                                                                                                                                                                                                           | variables-worker-setup.ts:197-210 — `const value = stampOnThrow(nodePath, thunk); // TDZ → stamped, rethrown, no event`, emit follows at :201; tests/variables-worker-setup.test.ts:511-523 `let y = x; let x = 1;` → zero read events, zero initialize events, halt `ReferenceError` with a string `nodePath` matching `__$vr.read("…", "x",`                                                                                                                                                                                                                              |
| `__$vr.assign(...)` — prior/writer thunk split, operator-derived write decision                | restore        | assign reads the prior via a thunk, decides whether a LOGICAL assignment writes FROM THE OPERATOR AND PRIOR VALUE (never by comparing before/after values — `??=` writes on nullish, `\|\|=` on falsy, `&&=` on truthy), runs the real write through a writer thunk (so a const target throws stamped with NO assign event), and returns the writer's result. All four decisions are separately losable.                                                                                                                                                                                     | variables-worker-setup.ts:212-256; :351-367 `isLogicalAssign` / `logicalWrote` — "never by comparing values (types.ts `AssignEvent`)"; :217-219 "the writer runs the real assignment (const target → TypeError → stamped, rethrown, no event)"; tests/variables-worker-setup.test.ts:540-548 const reassign → zero assign events, TypeError halt; :266-325 the six logical-operator cases                                                                                                                                                                                   |
| `assign` payload asymmetry: `wrote:true` carries `nextValue`, `wrote:false` OMITS it           | restore        | A short-circuited logical assignment emits `{ …, priorValue, wrote:false }` with the `nextValue` KEY ABSENT (not undefined) and leaves the registry untouched, so the scope-pop still shows the old value. A written assign emits `nextValue` and updates the home frame's binding. The tests assert key ABSENCE, so this is a pinned shape, not a convention.                                                                                                                                                                                                                               | variables-worker-setup.ts:225-254 (two distinct `emit` calls, the else-arm has no `nextValue`); tests/variables-worker-setup.test.ts:289-301 "\|\|= short-circuits on a truthy prior: wrote:false, no nextValue, registry unchanged" — `expect(assignEvent && 'nextValue' in assignEvent).toBe(false)` and pop shows `value: 1`                                                                                                                                                                                                                                             |
| `__$vr.increment(...)` — re-read for `nextValue`, three values on one event                    | restore        | increment runs the real `x++`/`--x` through a writer thunk and then RE-READS the binding via the prior thunk to obtain `nextValue`, rather than reconstructing it arithmetically — which is what makes bigint (and any coercion-sensitive) stepping correct. The event carries `operator`, `form` (prefix/postfix), and THREE values: `priorValue`, `nextValue`, and `returnedValue` (old for postfix, new for prefix); the helper returns the writer's returned value.                                                                                                                      | variables-worker-setup.ts:258-283 — `const next = priorThunk(); // re-read the just-written (now-initialized) binding`, emit at :270-281, `return returned;`; tests/variables-worker-setup.test.ts:344-356 "steps a bigint by re-reading the stored value, not reconstructing it" (`1n → 2n`); :329-341 postfix returns 1 stores 2; :358-367 prefix returns 2                                                                                                                                                                                                               |
| Scope-instance allocation: one fresh id per `open`, monotonic from 0                           | restore        | `instanceId` comes from a per-run counter incremented on every open, so a per-iteration scope (for-of) yields a DISTINCT instance id per iteration while a synthesized classic-for head scope keeps ONE instance across all iterations. Push and pop of the same instance share the id. This is the identity that makes per-iteration NM narration possible.                                                                                                                                                                                                                                 | variables-worker-setup.ts:130-132 `const instanceId = instanceCounter; instanceCounter += 1; stack.push({ scopePath, instanceId, scopeKind, bindings });`; tests/variables-worker-setup.test.ts:382-398 for-of over `[1,2]` → 2 pushes with 2 distinct instance ids; :400-415 classic for → exactly 1 `for` push, 2 increments, pop shows `i:2`; :102 push/pop share the id                                                                                                                                                                                                 |
| Registry per-frame isolation (shadowing)                                                       | restore        | Each frame owns its own `Map<string, BindingState>`, so a body-block `let i` shadowing a for-head `let i` keeps entirely separate values, and each pop reports its own. The `homeFrame` innermost-match scan is what routes each event to the right one.                                                                                                                                                                                                                                                                                                                                     | variables-worker-setup.ts:67-73 `Frame = { …, readonly bindings: Map<string, BindingState> }`; :126-129 fresh Map per open; tests/variables-worker-setup.test.ts:418-432 for-pop shows `{i:1}` while block-pop shows `{i:99}`                                                                                                                                                                                                                                                                                                                                               |
| Value snapshots: clone-safety with a live-reference carve-out                                  | restore        | `snapshot()` tags functions as `{opaqueValue:true, typeOf:'function'}`, probes everything else with `structuredClone` and tags the failures as opaque with their `typeof`, and otherwise returns the LIVE REFERENCE (the engine deep-clones at emit) — which is deliberate: the registry stores that reference so a scope-pop reflects the value's final state. Both halves matter; a rebuild that eagerly clones changes pop semantics, one that skips the probe crashes the postMessage.                                                                                                   | variables-worker-setup.ts:305-325 (`if (typeof value === 'function') return { opaqueValue: true, typeOf: 'function' }`; `try { structuredClone(value); return value } catch { return { opaqueValue: true, typeOf: typeof value } }`); :309-313 "this returns the live reference (the engine deep-clones at `emit`); the registry therefore stores that reference, so a `scope-pop` reflects the value's final state"; tests/variables-worker-setup.test.ts:573-591 function value rides as the opaque tag through initialize, read AND scope-pop without crashing the clone |
| `stampOnThrow` — one-shot `__nodePath` stamp on a thrown value                                 | restore        | Wraps every thunk; on a throw it stamps the source node path onto the error object ONLY IF not already stamped (so the innermost helper wins), inside its own try so a frozen error is left unattributed rather than crashing, then rethrows unchanged. This is the entire mechanism by which a TDZ read or const reassignment is attributable to an exact node.                                                                                                                                                                                                                             | variables-worker-setup.ts:327-349 — the `=== undefined` guard at :339, the inner `try { … } catch { /* frozen error — leave unattributed */ }` at :341-346, `throw error;` at :347; "Mirrors intercept's `__$ic`" at :330                                                                                                                                                                                                                                                                                                                                                   |
| `serializeVariablesHalt` — the tier's worker-side halt author                                  | restore        | Fires on every worker-side stop and authors the tier's own halt shape directly (no thread-side re-shaping): `'natural-end'` → `{natural:true, errorName:'', message:'', nodePath:null}`; a throw → `{natural:false, errorName, message, nodePath}` with name/message read from an `Error` and String()-coerced for a non-Error throw, and `nodePath` taken from the `__nodePath` stamp only when it is a string, else `null`. An undeclared identifier is deliberately unattributed (`nodePath: null`) because the instrumenter never wrapped it — the named contrast to a stamped TDZ read. | variables-worker-setup.ts:369-388; tests/variables-worker-setup.test.ts:105-110 natural halt asserted exactly; :525-538 "leaves an undeclared-identifier ReferenceError unattributed (no stamp)" → `nodePath: null`; :516-520 stamped TDZ halt has a string nodePath                                                                                                                                                                                                                                                                                                        |
| Error path: a throw makes EVERY open scope pop `'error'`                                       | restore        | The instrumentation's scope wrap calls `abrupt('error')` from its catch before rethrowing; because `close` never clears the flag, every scope unwound by the throw pops with reason `'error'` while still reporting its registry contents (including still-TDZ bindings). The pop stream and the halt are therefore two independent, consistent views of the same failure.                                                                                                                                                                                                                   | variables-worker-setup.ts:168-170 `abrupt(reason) { abruptReason = reason; // 'break' \| 'continue' \| 'error' (seam 4) }`; :30-31 "and by the scope wrap's catch (`'error'`)"; tests/variables-worker-setup.test.ts:481-492 every pop matches `{ reason: 'error' }` and halt is `ReferenceError`; :550-570 the error-pop still reports `[{y,tdz},{x,tdz}]` "without re-reading"                                                                                                                                                                                            |
| variables-worker-entry.ts — the thin worker main()                                             | restore        | A 13-line module whose entire body is `bootstrap(variablesWorkerSetup)` executed AT MODULE LOAD: it is the worker's `main()`, not a library module, and the bootstrap posts `ready` from here for the thread handshake. Trivial to write, easy to lose the semantics of — if a rebuild makes it an exported function the handshake never fires.                                                                                                                                                                                                                                              | variables-worker-entry.ts:9-13 `import bootstrap from '…/lib/engine/worker/bootstrap.js'; import variablesWorkerSetup from './variables-worker-setup.js'; bootstrap(variablesWorkerSetup);`; :2-6 "it executes at module load, and the bootstrap posts `ready` from here for the thread's handshake"                                                                                                                                                                                                                                                                        |
| variablesThreadLogic — a frozen, STATELESS ThreadLogic implementing ONLY `onMessage`           | restore        | The thread side is deliberately near-empty: no `onCall` (no call channel) and no `refineError` (the worker self-authors the halt), and no state at all, because the worker emits the COMPLETE event. This asymmetry — all logic worker-side, thread-side a guard — is the tier's architectural choice, and a rebuild that moves enrichment thread-side inverts it.                                                                                                                                                                                                                           | variables-thread-logic.ts:57-63 `const variablesThreadLogic: ThreadLogic = Object.freeze({ onMessage(message: unknown): unknown { … } });`; :11-13 "There is no call channel and the worker self-authors the halt, so `onCall` and `refineError` are not implemented"; tests/variables-thread-logic.test.ts:95-101 "is stateless — a dropped message does not affect the next yield"                                                                                                                                                                                        |
| The boundary guard: six discriminants + three base fields, yield-or-drop                       | restore        | `onMessage` yields iff the message is a non-null, NON-ARRAY object whose `event` is one of exactly six names (`scope-push`, `scope-pop`, `initialize`, `read`, `assign`, `increment`) and whose `step` is a number, `nodePath` a string and `scopeInstanceId` a number; anything else returns `undefined` (dropped). Per-variant interior fields are deliberately NOT revalidated — the worker is the field authority, so an event carrying unknown extra fields still yields.                                                                                                               | variables-thread-logic.ts:20-28 `EVENT_NAMES` set; :37-55 `isVariablesTraceEvent` (`Array.isArray(value)` exclusion at :39, base-field checks at :50-54); tests/variables-thread-logic.test.ts:18-51 thirteen drop cases incl. array and each missing/mistyped base field; :80-84 "yields an event carrying extra properties (the worker is the field authority)"                                                                                                                                                                                                           |
| `onMessage` returns the message BY REFERENCE and never freezes or clones                       | restore        | Identity is pinned by test (`toBe`, not `toEqual`) and non-freezing is pinned by an explicit assertion — the engine freezes at yield, so any thread-side freeze or clone would be a double-cost and would break the engine's ownership of the yielded item's shape.                                                                                                                                                                                                                                                                                                                          | variables-thread-logic.ts:58-60 `return isVariablesTraceEvent(message) ? message : undefined;`; :8-10 "returns the message BY REFERENCE — the engine freezes the yielded item at yield, so this module never freezes or clones"; tests/variables-thread-logic.test.ts:77 `expect(variablesThreadLogic.onMessage(message)).toBe(message)`; :86-92 "does not freeze the yielded message (the engine freezes at yield)"                                                                                                                                                        |
| Worker never emits scope-pop reason `'limit'`                                                  | restore        | Only four reasons are ever produced — `'normal'` from the `?? 'normal'` default and `'break' \| 'continue' \| 'error'` from the abrupt flag. The type's fifth member `'limit'` is structurally present but unreachable in the worker, which is a deliberate MVP boundary (the tier owns no instrumentation limit), not an oversight. Restoring the type without restoring this emission boundary would misrepresent the trace.                                                                                                                                                               | variables-worker-setup.ts:153 `const reason = abruptReason ?? 'normal';` and :169 `// 'break' \| 'continue' \| 'error' (seam 4)` — no other producer; context: types.ts:64-68 "`'limit'` is structurally present but never produced in MVP (this tier owns no instrumentation limit — README § Bounded context)"                                                                                                                                                                                                                                                            |
| The worker-setup test harness = a real-pipeline integration oracle                             | restore        | `runWorker` is not a mock harness: it runs validate → projectScopeTable → instrumentVariables → the real worker setup, executes genuine instrumented output via `new Function` WITH the bootstrap's `"use strict"` prefix, and its recording `emit` structured-clones every message so any non-clone-safe leak throws IN THE TEST. That clone-in-emit is the tier's clone-safety oracle and the strict prefix is what makes the semantics production-faithful — both are losable test-design decisions worth transporting.                                                                   | tests/variables-worker-setup.test.ts:18-62 — `const scopeTable = projectScopeTable(ast, buildScope(ast)); const code = instrumentVariables(ast, source, scopeTable);` (:33-34), `events.push(structuredClone(message) as VariablesTraceEvent)` (:39), `const body = "use strict";\n${code}` (:49), `new Function(...names, body)(...values)` (:54); docblock :24-26 "a non-clone-safe value leaked onto an event (a raw function instead of an OpaqueValue) throws here"                                                                                                    |

### B4 — ScopeTable projection

| member                                                                                                                                                | classification | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| projectScopeTable(program, scopeAnalysis) → ScopeTable — default export, pure 2-arg projector                                                         | restore        | The Project phase's whole public surface: a pure function taking the validated JEJ Program AST plus a SEPARATELY-computed package scope analysis (the projector does not call buildScope itself — the caller does, once). The rebuild must keep both parameters: the AST is needed independently of the analysis, because classic-for heads are invisible to build-scope and are found by walking the AST.                                                                                                                                                   | project-scope-table.ts:41-44 — "export default function projectScopeTable(program: Node, scopeAnalysis: ScopeAnalysis): ScopeTable"; caller at trace-variables.ts:69 — "const scopeTable = projectScopeTable(ast, buildScope(ast));" with the comment at :67-68 "One buildScope, fed only to the table; the instrumenter re-derives its own scope analysis internally."                                                                                                                                                                                                                          |
| Output is DEEP-frozen in place (table, each entry, each variables array, each {name,kind})                                                            | restore        | `deepFreezeInPlace` mutates and returns the same reference, freezing every nested object/array. Two unit tests pin it at two depths (table and variables array), so the rebuild losing depth would go undetected only below the array level. Reference identity is preserved (the util's contract), which matters because the same object is handed to the engine as `workerConfig`.                                                                                                                                                                         | project-scope-table.ts:69 — "return deepFreezeInPlace(table);"; :19 imports `@utils/deep-freeze-in-place.js`; src/lib/utils/deep-freeze-in-place.ts:36-58 (recursive Object.freeze over Object.values, returns same ref); tests/project-scope-table.test.ts:195-203 — "deep-freezes the table" / "deep-freezes each variables array"                                                                                                                                                                                                                                                             |
| Clone-safety guarantee: the projection emits plain data only — no Maps, no AST nodes, no functions                                                    | restore        | The table crosses the structured-clone boundary as the engine spec's `workerConfig`. The projector is the sole place this is enforced: it reads `ScopeInfo` (which holds `node: Node`, `parent` back-pointers and a `ReadonlyMap` of declarations) and emits only strings, string-keyed objects and arrays of `{name, kind}`. A rebuild that leaked a node reference or a Map would fail at postMessage, not at compile time.                                                                                                                                | project-scope-table.ts:46 "const table: Record<NodePath, ScopeTableEntry> = {}" + :60/:136 (only `{scopeKind, variables}` written) + :156 "variables.push({ name: declaration.name, kind: declaration.kind })"; upstream shape carrying nodes/Maps at src/lib/embody/lib/scope/types.ts:58-64 (ScopeInfo: node, parent, ReadonlyMap declarations, children); rationale recorded on the type at types.ts:316-321 — "plain data only (no `Map`s — `Object.freeze` does not freeze a Map and a Map does not structured-clone to the same shape)"; consumed as workerConfig at trace-variables.ts:89 |
| Keying convention: every table key is the scope's `$`-rooted NodePath from the canonical `buildNodePathMap`                                           | restore        | The key set is a shared ADDRESS SPACE, not an internal detail: instrumentation looks scopes up by the same path and the worker's `__$vr.open/close(scopePath)` receives it verbatim. Root is `'$'`, each child segment joined with `'.'` (e.g. `$.body.1`, `$.body.0.body.body.0`). If the rebuild changes the path grammar it must change it in all three places at once.                                                                                                                                                                                   | project-scope-table.ts:45 "const nodePathMap = buildNodePathMap(program);", :59 and :135 (`nodePathMap.get(...)!` used as the key); header docblock :12-14 — "Each scope's table key is its `$`-rooted NodePath from the canonical `buildNodePathMap`, the same key I2's instrumentation and the worker's `__$vr.open/close(scopePath)` address"; grammar at src/lib/embody/lib/parse-old/build-node-path-map.ts:9-13 and :43-47; keys observed in tests/project-scope-table.test.ts:42 ('$.body.1'), :187 ('$.body.0.body.body.0')                                                              |
| Entry shape `{ scopeKind, variables: [{ name, kind }] }` with variables in SOURCE ORDER                                                               | restore        | Source order is load-bearing downstream, not cosmetic: the type documents it as "the order of the declare burst on its `scope-push`", and the worker replays `entry.variables` into the frame's binding map in that order. Order comes from the insertion order of build-scope's declarations Map (Pass B) and declarator order (Pass A).                                                                                                                                                                                                                    | project-scope-table.ts:152-157 (iterates `scope.declarations.values()` in order) and :108-115 (declarator loop); types.ts:307-314 — "its kind and the bindings it declares, in source order (the order of the declare burst on its `scope-push`)"; pinned by tests/project-scope-table.test.ts:50-63 ("projects them in source order with their kinds", a,b,c); consumer at variables-worker-setup.ts:124-129                                                                                                                                                                                    |
| Top-scope rename: build-scope's `program` → the notional-machine `script`                                                                             | restore        | A deliberate vocabulary re-homing at the tier boundary: the package analysis' ScopeKind union is `'program' \| 'block' \| 'for-of'`; this tier's is `'script' \| 'block' \| 'for' \| 'for-of'`. `mapScopeKind` is the only translation point, and `block`/`for-of` pass through unchanged. Losing it would surface `program` in learner-facing scope-push events.                                                                                                                                                                                            | project-scope-table.ts:161-164 — "function mapScopeKind(kind: ScopeInfo['kind']): ScopeKind { return kind === 'program' ? 'script' : kind; }"; source union at src/lib/embody/lib/scope/types.ts:26 — "type ScopeKind = 'program' \| 'block' \| 'for-of'"; target union at variables/types.ts:64 — "type ScopeKind = 'script' \| 'block' \| 'for' \| 'for-of'"; pinned by tests/project-scope-table.test.ts:24-27                                                                                                                                                                                |
| The script scope is ALWAYS emitted, even with zero declarations                                                                                       | restore        | Asymmetric rule, easy to lose in a rebuild that uniformly drops empty scopes: the `scope.kind === 'program'` disjunct emits the root unconditionally, so a program with no top-level declarations still produces `{ $: { scopeKind: 'script', variables: [] } }` — and therefore still gets a script-level scope-push/pop pair. Pinned by the empty-program test and re-pinned incidentally by five other cases whose script entry is empty.                                                                                                                 | project-scope-table.ts:133 — "if (scope.kind === 'program' \|\| variables.length > 0)"; tests/project-scope-table.test.ts:22-28 — "projects one script scope with no variables" expecting `{ $: { scopeKind: 'script', variables: [] } }`                                                                                                                                                                                                                                                                                                                                                        |
| Declaration-less NON-script scopes are dropped from the table                                                                                         | restore        | The NM elides the environment of a block with no lexical declarations. Because instrumentation wraps a scope only when the path is present in the table, this drop is what suppresses the scope-push/scope-pop pair for `if (true) {}`. It is a pedagogy decision expressed as a projection rule.                                                                                                                                                                                                                                                            | project-scope-table.ts:133 (the `variables.length > 0` guard) + header :7-9 — "declaration-less blocks are dropped (the NM elides the environment of a block with no lexical declarations)"; tests/project-scope-table.test.ts:128-134 — "drops the block, leaving only the script scope" for `if (true) {}`                                                                                                                                                                                                                                                                                     |
| A block left empty BY for-head re-homing is dropped too (drop test runs after re-homing)                                                              | restore        | Second-order consequence of ordering Pass A before Pass B: `{ for (let i = 0; ...) {} }` has its `i` re-homed out of the enclosing block, the block's surviving-declaration count falls to zero, and the block is then dropped — while the synthesized for-scope survives at the inner path. A rebuild that computed emptiness before re-homing would keep a spurious block scope.                                                                                                                                                                           | project-scope-table.ts:52-67 (Pass A populates `forHeadIds`, then Pass B runs) with :64-66 — "skipping re-homed for-head declarations and dropping any non-script scope left declaration-less"; tests/project-scope-table.test.ts:136-146 — "drops the now-empty block after re-homing the head", expecting only `$` and `$.body.0.body.0`                                                                                                                                                                                                                                                       |
| Pass A: one SYNTHESIZED `for` scope per classic ForStatement with a declaring head, keyed at the ForStatement's own node path                         | restore        | This is the tier's core correction of the upstream analysis. build-scope surfaces no scope for classic `for` and homes the head declaration in the ENCLOSING scope, overwriting across sibling loops (its declarations Map is name-keyed), so two `for (let i …)` siblings would collapse to one `i`. Keying the synthesized scope at the ForStatement node — not its body block — gives each loop a distinct address. Note the scope is per-LOOP, not per-iteration (instrumentation opens it once around the whole loop).                                  | project-scope-table.ts:48-62, esp. the comment :48-51 — "build-scope homes for-init declarations in the ENCLOSING scope and overwrites sibling loops there" — and :59-60 "const scopePath = nodePathMap.get(forStatement)!; table[scopePath] = { scopeKind: 'for', variables };"; header :9-12; tests/project-scope-table.test.ts:65-83 — "synthesizes a distinct for-scope per loop and leaves the script empty" ($.body.0 and $.body.1 each with `i`); per-loop-not-per-iteration confirmed at instrument-variables.ts:190-192                                                                 |
| A classic-for head that declares nothing synthesizes NO scope                                                                                         | restore        | `readForHead` returns `[]` when `init` is absent or is not a VariableDeclaration, and Pass A writes no entry when the list is empty — so `for (;;) {}` and `for (i = 0; …) {}` produce no for-scope and therefore no scope-push/pop. Two separate tests pin the two shapes.                                                                                                                                                                                                                                                                                  | project-scope-table.ts:98-102 — "if (init?.type !== 'VariableDeclaration') { return []; }" — and :57 "if (variables.length > 0)"; docblock :91-92 — "Returns `[]` when the head declares nothing (`for (;;)`, `for (i = 0; …)`), so no for-scope is synthesized"; tests/project-scope-table.test.ts:112-126 (empty head; assignment head)                                                                                                                                                                                                                                                        |
| Every declarator of a multi-binding for head is lifted, in declarator order, into the ONE synthesized for-scope                                       | restore        | `for (let i = 0, j = 0; …)` yields a single for-scope holding both bindings in head order — not two scopes, not reordered. All declarators share the head's single `kind`.                                                                                                                                                                                                                                                                                                                                                                                   | project-scope-table.ts:104-116 (loop over `initRecord.declarations`, pushing `{name, kind}` per declarator with the shared `kind` from :105); tests/project-scope-table.test.ts:85-98 — "lifts every head binding into the for-scope in order" (i then j)                                                                                                                                                                                                                                                                                                                                        |
| Re-homing is tracked by NODE IDENTITY (`Set<Node>` of head `id` nodes), not by name                                                                   | restore        | Pass B skips a declaration iff `forHeadIds.has(declaration.node)` — the identity of the declarator's `id` node. Name-based exclusion would be wrong: an enclosing scope may legitimately declare its own `i` alongside a loop head named `i`, and only the head's binding must be removed. Depends on the analysis' `DeclarationInfo.node` being the same `id` node the head walk collected — a coupling the rebuild must preserve or replace deliberately.                                                                                                  | project-scope-table.ts:113 "forHeadIds.add(id);" (id = the declarator's `.id`, read at :109) and :152-155 — "if (forHeadIds.has(declaration.node)) { continue; }"; DeclarationInfo.node at src/lib/embody/lib/scope/types.ts:38-46                                                                                                                                                                                                                                                                                                                                                               |
| ForStatements are collected recursively over the WHOLE program, so nested loops each get their own scope                                              | restore        | `collectForStatements` recurses through `getChildNodes` from the Program root in source order, so a loop inside a loop body is found and given its own entry; the inner loop's body block is separately dropped for having no surviving declarations. The traversal is generic (property-walk over the node), not an allowlisted-shape walk.                                                                                                                                                                                                                 | project-scope-table.ts:79-86 (recursive collector) and :54; traversal semantics at src/lib/embody/lib/parse-old/get-child-nodes.ts:27-47 (generic key walk skipping type/start/end/loc); tests/project-scope-table.test.ts:175-193 — "synthesizes a for-scope at each loop and drops the bodies" ($.body.0 and $.body.0.body.body.0)                                                                                                                                                                                                                                                             |
| for-of is NOT re-homed: head and body bindings merge into ONE entry keyed at the ForOfStatement, head first                                           | restore        | Deliberate asymmetry with classic `for`. build-scope already models `for-of` as a real per-iteration scope whose node is the ForOfStatement, so the projector passes it through untouched — with the consequence that a declaring body block contributes its bindings to the SAME entry rather than a nested block entry, head binding first. A rebuild that treated the two loop forms uniformly would produce a different event stream here.                                                                                                               | project-scope-table.ts:126-141 (Pass B passes build-scope scopes through; nothing special-cases for-of) with mapScopeKind :162-164 leaving 'for-of' intact; src/lib/embody/lib/scope/types.ts:20-26 ("`for...of` statements (which create a scope for the iterator variable)"); tests/project-scope-table.test.ts:148-161 — "merges head and body bindings into one for-of scope, head first" (x const, then z let, under $.body.0)                                                                                                                                                              |
| A non-declaring for-of head still yields a for-of entry carrying only the body bindings                                                               | restore        | `for (x of y) { let z; }` where `x` is an outer binding: the for-of scope survives because its body declarations make it non-empty, and the outer `x` stays in the script scope. The scope is kept for the body's sake, not the head's.                                                                                                                                                                                                                                                                                                                      | tests/project-scope-table.test.ts:163-173 — "projects only the body bindings into the for-of scope" (script keeps `x`; $.body.1 for-of holds only `z`); mechanism = the generic emit guard at project-scope-table.ts:133                                                                                                                                                                                                                                                                                                                                                                         |
| Shadowing is preserved: same-named bindings stay under their own scope keys                                                                           | restore        | Because the table is keyed by scope node path and each scope carries its own variables list, `let x; { let x; }` yields two entries each holding an `x`. Nothing in the projector de-duplicates by name across scopes.                                                                                                                                                                                                                                                                                                                                       | tests/project-scope-table.test.ts:100-110 — "keeps each same-named binding under its own scope"; mechanism at project-scope-table.ts:132-137                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| The table is intentionally PARTIAL, and its key set is the instrumentation's wrap decision                                                            | restore        | Not every scope-shaped node has an entry (dropped blocks, non-declaring for heads). Instrumentation wraps a construct only when `context.scopeTable[path]` is truthy, so the projector's drop rules ARE the instrumentation's scope-wrapping policy — an implicit contract between two files that a rebuild can silently break by making the table total. (The worker additionally defends with `entry?.scopeKind ?? 'block'` / `entry?.variables ?? []`, so a rebuild's key-set drift would degrade silently, not crash.)                                   | project-scope-table.ts:133 and :57 (the two emit guards); consumers at instrument-variables.ts:183 "if (context.scopeTable[path]) {" and :201 (same test for classic for); defensive fallback at variables-worker-setup.ts:124-127 — "const entry = scopeTable[scopePath]; const scopeKind: ScopeKind = entry?.scopeKind ?? 'block';"                                                                                                                                                                                                                                                            |
| Two-pass key disjointness: Pass A keys ForStatement nodes, Pass B keys Program/Block/ForOf nodes — no collision, no overwrite                         | restore        | Pass B runs AFTER Pass A and writes into the same object, so correctness depends on the two key sets being disjoint. They are, because build-scope only ever creates scopes at Program/BlockStatement/ForOfStatement nodes while Pass A only keys ForStatement nodes. An implicit invariant with no test and no assertion — worth stating in the rebuild rather than re-discovering it as a bug.                                                                                                                                                             | project-scope-table.ts:52-67 (Pass A writes table, then `projectScopes(...)` writes the same table); scope-boundary set documented at src/lib/embody/lib/scope/types.ts:9-11 — "Scope boundaries are `Program`, `BlockStatement`, and `ForOfStatement` only"; Pass A's key source :59-60                                                                                                                                                                                                                                                                                                         |
| Defensive Identifier-only filter on for-head declarator ids (destructuring patterns silently skipped)                                                 | restore        | `readForHead` records a binding only when the declarator's `id.type === 'Identifier'`; an ObjectPattern/ArrayPattern head would contribute no variable AND no re-homing entry. Currently unreachable for gate-passing programs — ObjectPattern/ArrayPattern are absent from the JEJ node allowlist — so this is defense, not behavior under test. Restore the guard (cheap), but the rebuild should know it is dead code today and would become a real hole the day JEJ admits destructuring.                                                                | project-scope-table.ts:110-114 — "if (id.type === 'Identifier') { … variables.push({ name, kind }); forHeadIds.add(id); }"; allowlist without any Pattern node at src/lib/embody/lib/validating/just-enough-js.ts:553-599 [measured: grep -n "Pattern" src/lib/embody/lib/validating/just-enough-js.ts → no hits]                                                                                                                                                                                                                                                                                |
| Unwritten precondition: the input must be a JEJ-VALIDATED program (the `let\|const` kind cast is unchecked)                                           | restore-as-doc | `readForHead` casts the declaration's `kind` to `'let' \| 'const'` with no runtime check; a `var` head would be projected as kind `'var'` behind a lying type. It is safe only because the JEJ gate rejects `var` upstream in the real pipeline — but the function is exported and the tests call it on a bare acorn parse, so the precondition lives nowhere but the pipeline's ordering. Transport it as a documented precondition (@param contract) rather than as silent luck.                                                                           | project-scope-table.ts:105 — "const kind = initRecord.kind as 'let' \| 'const';" (no guard); the only enforcement is upstream at src/lib/embody/lib/validating/just-enough-js.ts:20-37 — "'${kind}' declarations are not allowed — use 'let' or 'const'" — invoked before projection at trace-variables.ts:57-64; tests bypass the gate: tests/project-scope-table.test.ts:16-19 calls `projectScopeTable(ast, buildScope(ast))` on a raw acorn parse                                                                                                                                            |
| Unit-test coverage level: 16 cases pinning the projection rules (the drop/re-home rules are all pinned; freeze depth and clone-safety only partially) | restore        | Rebuild-relevant fact about what a port can lean on. Pinned: empty program, single/multiple declarations, declaring block, sibling for loops, multi-declarator head, shadowing, empty head, assignment head, declaration-less block, block-holding-only-a-for-head, for-of with declaring body, for-of with non-declaring head, nested for loops, frozen table, frozen variables array. NOT pinned: freeze depth below the variables array (the {name,kind} objects), absence of Maps/nodes in the output, and the Pass-A/Pass-B key-disjointness invariant. | [measured: grep -c "\tit(\\\|\t\tit(" src/lib/embody/lib/evaluating/trace/variables/tests/project-scope-table.test.ts → 16]; file read end-to-end, tests/project-scope-table.test.ts:21-204                                                                                                                                                                                                                                                                                                                                                                                                      |

### B5 — docs commitments, browser tier, sandbox

| member                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | classification | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| README § Vocabulary pinning — the 6-row event pinning table (scope push, scope pop, initialize, read, assign, increment) with its five data columns beyond the event name (4 cross-reference + 1 payload) (payload beyond step/nodePath/scopeInstanceId · NM authority (canon) · semantics vocab (inspiration) · embody correspondence · JEJ syntax producer)                                                                                                                                                                          | restore-as-doc | This table is declared the naming contract itself, not commentary — 'This table is the naming contract; code, types, JSDoc, and tests use these terms.' It is the only place the per-event payload set and the producing JEJ syntax are stated side by side, and the only place the deliberate deviations from NM/semantics names are recorded. A rebuild that drops it loses the authority that adjudicates every event-name and payload-field question r1/r3 rows individually.                                                                                                               | src/lib/embody/lib/evaluating/trace/variables/README.md:36-55 — "The tier event names are pinned here. This table is the naming contract"; rows at :50-55                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| README § Glossary (ubiquitous language) — 19 pinned terms: tracer, lifecycle event, scope push/pop, declaring scope, synthesized for-scope, scope instance, initialize, read, assign, increment, value snapshot, registry, abrupt flag, halt attribution, scope table, worker logic, thread logic, dialog stub, handle/settlement                                                                                                                                                                                                      | restore-as-doc | Explicitly a propagation contract, not a reader aid: 'These terms propagate into types, JSDoc, DOCS.md, tests, and this README. Use them consistently.' Several entries carry behavior no code file states — e.g. the read/access narrowing ('the implicit access inside x += 1 / x++ does not emit a read; it is captured as the priorValue'), the wrote-derivation rule ('derived from the operator and prior value, not from comparing values'), and the value-capture split ('captured live … only scope-pop payload values are read from the registry').                                   | README.md:57-141 — header :57, propagation claim :59-60, read narrowing :92-95, wrote derivation :98-100, value-capture split :106-111                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| README § Bounded context — the ownership list (JEJ gate at the boundary, scope-table projection, instrumentation transform, worker logic + thin entry, thread logic, built generator + typed facade) and the explicit exclusions (console/dialog events; the embody adapter mapping; the quiz lens; realm and global bindings; per-iteration environment copies for classic for; the 'limit' pop reason and timeout/cancel pops; labels/with/eval-tampering/expression-target-for-of/undeclared identifiers; the trace/ module docs)   | restore-as-doc | Each exclusion is a named boundary that prevents a future reader from filing absence as a bug — 'Each is a named boundary, not a silent mistrace.' The exclusions also encode positive commitments a rebuild can lose silently: inert dialog stubs are injected so dialog calls do not crash; a classic-for body block that declares bindings still shows its own per-iteration push/pop while a for-of body block is merged; undeclared identifiers reach the worker unwrapped so their ReferenceError arrives without node-path attribution.                                                  | README.md:143-189 — ownership :143-149, exclusions :151-185, "Each is a named boundary, not a silent mistrace." :182-183, comma/paren transparency :187-189                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| README § Structure — the 7-file responsibility table (types.ts, trace-variables.ts, project-scope-table.ts, instrument-variables.ts, variables-worker-setup.ts, variables-worker-entry.ts, variables-thread-logic.ts)                                                                                                                                                                                                                                                                                                                  | restore-as-doc | The tier's file-anatomy commitment: one row per module with its single responsibility. It is the map that makes the four-seam split (project / instrument / worker / thread) legible; the rebuild will re-shape onto the kind/handle contract at P0-V, so the table's CONTENT must be re-authored rather than copied, but the commitment to carrying a per-file responsibility table is transportable.                                                                                                                                                                                          | README.md:191-201 — e.g. :197 "project-scope-table.ts \| Pure: validated AST + package scope analysis → the tier's clone-safe scope table"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| README opening architectural promise: standalone tracer streaming variable-lifecycle events with 'deterministic ordering, value snapshots, and node-path attribution for source highlighting'; 'the first real consumer of the engine'; instruments with 'Acorn + string-splicing (no Aran)'; calls the engine factory in-module; 'Code in, handle out — consumers never assemble engine parts.'                                                                                                                                       | restore-as-doc | Four separable commitments a rebuild could silently break: (a) the three stream guarantees named as the tier's product; (b) the no-Aran instrumentation choice (the repo has an Aran-based tracer elsewhere, so this is a real fork in the road); (c) the factory call happening INSIDE the module; (d) the consumer never assembling engine parts — the facade promise the kind/handle reshape must preserve in some form.                                                                                                                                                                     | README.md:3-13 — "deterministic ordering, value snapshots, and node-path attribution" :6-7; "instruments it with Acorn + string-splicing (no Aran), calls the engine factory in-module, and exports the built generator. Code in, handle out — consumers never assemble engine parts." :11-13                                                                                                                                                                                                                                                                                                                                                |
| README's three-tier vocabulary stance: the JEJ notional-machine doc is CANON ('Every tier event must be true to it'); semantics/tracing BindingEvent and syntax ScopeStep/InitializationStep/WriteStep are INSPIRATION ('existing shapes this tier borrows names from, not vocabularies it must conform to'); embody RuntimeScopeNMEvent/RuntimeBindingNMEvent are CORRESPONDENCE (a future adapter's mapping target, out of scope)                                                                                                    | restore-as-doc | This is the tier's conflict-resolution rule: when the borrowed names and the NM disagree, the NM wins and the deviation gets a glossary note. Without it a rebuild has no principled basis for keeping a name that differs from the neighboring tracers, and the cross-cutting claim ('spans what embody splits across the trace.variables filter (binding values) and the trace.syntax filter (scope framing)') is exactly the kind of statement that gets re-litigated if unrecorded.                                                                                                         | README.md:15-34 — "Its one authority is the notional machine; the other vocabularies are reference, not contract" :17-18; canon :20-22; inspiration :23-26; correspondence + the filter-split reconciliation "is the adapter's job, not this tier's" :27-34                                                                                                                                                                                                                                                                                                                                                                                  |
| DOCS § Architectural Sketch framing: the sketch is Phase-0, pre-implementation, and 'The Refactor step is held against this document — not what the code does, but what shape it takes.'                                                                                                                                                                                                                                                                                                                                               | restore-as-doc | A process commitment, not description: it makes the DOCS the design target that code is conformed to (matching the repo's standing 'docs are the design target, not a code mirror' discipline). A rebuild that regenerates DOCS from the new code inverts the direction this line establishes.                                                                                                                                                                                                                                                                                                  | DOCS.md:9-11 — "Written Phase 0, before implementation. The Refactor step is held against this document"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DOCS § Execution phases — SIX named phases with per-phase sync/async/purity/throw discipline and explicit Input/Output: 1 Validate (sync, throws), 2 Project (sync, pure), 3 Instrument (sync, pure given an instrumentable program; throws on rejected constructs), 4 Run and emit (factory call sync and lazy; emission async), 5 Narrow (per message, pure), 6 Settle (sync)                                                                                                                                                        | restore-as-doc | The pipeline decomposition plus the per-phase contract annotations is the tier's architectural spine and is stated at a granularity no code comment matches. NOTE THE DISCREPANCY r1 also reports from the other side: DOCS enumerates SIX phases here, while trace-variables.ts's entry body comments FOUR (validate → project → instrument → assemble); phases 5 (Narrow) and 6 (Settle) exist only in the docs and in the thread-logic/settlement code. The rebuild must resolve the count, not inherit both.                                                                                | DOCS.md:13-67 — phase headers at :14 ("**Validate** (sync, throws)"), :21, :32, :46, :57 ("**Narrow** (per message, pure)"), :61 ("**Settle** (sync)"); phase-4 laziness "nothing runs until the first pull" :50                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DOCS § Data flow — the mermaid flowchart (SRC → validate → AST → {project → TABLE, instrument → CODE} → SPEC → RUN → {emit → MSG → narrow → EVENT, author halt → HALT → settle} + the STOP branch for engine-made stops → SETTLEMENT; EVENT + SETTLEMENT → RESULT)                                                                                                                                                                                                                                                                     | restore-as-doc | The only artifact that shows the TWO settlement sources converging (worker-authored halt vs engine-made stop with no halt) and the fan-out where one validated AST feeds both project and instrument. Edge labels carry contract text found nowhere else, e.g. the emit edge's 'pauses until disposed' and the narrow edge's 'malformed → drop, frozen at yield'.                                                                                                                                                                                                                               | DOCS.md:71-86 — emit edge :79 "emit — clone-safe message per moment, pauses until disposed"; narrow edge :80 "narrow — pure, malformed → drop, frozen at yield"; STOP branch :83                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DOCS structural constraint — everything downstream of a successfully instrumented program degrades into a settlement, never an exception (both throwing boundaries fail fast, before any engine work)                                                                                                                                                                                                                                                                                                                                  | restore        | A behavioral invariant with a testable consequence: once instrumentation succeeds, no path out of the tier throws — timeouts, cancels, worker crashes, TDZ reads and const reassignments all arrive as settlements. The browser suite exercises exactly this for the errored/cancelled/timed-out classes (none of those tests wraps the call in a try/catch). (The validate arm's eager-throw placement is FLAGged separately, not classified here.)                                                                                                                                            | DOCS.md:90-95 — "Everything downstream of a successfully instrumented program degrades into a settlement, never an exception."; corroborated by tests/trace-variables.browser.test.ts:29-60 (three non-throwing settlement assertions)                                                                                                                                                                                                                                                                                                                                                                                                       |
| DOCS structural constraint — Project and Instrument are pure: 'No I/O, no engine, no shared state — both are deterministic functions of their inputs, testable in isolation'                                                                                                                                                                                                                                                                                                                                                           | restore        | A testability contract the rebuild's module split must preserve: it is what allows project-scope-table and instrument-variables to have their own Node-only unit suites with no engine in the picture. The qualifier matters too — instrumentation is pure GIVEN an instrumentable program, i.e. it inspects and throws first, then splices.                                                                                                                                                                                                                                                    | DOCS.md:96-99 — "Project and instrument are pure. No I/O, no engine, no shared state"; the qualifier :98-99                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DOCS structural constraint — instrumentation is line-preserving: no splice inserts or removes a newline, so the instrumented source has the same line count as the original and any engine line-report maps back; 'the binding invariant is line count, not byte-for-byte preservation', with the simple `=` form named as the one that relocates its RHS (into the eager `incoming` argument position) while staying within its original lines                                                                                        | restore        | The precise invariant is easy to over- or under-state in a rebuild: it is line COUNT, not byte fidelity, and the doc pre-empts the obvious wrong reading by naming the one form that moves code. Losing it breaks the mapping from any engine line-report back to learner source. (r2 owns the code-side splice rows; this is the stated invariant they must satisfy.)                                                                                                                                                                                                                          | DOCS.md:100-105 — "Instrumentation is line-preserving. No splice inserts or removes a newline" :100-102; the `=`-form carve-out and "the binding invariant is line count, not byte-for-byte preservation" :103-105                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DOCS structural constraint — only declaring scopes emit scope events; the script scope always does; an empty block pushes no scope, per the NM                                                                                                                                                                                                                                                                                                                                                                                         | restore        | Determines the event stream's shape for every program with non-declaring blocks; a rebuild that emits per-block unconditionally is wrong against the NM and would silently inflate every trace. The script-scope exception is the bookend the README also pins ('the evaluate stream's bookend even when it declares nothing').                                                                                                                                                                                                                                                                 | DOCS.md:106-108 — "Only declaring scopes emit scope events (the script scope always does; blocks only when they declare a binding). An empty block pushes no scope, per the NM."; README.md:76-80 restates it                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DOCS structural constraint — the abrupt flag is the sole authority for a pop reason, with its full clear/no-clear discipline: value-bearing helpers and scope opens clear it; closes NEVER clear it (one break unwinds several scopes, all reporting the same reason); a post-loop clear kills a flag left set by a final-iteration break or continue                                                                                                                                                                                  | restore        | Named in the same document as 'the single subtlest correctness point in the tier'. Three separable sub-rules (who clears, who must not clear, the post-loop clear) each of which a rebuild can lose independently while still passing naive tests; the multi-scope-unwind rule in particular only shows up in nested-break cases.                                                                                                                                                                                                                                                               | DOCS.md:109-113 — "The abrupt flag is the sole authority for a pop reason." and the clear discipline; "This is the single subtlest correctness point in the tier" :163-165; README.md:116-120 restates the flag protocol                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DOCS structural constraint — the registry is read, never the program, for cleanup values (re-reading a still-TDZ binding could throw and would perturb the trace), while event values (`value`/`priorValue`/`nextValue`) are captured live at the spec-correct moment via the deferred thunks                                                                                                                                                                                                                                          | restore        | A two-sided rule with a stated failure mode on each side: read the program at pop and you can throw or perturb; read the registry for live event values and you get a stale snapshot rather than the spec-correct moment. Both halves must survive together.                                                                                                                                                                                                                                                                                                                                    | DOCS.md:114-118 — "The registry is read, never the program, for cleanup values."; README.md:112-115 (registry glossary) restates "re-reading a TDZ binding would throw"                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DOCS structural constraint — an event is emitted only after the operation it describes completes without throwing (a TDZ read, a const reassignment, or any throw produces the attributed halt and NO event), with the carve-out that a short-circuiting logical assign (`??=`/`\|\|=`/`&&=`) still emits its `assign` with `wrote: false` and no `nextValue`, 'because a justified no-write is a completed operation, not a failure'; increment is governed by the same discipline                                                    | restore        | The emit/no-emit decision rule for the whole tier, including the one case that looks like a failure but is not. A rebuild is likely to get the throw case right and the short-circuit case wrong (suppressing the event) or vice versa (emitting on a throw); both are named here with the reasoning.                                                                                                                                                                                                                                                                                           | DOCS.md:119-124 — "An event is emitted only after the operation it describes completes without throwing." and the short-circuit carve-out; README.md:96-100 (assign glossary) pins `wrote` semantics                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DOCS structural constraint — determinism: step numbers and scope-instance ids are assigned worker-side in emission order, so the event stream is a pure function of the source, 'the property the quiz lens depends on'                                                                                                                                                                                                                                                                                                                | restore        | Names both the mechanism (worker-side, emission-order allocation) and the downstream consumer whose requirement it exists to satisfy. A rebuild that allocates ids thread-side or lazily breaks a consumer contract that is stated nowhere in the consumer.                                                                                                                                                                                                                                                                                                                                     | DOCS.md:125-127 — "Determinism. Step numbers and scope-instance ids are assigned worker-side in emission order … the property the quiz lens depends on."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DOCS structural constraint — the worker holds the only mutable state (registry, counters, abrupt flag; 'one declared mutable module'); the thread logic is a stateless mapping                                                                                                                                                                                                                                                                                                                                                         | restore        | The state-placement rule that makes the thread half 'trivially correct and testable' and justifies the worker/thread file split. The rebuild's module boundaries have to honour it or the thread suite's simplicity is unearned.                                                                                                                                                                                                                                                                                                                                                                | DOCS.md:128-130 — "The worker holds the only mutable state; the thread is pure."; README.md:128-132 (worker logic / thread logic glossary)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DOCS § Out of scope restated boundaries — a timeout or cancel KILLS the worker, so the scopes open at that moment do not pop at all; and non-time limits do not exist in this tier, so no scope ever pops with reason `'limit'`                                                                                                                                                                                                                                                                                                        | restore        | Two absence-guarantees that a rebuild cannot discover from the happy path and that a test author would otherwise file as a bug (missing pops on timeout). The `'limit'` value is stated as a member of the pop-reason type that is NEVER produced — a type/behavior gap that must be carried forward deliberately or the type narrowed on purpose.                                                                                                                                                                                                                                              | DOCS.md:132-141 — "a timeout or cancel kills the worker, so the scopes open at that moment do not pop; and non-time limits do not exist in this tier, so no scope ever pops with reason 'limit'"; README.md:51 pins "`limit` never produced"; README.md:174-175 the same exclusion                                                                                                                                                                                                                                                                                                                                                           |
| DOCS § Why this design — 'Thunks, not eager arguments, for reads': a read is wrapped as a deferred thunk so the binding is evaluated INSIDE the helper, because an eager argument would let a TDZ read throw before any code could stamp the node path, losing halt attribution; the assignment forms arrange prior-read and writer thunks so a TDZ or const throw fires at the exact moment the real assignment would have thrown, 'with the original expression doing all coercion (no operator is reimplemented)'                   | restore-as-doc | The rationale is the load-bearing part: it explains WHY the instrumentation shape is contorted, and the 'no operator is reimplemented' rule is a hard constraint on any rebuild that is tempted to compute compound-assignment results itself. Without the rationale a simplifying rebuild would 'clean up' the thunks and silently lose halt attribution on TDZ.                                                                                                                                                                                                                               | DOCS.md:145-155 — "The thunk moves the throw to where the path is known." :150-151; "with the original expression doing all coercion (no operator is reimplemented)" :154-155                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DOCS § Why this design — 'Scope reasons need a flag, not a finally': try/finally brackets every scope because it fires on every exit path, but finally cannot distinguish normal exit from break/continue/throw, and a break never propagates past the loop it targets; the flag is set just before the abrupt statement (or by the scope's own catch on a throw), read by close, cleared whenever normal evaluation resumes                                                                                                           | restore-as-doc | Explains the mechanism the previous row constrains, including the non-obvious 'a break never propagates past the loop it targets' — the fact that kills the naive catch-based alternative. Explicitly called out as the tier's subtlest correctness point whose edge cases live in the worker-logic tests, so the docs are the index into that test surface.                                                                                                                                                                                                                                    | DOCS.md:157-165 — "a break never propagates past the loop it targets" :161; "This is the single subtlest correctness point in the tier; the constraint list above and the worker-logic tests enumerate its edge cases." :163-165                                                                                                                                                                                                                                                                                                                                                                                                             |
| DOCS § Why this design — 'Emit everything; gate worker-side if it ever matters': every emit costs a full pause round-trip, but JEJ programs are tiny and the quiz UX wants a pause at every lifecycle moment (stepping IS the engine's backpressure); the disclosed cost is that an iteration-heavy loop exhausts the time budget through per-emit yield charges and ends `timed-out` with open scopes never popping; the named pressure valve is worker-side aggregation before emitting, 'tier discipline the engine cannot enforce' | restore-as-doc | A deliberate performance trade-off with its cost disclosed and its escape hatch pre-designed. A rebuild that meets the same timeout symptom without this record will treat it as a defect and add filtering, silently changing the stream the quiz lens depends on. Ties directly to the engine's per-yield charge (a live campaign concern).                                                                                                                                                                                                                                                   | DOCS.md:178-186 — "So the tier emits everything. The honest cost: an iteration-heavy loop exhausts the time budget through per-emit yield charges and ends `timed-out`" :183-185; valve :185-186                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DOCS § Why this design — inert dialog stubs: a worker has no native prompt/alert/confirm, so the worker injects `prompt → null`, `confirm → false`, `alert → undefined` to keep dialog-bearing valid JEJ programs running to a faithful variable trace; dialog interaction is explicitly NOT a lifecycle event in this tier                                                                                                                                                                                                            | restore        | Three exact return values, a stated reason (missing globals crash an otherwise-valid program), and a scope boundary (pairing dialogs with the trace is the embody-wiring half). All three parts are losable independently — in particular the exact values, which a rebuild would otherwise guess.                                                                                                                                                                                                                                                                                              | DOCS.md:188-195 — "(`prompt → null`, `confirm → false`, `alert → undefined`)" :192-193; README.md:133-136 (dialog stub glossary) restates the same three values                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DOCS § Why this design — 'A re-homed scope table, not the package analysis verbatim': the RATIONALE that the package scope analysis places classic-for head bindings in the ENCLOSING scope and overwrites sibling loops sharing a head name, so feeding it to push payloads would mis-attribute the loop variable and lose sibling loops                                                                                                                                                                                              | restore-as-doc | The two concrete defects in the upstream analysis (enclosing-scope placement, sibling-loop overwrite) are the entire justification for the projection step existing. Whatever the rebuild's upstream scope source turns out to be, it must be checked against these two failure modes. (WHERE the analysis is re-homed FROM is a FLAG — see flags; this row is only the recorded defect rationale.)                                                                                                                                                                                             | DOCS.md:167-176 — "places classic-`for` head bindings in the ENCLOSING scope and overwrites sibling loops that share a head name. Feeding that to the push payloads would mis-attribute the loop variable and lose sibling loops." :169-172                                                                                                                                                                                                                                                                                                                                                                                                  |
| DOCS/README disclosed simplification — one synthesized for-scope per classic `for` loop instead of the NM's fresh per-iteration environment, justified as value-equivalent in closure-free JEJ, with the carve-out that a for-body block declaring bindings still shows its own per-iteration push/pop                                                                                                                                                                                                                                 | restore-as-doc | A knowing divergence from the tier's own canon (the NM), disclosed twice with its validity condition (closure-free JEJ). If JEJ ever admits closures the simplification becomes wrong — a rebuild needs the condition recorded to know when to revisit, not just the behavior.                                                                                                                                                                                                                                                                                                                  | DOCS.md:172-176 — "The NM models a fresh environment per for-iteration; this tier models one for-scope per loop and discloses the simplification"; README.md:81-85 (synthesized for-scope glossary) and :166-172 (Bounded context exclusion, incl. the for-of body merge)                                                                                                                                                                                                                                                                                                                                                                    |
| README + DOCS § Navigation blocks — the outbound link set: enclosing module front door `../../README.md`, the engine's README/DOCS, the JEJ notional-machine authority, the sibling `./DOCS.md`/`./README.md` pair, and `./types.ts` as 'the contract' / 'the contract and the four cross-increment seams'                                                                                                                                                                                                                             | restore-as-doc | Every one of these relative paths breaks under the rebuild's re-homing, and the set encodes which documents this tier considers authoritative over it (engine two-sided contract, NM canon). The 'four cross-increment seams' phrase in particular asserts a types.ts structure the rebuild's contract file should still be able to answer to.                                                                                                                                                                                                                                                  | README.md:203-213; DOCS.md:1-5 and :197-205 — "[types.ts](./types.ts) — the contract and the four cross-increment seams." :201                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Browser tier doctrine (docstring of tests/trace-variables.browser.test.ts): the real-engine-transport suite proves what the same-thread Node fake cannot — worker spawn, the pause/resume protocol, clone-transport across the real postMessage boundary, real cancel/termination, and the real timer — and is deliberately held to ONE case per settlement class the transport handles distinctly, exact event semantics and the full ZOMBIES surface being the Node suites' job ('the real layer certifies fidelity, not logic')     | restore        | A test-strategy contract, not a comment: it fixes both the browser suite's minimum (one case per distinctly-handled settlement class) and its ceiling (no semantics coverage), and names the five transport facts the Node fake cannot certify. A rebuild that either skips the browser tier or lets it grow into a semantics suite violates a stated engine doctrine.                                                                                                                                                                                                                          | tests/trace-variables.browser.test.ts:5-15 — "worker spawn, the pause/resume protocol, clone-transport across the real postMessage boundary, real cancel/termination, and the real timer" :9-11; "ONE case per settlement class the transport handles distinctly (engine doctrine: the real layer certifies fidelity, not logic)" :13-15                                                                                                                                                                                                                                                                                                     |
| Browser fidelity case — COMPLETED: `traceVariables('let total = 0; for (let i = 0; i < 2; i = i + 1) { total = total + i; }')` called with NO options, awaited via `handle.result`, yields `settlement.outcome === 'completed'`, a non-empty `events` array, and at least one event whose `.event` is `'scope-push'`                                                                                                                                                                                                                   | restore        | Certifies over the real worker: the option-less call path (no `seconds` argument), the `{ events, settlement }` destructure off `handle.result`, the `event` discriminant field name and the literal `'scope-push'` value, and end-to-end clone transport of a classic-for trace. The literal discriminant value is a naming commitment the rebuild's event union must keep or consciously supersede.                                                                                                                                                                                           | tests/trace-variables.browser.test.ts:17-27 — call :18-20, destructure :22, assertions :24-26 including `events.map((event) => event.event)).toContain('scope-push')`                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Browser fidelity case — ERRORED: `traceVariables('const c = 1; c = 2;').result` settles `outcome === 'errored'` with `settlement.halt?.errorName === 'TypeError'` and `typeof settlement.halt?.nodePath === 'string'`                                                                                                                                                                                                                                                                                                                  | restore        | Proves the worker-authored halt survives the REAL structured-clone boundary with both attribution fields intact — the const-reassignment case named in the DOCS emit rule. Pins three surface facts: `settlement.halt` is a nullable object, its error identity field is `errorName` (a name, not an Error instance — instances do not clone), and `nodePath` is a string. r1 owns the type shape; this row owns the over-the-wire guarantee.                                                                                                                                                   | tests/trace-variables.browser.test.ts:29-35 — "authors a stamped halt for a runtime error over the real clone boundary" :29; assertions :32-34                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Browser fidelity case — CANCELLED: `break`ing out of `for await (const event of handle)` after the first event terminates the real worker; `settlement.outcome === 'cancelled'` and `settlement.halt === null`                                                                                                                                                                                                                                                                                                                         | restore        | Certifies three things the Node fake cannot: the handle is directly async-iterable (`for await … of handle`, not `handle.events()`), consumer-side `break` is a real cancel that terminates the worker, and cancellation produces NO halt (halt is strictly null) while `result` still settles after the iteration was abandoned. The halt-null assertion is the observable form of the DOCS' 'engine-made stop with no halt' branch.                                                                                                                                                           | tests/trace-variables.browser.test.ts:37-50 — iteration + break :41-44, assertions :47-49 including `expect(settlement.halt).toBeNull()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Browser fidelity case — TIMED-OUT: `traceVariables('while (true) { let x = 1; }', { seconds: 0.2 }).result` settles `outcome === 'timed-out'` with `settlement.engineError?.cause === 'timeout'` and `settlement.halt === null`                                                                                                                                                                                                                                                                                                        | restore        | Certifies the real timer (not a fake clock), sub-second `seconds` values being honoured as fractional seconds, and the two-field settlement shape on an engine-made stop: `engineError.cause` carries the literal `'timeout'` while `halt` stays null. Also the only test that exercises the DOCS' disclosed cost path (a budget-exhausting loop) end to end.                                                                                                                                                                                                                                   | tests/trace-variables.browser.test.ts:52-60 — call with `{ seconds: 0.2 }` :53-55, assertions :57-59 including `expect(settlement.engineError?.cause).toBe('timeout')`                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Browser-suite import + runner shape: the suite imports the tier by DEFAULT export (`import traceVariables from '../trace-variables.js'`, extensioned .js specifier) and runs under a dedicated vitest 'browser' project that includes only `src/lib/**/*.browser.test.ts` and serves COOP/COEP headers so SharedArrayBuffer is available                                                                                                                                                                                               | restore        | The real-worker tier only exists if the harness exists: SharedArrayBuffer requires cross-origin isolation, which the workspace supplies via a `coop-coep-headers` plugin, and the Node project explicitly EXCLUDES `*.browser.test.ts` so the two tiers never collide. A rebuild that ports the tests without the project wiring gets a silently-unrun or silently-broken fidelity tier. Also pins that the module's public entry is a default export.                                                                                                                                          | tests/trace-variables.browser.test.ts:3 — `import traceVariables from '../trace-variables.js'`; vitest.workspace.ts:39 exclude of `src/lib/**/*.browser.test.ts`, :70-74 the `coop-coep-headers` plugin setting `Cross-Origin-Opener-Policy: same-origin` / `Cross-Origin-Embedder-Policy: require-corp`, :81-82 the `browser` project include [measured: grep -n "browser\|coop\|coep\|Cross-Origin" vitest.workspace.ts vitest.config.ts]                                                                                                                                                                                                  |
| SANDBOX PAGE — the quarry variables tracer has NO sandbox page (no `sandbox.html`, no `vite.sandbox.config.ts`) while all three sibling evaluators do; under HR-15 the rebuild GETS one, built EARLY in the chain and extended per increment, with every user-observable increment firing its own 🔍 checkpoint                                                                                                                                                                                                                        | ADDITION       | MANDATORY ROW per charter. This is a ruled ADDITION, not a restore: there is nothing to restore. Measured absence — the variables tree holds 15 tracked files — 2 docs + 7 .ts + 6 tests, and zero html/sandbox artifacts, whereas run/, intercept/ and trace/semantics/ each carry a `sandbox.html` + `vite.sandbox.config.ts` pair. HR-15 overrides the tier's declared 🔍 skip by name, so the rebuild owes a page from the first user-observable increment onward — and per the ledger's sandbox section the page must carry the UNION of both lineages' features where counterparts exist. | [measured: find src/lib/embody/lib/evaluating/trace/variables -iname '*sandbox*' -o -iname '*.html'] → no output; [measured: find src/lib/embody/lib/evaluating -iname '*sandbox*'] → run/, intercept/, trace/semantics/ each have sandbox.html + vite.sandbox.config.ts, variables has neither. Ruling: .planning-handoffs/evaluators-api-restoration/LOSS-LEDGER.md § Rulings of record › Ceremony and sandboxes (HR-15) — "sandbox pages are built EARLY in each evaluator chain and extended per increment … the declared 🔍 skip for variables is overridden"; ledger row :154 — "variables' declared skip OVERRIDDEN — it gets a page" |

### B6 — history, consumer seam, design intent

| member                                                                                                                                                                                                                                           | classification                                  | rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| consumer-facing member name + shape: `evaluation.events.traceVariableLifecycle(opts?)` is a RAW TRACER METHOD, not a tier — it sits beside `run`/`intercept`/`trace.{variables,syntax,semantics}` and is deliberately absent from the tier table | restore — DEFERRED (attachment campaign, HR-10) | The consumer seam is a named, documented non-tier. A rebuild that folds the variables tracer into the `trace.variables` tier member erases a distinction embody's docs make three times. NOTE: the NAME itself is HR-8's business; what I classify here is the SHAPE (a separate raw member returning the tracer's own handle, alongside a same-named tier member that returns a stub).                                                                                                                                                                                                                                                                                                                               | src/lib/embody/index.ts:499-503 — `traceVariableLifecycle: (options?…, createTransport?…): VariablesTraceHandle =>`, declared in the same object literal as `trace: { variables: … => makeStubEvaluateHandle(runInstance) }` (index.ts:492-498); src/lib/embody/DOCS.md:266 — "**`traceVariableLifecycle` is the exception — a raw tracer method, not a tier.** It is absent from the table above"; src/lib/embody/README.md:223 table row                                                         |
| gate placement: real-composition-vs-canned-scenario, NOT `status.created` — and the `status.created` correlation is INVERTED                                                                                                                     | restore — DEFERRED (attachment campaign, HR-10) | Load-bearing and counter-intuitive: canned `OK` is `created: true` yet throws; real apex is `created: false` yet forwards. A rebuild that 'tidies' this to gate on `created` breaks every working real composition and admits every throwing canned leaf. Documented as a consumer instruction (`try/catch`, never a `created` check).                                                                                                                                                                                                                                                                                                                                                                                | src/lib/embody/index.ts:475-476 — `if (realSource === null) { throw new Error('traceVariableLifecycle: not available on canned scenario');`; src/lib/embody/DOCS.md:281-283 — "The `status.created` correlation is **inverted** here: canned `OK` is `created: true` yet throws, while real apex is `created: false` yet forwards. Consumers therefore guard `traceVariableLifecycle` with `try/catch`, never with a `status.created` check."                                                      |
| below-gate behavior: the forward THROWS rather than returning a `not-runnable` no-op — asymmetric with every NM tier                                                                                                                             | restore — DEFERRED (attachment campaign, HR-10) | Every other evaluate member short-circuits below its gate with `endReport.outcome: 'not-runnable'` and an empty stream. This one member throws synchronously in BOTH refusal channels (canned sentinel; inadmissible JEJ propagated from the tracer). Losing the asymmetry silently changes the consumer contract from try/catch to result-inspection.                                                                                                                                                                                                                                                                                                                                                                | src/lib/embody/DOCS.md:267-269 — "It is absent from the table above because it neither gates on `status.created` nor returns a `not-runnable` no-op"; DOCS.md:274-276 — "inadmissible source (non-JEJ, unparseable, or an instrumenter-rejected construct) **throws synchronously at the call**"; contrast src/lib/embody/index.ts:492-498 (`trace.variables` → `makeStubEvaluateHandle`)                                                                                                          |
| return type is FOREIGN by design: `.events` iterate `VariablesTraceEvent` (not `AnyNMEvent`); `.result` resolves to `VariablesTraceResult` = `{ events, settlement }` (not a `RunInstance`, no `endReport`)                                      | restore — DEFERRED (attachment campaign, HR-10) | embody deliberately does NOT adapt the tracer's handle onto its own `EvaluateHandle`/`AnyNMEvent` vocabulary at this member — the tracer's own typed surface reaches the consumer unmediated. This is the one place the quarry's tracer types cross the embody boundary intact; an adapter-normalizing rebuild would lose it.                                                                                                                                                                                                                                                                                                                                                                                         | src/lib/embody/index.ts:474 — `): VariablesTraceHandle {`; src/lib/embody/DOCS.md:284-288 — "The returned handle is foreign to the other evaluate surfaces: its `.events` iterate `VariablesTraceEvent` (not `AnyNMEvent`), and its `.result` resolves to `VariablesTraceResult` (`{ events, settlement }`), not a `RunInstance`."                                                                                                                                                                 |
| option pass-through: a DELIBERATELY DUPLICATED option type (`TraceVariableLifecycleOptions`), not an alias/re-export of the tracer's `TraceVariablesOptions`                                                                                     | restore — DEFERRED (attachment campaign, HR-10) | Structurally identical today (`{ seconds? }`), distinct on purpose so a tracer-side option change surfaces as a COMPILE ERROR at the forward site rather than silently widening embody's public surface. This is a deliberate anti-coupling device; a rebuild that 'DRYs' it into a re-export destroys the guarantee without any visible diff in behavior.                                                                                                                                                                                                                                                                                                                                                            | src/lib/embody/index.ts:472 — `options?: TraceVariableLifecycleOptions,` (then forwarded raw at :478 `return traceVariables(realSource, options, createTransport);`); src/lib/embody/types.ts:382-390 — "Deliberately distinct from the tracer's own `TraceVariablesOptions` (though structurally identical today): a tracer option change becomes an embody-side propagation decision, surfaced as a compile error at the forward site rather than silently widening this public surface."        |
| the empty program `''` is a REAL composition that forwards (not a canned sentinel)                                                                                                                                                               | restore — DEFERRED (attachment campaign, HR-10) | A falsy-string check is the obvious rebuild bug here. embody discriminates on `realSource === null` specifically so `''` reaches the tracer as a real (admissible, zero-event) program. Named explicitly in the docblock, so it is a pinned intent, not an accident.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | src/lib/embody/index.ts:461-462 — "Real compositions pass `source.code` (including `''`, a real empty program): this forwards to the variables tracer"; index.ts:475 — `if (realSource === null)`                                                                                                                                                                                                                                                                                                  |
| per-call binding to the snippet's real source string (`realSource` closed over at `makeEvaluationEvents` time, not read from the snippet at call time)                                                                                           | restore — DEFERRED (attachment campaign, HR-10) | The member is minted bound to one source; `realSource: string \| null` IS the canned/real discriminant, computed once upstream. The rebuild's equivalent must keep the discriminant at construction, not re-derive it per call from mutable snippet state.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | src/lib/embody/index.ts:457-458 — "The raw `traceVariableLifecycle` forward, bound per call to a snippet's real source string"; index.ts:483-484 — `function makeEvaluationEvents(runInstance: RunInstance, realSource: string \| null,)`                                                                                                                                                                                                                                                          |
| embody self-gates on the TRACER's JEJ admission, explicitly NOT on `status.created` — the tracer owns its own gate at call time                                                                                                                  | restore — DEFERRED (attachment campaign, HR-10) | Division of responsibility: embody performs exactly one check (real vs canned) and delegates admissibility wholly to the tracer's own synchronous gate. A rebuild that pre-validates in embody duplicates (and can diverge from) the tracer's gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | src/lib/embody/index.ts:462-464 — "forwards to the variables tracer, which self-gates on its own Just-Enough-JavaScript admission — NOT on `status.created`"; the tracer-side gate at src/lib/embody/lib/evaluating/trace/variables/trace-variables.ts:61-63 — `const report = validateProgram(code, justEnoughJs); if (!report.isValid \|\| report.ast === undefined) { throw gateError(report); }`                                                                                               |
| design intent (PHASE-1-HANDOFF): a `binding` event family as a FIRST-CLASS family distinct from a `variable` (syntax-occurrence) family — one slot's value life, including the explicit `TDZ → initialized` transition                           | ADDITION                                        | The quarry ships ONE flat 6-variant union that conflates the two readings: `read`/`assign`/`increment` are syntax occurrences AND binding-life events at once, keyed by `scopeInstanceId` + `name`. TDZ is present only implicitly (the scope-push burst declares 'every variable in implicit TDZ'); the quarry's InitializeEvent IS the binding's TDZ→initialized moment (types.ts:161-166 — 'A binding receives its first value'; BindingStatus 'tdz'\|'initialized' at types.ts:76); what the quarry LACKS is the first-class FAMILY split and an explicit from/to status payload, not the transition event itself. Splitting the families is a design-intent ADDITION for P0-V, not a quarry behavior to restore. | src/lib/study-lenses/evaluators-deprecated/PHASE-1-HANDOFF.claude-delete-if-stale.md:99-102 — "**variable event** — a syntax occurrence (a read/write at a source location); **binding event** — one slot's value life (the `TDZ → initialized` transition, reads, writes)"; quarry union at src/lib/embody/lib/evaluating/trace/variables/types.ts:205-212 (`ScopePushEvent \| ScopePopEvent \| InitializeEvent \| ReadEvent \| AssignEvent \| IncrementEvent`); implicit TDZ at types.ts:139-141 |
| design intent (PHASE-1-HANDOFF): an `environmentDiff` event family — git-style deltas to the live environment records (new scopes / frames on recursion, declarations)                                                                           | ADDITION                                        | The quarry has NO diff event. Its closest analogue is the scope-push declared-variable BURST and the scope-pop final-variable list — full snapshots at scope boundaries, not deltas, and nothing at all between boundaries. Recursion produces new `scopeInstanceId`s but no frame-creation delta event.                                                                                                                                                                                                                                                                                                                                                                                                              | PHASE-1-HANDOFF…md:103-105 — "**environmentDiff event** — a git-style delta to the live environment records (new scopes / frames on recursion, declarations)"; quarry scope events at src/lib/embody/lib/evaluating/trace/variables/types.ts:143-155 (`ScopePushEvent … readonly variables: ReadonlyArray<DeclaredVariable>` / `ScopePopEvent … ReadonlyArray<FinalVariable>`)                                                                                                                     |
| design intent (PHASE-1-HANDOFF): events `prev`/`next`-linked and folded by a lens into a scrubbable timeline                                                                                                                                     | ADDITION                                        | The quarry variables tracer has ZERO prev/next linkage — that pattern exists only in trace/semantics. Variables events carry a monotonic `step` number and nothing else navigational. Chain accessors on variables events are a genuine ADDITION for P0-V (and per digest requirement #7 the scrubbing is served by the memoized result array, so this does not re-open HR-2).                                                                                                                                                                                                                                                                                                                                        | [measured: grep -c 'readonly prev\\\|readonly next\b' src/lib/embody/lib/evaluating/trace/variables/types.ts → 0]; PHASE-1-HANDOFF…md:106-108 — "Events are reversible (carry both sides, à la the quarry's `priorValue`/`nextValue`), `prev`/`next`-linked, and folded by a lens into a scrubbable timeline"; quarry base fields at types.ts:132-136 (`step` / `nodePath` / `scopeInstanceId` only)                                                                                               |
| design intent (PHASE-1-HANDOFF): the three event families are CONFIG-GATED — 'a single notional-machine moment fans into up to three config-gated event families'                                                                                | ADDITION                                        | The quarry offers no event gating whatsoever: its entire option surface is `{ seconds? }`, forwarded to the engine spec. Per-family (and, per the historical lineage, per-variable) gating is an ADDITION the current 1-option surface would foreclose. Ties directly to forward-compat requirement #10 and to HR-16's config-richness archaeology (see flags).                                                                                                                                                                                                                                                                                                                                                       | PHASE-1-HANDOFF…md:95-98 — "A single notional-machine moment fans into up to three config-gated event families"; quarry options at src/lib/embody/lib/evaluating/trace/variables/types.ts:216-220 — `type TraceVariablesOptions = { /** Time budget in seconds… */ readonly seconds?: number; };`                                                                                                                                                                                                  |
| design intent (PHASE-1-HANDOFF): reversibility ('carry both sides') generalized to ALL event families                                                                                                                                            | ADDITION                                        | Quarry reversibility is PARTIAL and the handoff cites it as the model to generalize. `assign` carries `priorValue`/`nextValue?`; `increment` carries `priorValue`/`nextValue`/`returnedValue`. But `initialize` carries only `value` (no prior), `read` only `value`, and the scope events carry snapshots with no both-sides pairing. Extending reversibility to initialize/scope/environmentDiff is new.                                                                                                                                                                                                                                                                                                            | PHASE-1-HANDOFF…md:106-107 — "reversible (carry both sides, à la the quarry's `priorValue`/`nextValue`)"; quarry: types.ts:176-187 (`AssignEvent` priorValue + optional nextValue), :191-203 (`IncrementEvent`), vs :158-166 (`InitializeEvent` — `readonly value: ValueSnapshot;` only) and :168-173 (`ReadEvent` — `value` only)                                                                                                                                                                 |
| design intent (PHASE-1-HANDOFF): `environment` stays STRICTLY the static fact in all kind-level vocabulary; the runtime lives in the tracer event unions                                                                                         | restore-as-doc                                  | A vocabulary constraint, not code — but exactly the kind of commitment a rebuild loses silently and then violates by naming a runtime event payload `environment`. The quarry is already compliant (it says `scope`/`scopeInstanceId`/`variables`, never `environment`), so this transports as a doc rule the rebuild's types.ts and DOCS must carry.                                                                                                                                                                                                                                                                                                                                                                 | PHASE-1-HANDOFF…md:108-110 — "`environment` stays **strictly the static fact** in all kind-level vocabulary; the runtime lives in these tracer event unions"; [measured: grep -c 'environment' src/lib/embody/lib/evaluating/trace/variables/types.ts → 0 occurrences in the quarry's event vocabulary]                                                                                                                                                                                            |
| design intent (PHASE-1-HANDOFF): a tracer whose worker half needs data mints its OWN clone-safe slice and posts that, never the cyclic graph                                                                                                     | already survives                                | The handoff names the quarry's own `ScopeTable` as the pattern to follow — so this is not an ADDITION, it is the quarry behavior being ratified forward. Recording it so the rebuild does not treat 'post `Facts`' as an available shortcut. (The upstream ANALYSIS the ScopeTable is projected FROM is a separate, FLAGged question — see flags.)                                                                                                                                                                                                                                                                                                                                                                    | PHASE-1-HANDOFF…md:129-131 — "`Facts` is embody's frozen main-thread graph — a tracer whose worker half needs data mints its **own** clone-safe slice (the quarry `ScopeTable` / `RuntimeGates` pattern) and posts that, never the cyclic graph"; quarry `ScopeTable` clone-safe workerConfig at src/lib/embody/lib/evaluating/trace/variables/types.ts:311-325                                                                                                                                    |
| design intent (PHASE-1-HANDOFF): each tracer lives at `tracers/<name>/`, exports ONE `Evaluator` object as `index.ts` default export, with its own event union + settlement in its own `types.ts`, plus `README.md` / `DOCS.md` / `tests/`       | restore-as-doc                                  | The file-anatomy half of the attachment contract is orthogonal to the kind reshape (the `EvaluationStream`/`settled` RETURN half is FLAGged, and per the research digest HR-3 supersedes handoff lines 130-133). The quarry already satisfies README/DOCS/types/tests but has NO `index.ts` and no single exported `Evaluator` object — its public entry is a bare default-exported function file, `trace-variables.ts`. Transport the anatomy rule as doc.                                                                                                                                                                                                                                                           | PHASE-1-HANDOFF…md:112-116 — "Each tracer is `tracers/<name>/`, exporting one `Evaluator` object (`index.ts` default export), its own event union + settlement in its own `types.ts`, plus `README.md` / `DOCS.md` / `tests/`"; [measured: git ls-files src/lib/embody/lib/evaluating/trace/variables/ → 15 files, no index.ts]; src/lib/embody/lib/evaluating/trace/variables/trace-variables.ts:54 — `export default function traceVariables(`                                                   |

<!-- markdownlint-enable MD037 MD038 -->

---

## Rulings of record

The campaign's binding decisions, each dated at the turn the human gave it. This
section is the home; the plan of record
(`~/.claude/plans/read-and-execute-the-rosy-sky.md` § ②) mirrors them for
execution and never holds one alone.

### Scope and posture

- **Fidelity-first (human ruling 2026-08-06, HR-4).** The task is to port the
  REFERENCE signature and behavior (`src/lib/embody/lib/evaluating/`). A
  port-side deviation survives ONLY where demonstrably stronger, and every such
  exception is named in this ledger with its strength argument. The default
  classification is `restore`.
- **`danger/` is out of scope for repair (human ruling 2026-08-05, HR-1).** Its
  synchronous-only IO mocks (`danger/backend/types.ts:63`, with the constraint
  at `:57` that a real `<script>` cannot `await`) are expected and correct.
  Corollary: danger having mocks does NOT excuse run not having them — that
  reasoning is re-partitioning, and run's IO question stands on its own.
- **Replay stays out (human ruling 2026-08-05, HR-2).** The port's non-goal at
  `lib/engine/DOCS.md:174-175` — _"Replay by re-iterating a settled handle — the
  result's items array is the cache; each evaluate call is a fresh run"_ — is
  affirmed. The re-iteration machinery in `create-execution.ts` is therefore NOT
  part of the migration: the handle contract is wanted, the replay cache is not.
- **Scope (human ruling 2026-08-06, HR-10):** run + intercept rebuild plus the
  trace/variables port; variables was never audited, so its own fidelity pass
  precedes its Phase 0 and is staged strictly later so it cannot block
  run/intercept. trace/semantics, trace/syntax and adapter/ are future
  campaigns. The kind is designed against the fourteen tracer
  forward-compatibility requirements (`research-digests-2026-08-05.json` —
  recoverable via
  `git show a8a0128d:<this directory>/research-digests-2026-08-05.json`, key
  `.result.tracers`).
- **Build forward; deprecate and rebuild (human ruling 2026-08-06, HR-11 +
  HR-14).** New commits only, no reverts. The current port evaluators are
  renamed to `src/lib/study-lenses/evaluators-deprecated` (single-dash,
  kebab-legal) and `evaluators/` is rebuilt fresh under the canonical name, with
  the quarry AND the deprecated port both serving as references. The deprecated
  region stays in tsc and vitest, frozen — compile-and-green only; its exit
  condition is danger's future migration onto the new kind.
- **The `nodeAtSpan` helper is this campaign's own unit, and the campaign MAY
  edit greenfield `embody/` for it (human ruling 2026-08-13, HR-22).** This
  REVERSES the 2026-08-11 pivot that routed W1.c to the session owning
  `src/lib/study-lenses/embody/`: nothing needs the helper yet — its only
  consumer is the intercept enrichment increment, waves out — so rather than
  carrying it as an outstanding obligation on another session, it folds back
  into this campaign's own wave plan and lands when its consumer does. The
  ratified contract survives the reversal unchanged and is spec'd in full at
  `~/.claude/plans/node-at-span-embody-handoff.md` (total function,
  exact-span-or-`null`, ascent from `byOffset[start]`, three mandatory tests
  including the empirically-verified identical-span-ancestor case). That file is
  now a CONTRACT SOURCE for a campaign unit, not a request to a sibling session.
- **Full JavaScript is the target (human ruling 2026-08-12, HR-18).** Design
  against all of JavaScript: **the shared evaluator signature and the engine are
  LEVEL-BLIND.** JEJ — or any other level — is a per-tracer scoping concern,
  settled per tracer and later; it must not shape the kind, the shared
  signature, or the engine. A design argument that rests on what JEJ admits or
  forbids is out of order at this layer.
- **Instrumentation is assumed sound (human ruling 2026-08-12, HR-19).** The
  design premise is that instrumentation introduces no errors of its own; tracer
  authors carry that guarantee. The kind does not optimize for detecting
  instrumentation defects, and no contract surface exists to report them — the
  machinery-defect kind (HR-8) does not reach them, because an
  instrumentation-injected throw is indistinguishable from a learner throw at
  the halt boundary. The cost is stated so it is not rediscovered as a surprise:
  when the premise is violated, the failure presents as the learner's own.

- **The discharge rule extends to Phase 0 (human ruling 2026-08-12, HR-21).**
  The full statement — every Phase-0 unit's README owes a `## Discharges`
  section whose identifiers resolve, checked by `ar-1`/`ar-2` in addition to
  their focus areas — lives in this ledger's preamble; recorded here so the
  section that declares itself the rulings' home resolves HR-21 by name.

### The contract

- **A handle, not a bare stream (human ruling 2026-08-05, HR-3).** `main()`
  returns the reference's shape —
  `AsyncIterable & PromiseLike & { result, cancel }` — widened per evaluator
  with its own eager fields. Ruled on three grounds: zero current consumers, the
  handle being a strict superset that leaves consumer-paced iteration intact,
  and `AsyncIterable<never>` being a shape that exists only to satisfy an
  interface it does not benefit from. Consequences ruled in by implication: the
  shared kind's `types.ts` changes (it declares `main`'s return type, so eager
  fields cannot be added evaluator-side); it is a Phase-0 contract redesign with
  `ar-1` and `ar-2`; and eager-versus-deferred is a per-evaluator choice, which
  the reference itself made both ways (`run/types.ts:203` synchronous,
  `intercept/types.ts:315` a Promise).
  - **Its consequence 2 is superseded in mechanism (2026-08-06).** HR-3 said
    danger gets touched to keep compiling. Under HR-11/HR-14 the old kind's
    `types.ts` is never edited — it rides into `evaluators-deprecated/`
    untouched — so danger keeps compiling with ZERO edits (zero importers
    outside the region, no engine imports, inside tsc and vitest). The
    obligation is discharged by the old kind's immutability, not by an edit.
- **The full generator surface (human ruling 2026-08-06, HR-5).** intercept's
  handle widens to `.next`/`.return`/`.throw` plus
  `{ fail, code, options, ast }` per the reference (`intercept/types.ts:261`).
  Where the reference's own semantics were accidental or engine-incompatible —
  `.throw` was the NATIVE generator method, only next/return were overridden —
  this ledger specifies the replacement and records the delta as its own row.
- **Both consumption modes (human ruling 2026-08-06, HR-6).** `for await`
  step-through AND `await handle` → the complete result. Creation stays inert:
  the run starts at first consumption (first pull OR first await), never at
  construction. This is the campaign's one laziness carve-out against the
  reference, which queueMicrotask-auto-started at creation
  (`shared/create-execution.ts:107-117`); it also kills that design's
  one-microtask claim race.
- **Drain-on-await cancels at an unanswered ask (human ruling 2026-08-06,
  HR-7).** Under a batch drain, an intercept ask nobody answers cancels the run
  at that ask, settling with the events so far. "Unanswered" is STRUCTURAL — no
  mock supplied for that verb — never temporal.
- **Reference names and values, wholesale (human ruling 2026-08-06, HR-8).**
  `Execution`, `RunHandle`, `RunResult`, `InterceptHandle`, `InterceptResult`,
  `outcome: 'complete'|'cancel'|'fail'|'timeout'|'iteration-limit'|'error'`,
  `ok`, the result fields (`events`, `code`, `options`, `ast`, `visitCounts`),
  and the reference's event-field spellings all return. Port enrichments ride as
  ADDITIONS in reference style: machinery-defect discrimination as an added
  error kind, `loc` plus an offset pair on events, the pending-interaction arm,
  and the `trip` record on iteration-limit.
- **IO mocks on both evaluators (human ruling 2026-08-06, HR-9).** run gains
  worker dialog traps over the engine's existing `onCall` seam; a supplied mock
  answers, and no mock settles a classified io-error outcome. intercept answers
  mocks at its `serveAsk` seam BEFORE a pending interaction is minted; no mock
  yields the pending interaction as today. `io.console` per-method callbacks
  return.
- **Entwining re-derived against current embody (human ruling 2026-08-06,
  HR-12).** The reference's event signatures predate today's
  `src/lib/study-lenses/embody/`, so fidelity here targets the CAPABILITY, not
  the mechanism. Events gain an offset pair stamped from the wrap's
  original-text parse; enrichment happens thread-side inside `onMessage`, before
  the engine's shallow freeze-at-yield (`lib/engine/evaluate.ts:275`); the
  result carries `eventsByNode` and `visitCounts`; `link/`'s shadow AST tree is
  NOT ported. **Mechanism:** enumerable event fields stay plain data (`step`,
  `loc`, offsets, `nodePath`); `node`, `prev`, `next` and `callee` are
  NON-ENUMERABLE ACCESSORS resolving through `facts.entwined.byPath` and a
  thread-side pointer, so `JSON.stringify` stays safe while `event.node` answers
  with the real `EntwinedNode`. The mutable-pointer accessor is a NAMED
  exception to the no-mutable-closures rule, scoped: installed inside
  `onMessage` before return, never written after yield. **Caveat carried
  deliberately:** the quarry precedent at
  `trace/semantics/tracing/types.ts:836-843` covers `prev`/`next` ONLY — that
  tracer explicitly DECLINES a `.node` reference (`:845-849`, _"There is no
  `.node` reference — attribute via `event.nodePath`"_). The `.node`/`.callee`
  accessors therefore EXTEND the precedent rather than follow it; `ar-1` must
  challenge that extension on its own terms.
- **Tracer config fidelity targets git history (human ruling 2026-08-10,
  HR-16).** The trace config surfaces were painstakingly crafted with the
  creator of Aran, and the working-tree quarry copy is itself a descendant — so
  the fidelity target is the RICHEST revision in history, not the current
  checkout. Anchors
  `[measured: git log --follow over the quarry's semantics/config.types.ts; git log --diff-filter=A over '*trace*/config*']`:
  a full `configuring/` pipeline (ajv schema validation, expand-shorthand,
  fill-defaults, prepare-config, validate-config, tests, README) exists at
  `68fa9981` and `c969412b` under
  `src/lib/welcome-to-programming/just-enough/javascript/lib/evaluating/trace/`.
  Consequences: every tracer fidelity audit sweeps history (`git log --follow`
  plus `git show <sha>:<path>`) and may cite `SHA:path` as reference evidence;
  the kind must not foreclose that config richness; and the semantics port's
  future audit opens with this archaeology.
- **Refusal, never a throw at the learner (human ruling 2026-08-12, HR-17).** An
  evaluator that cannot serve a spec returns a STRUCTURED REFUSAL. This settles
  the throw-versus-refuse tension the W1.b audit flagged (the quarry's variables
  tracer throws synchronously at its gate; the region's own stated posture is
  refusal-as-data). Note the ratified forward-compat requirement that typed sync
  throws stay _permitted_ at the kind level: that permission is not withdrawn,
  but this campaign's evaluators refuse.
- **Evaluators evaluate (human ruling 2026-08-12, refined 2026-08-13, HR-20).**
  An evaluator does NO lifecycle-phase work: it runs the program and reports
  errors as a bare runtime would. **Its error phase discriminant is exactly two
  values — `'creation' | 'evaluation'`** (human ruling 2026-08-13): did the
  program fail before it ran, or while running. Nuance WITHIN creation belongs
  to the embodiment and the main orchestrator, which already hold it; the
  evaluator neither mirrors, translates, nor re-derives it, and carries no
  lifecycle-stage vocabulary of its own.
  - This **refines rather than retires** the ratified `phase` row and E2 (§ run
    — result and error taxonomy; § The ratification): the split stands and is
    still its own engine increment. What changed is the second value's spelling
    — `'evaluation'`, matching the JS lifecycle
    `source → tokens → ast → environment → evaluation`
    `[read: src/lib/study-lenses/embody/types.ts — "The five flat phases, in the specification's own order"; LifecyclePhaseOrder]`,
    rather than the reference's `'execution'`. A deliberate departure from
    HR-8's reference-names default, ruled by the human.
  - ⚠ Citation hazard, met once already: `embody/types.ts` matches TWO files.
    The greenfield region is `src/lib/study-lenses/embody/`; the read-only
    quarry `src/lib/embody/` still carries a SUPERSEDED lifecycle
    (`'realm' | 'parse:tokenize' | 'parse:ast' | 'creation' | 'evaluation'`)
    which cannot be corrected in place because the quarry is copy-never-modify.
    Cite the greenfield region by full path.
  - **Consequence carried OPEN, and it is P0-K's to resolve** (P0-K — the
    Phase-0 unit for the shared kind and handle contract): "as a bare runtime
    would" is not satisfied by the engine's **`'function'` execution path** for
    a **`script`** snippet. That path runs learner code as a function body
    (`new Function(...names, body)`, engine `worker/bootstrap.ts`) under an
    injected `"use strict"` prefix, while `facts.type` declares the parse goal a
    script — so top-level `var` and function declarations become locals rather
    than globals, a top-level `return` is legal where a real script would be a
    SyntaxError, and the prefix itself changes semantics a bare script would
    not. (The `'module'` path already runs a genuine ES module; the axis is the
    lens's to set and is deliberately distinct from `facts.type`.) Reaching true
    script semantics does NOT require solving globals injection — indirect eval
    `(0, eval)(code)` runs in global scope and takes globals by assignment. The
    live constraints: strict-mode eval gets its own variable environment;
    `importScripts` needs a classic worker the ESM bootstrap forbids; and
    per-run blob WORKER SCRIPTS were ratified out (§ run — protocol and
    infrastructure), though the module path's per-run blob URL for learner code
    is a different object and is live. Unresolved; HR-20 ratifies no execution
    path.

### Ceremony and sandboxes

- **`ceremony: full` (human ruling 2026-08-05, HR-13):** AR-1 · AR-2 · AR-3 on
  every un-skip · AR-4 on every increment · AR-5. Uniform within the level — no
  agent-side lightening.
- **AR-5 changeset form and baseline (human ruling 2026-08-05).** The review
  changeset is the campaign's OWN SHA LIST, never `baseline..HEAD` — foreign
  commits interleave faster than any range survives (DEV.md § Shared-worktree
  git mechanics). Baseline `1b516bd4`
  `[relayed: the retired AR-LOG's baseline record — recover via git show 7c93080c^:.planning-handoffs/evaluators-api-restoration/AR-LOG.md — which carried [measured: git rev-parse HEAD at the first post-approval write]]`.
  The SHA list accumulates in each commit body as it lands (the durable
  primary), mirrored in `~/.claude/plans/read-and-execute-the-rosy-sky.md` § ②
  RESUMPTION POINT.
- **Sandbox cadence and sandbox fidelity (human ruling 2026-08-06, HR-15):**
  sandbox pages are built EARLY in each evaluator chain and extended per
  increment, so every user-observable increment fires its own 🔍 checkpoint
  rather than one end-of-chain check; the declared 🔍 skip for variables is
  overridden. And the DEPRECATED port's sandbox pages are fidelity targets
  alongside the reference's — both lineages' page features are inventoried in
  this ledger's sandbox section, and the rebuilt pages carry the union.

- **E2 slots as the W4b run chain's opener (human ruling 2026-08-17).** The
  ratification's "joins the engine chain after W1.a" left the increment unowned
  once W1.a landed; it now opens the W4b run chain, beside its consumers (the
  run settlement mapping reads `phase`), per HR-22's own build-beside-consumer
  logic. P0-K declares only the two-value vocabulary (HR-20); the engine's
  try/catch split and the worker stop record's discriminant land at W4b with
  their own AR pair.
- **W2's Phase-0 🔍 override (human ruling 2026-08-17).** P0-K's artifacts are
  types, docs, and a type-contract test — no user-loadable surface — but its
  README carries two GFM tables and the table-integrity checker (CP-b) is
  unbuilt, so the human eyeballs the RENDERED README (VS Code markdown preview
  or an equivalent GFM renderer) between quality checks and the Phase-0 commit
  and confirms both tables render as tables, not paragraphs; a broken render
  blocks the commit. markdownlint's MD056 only inspects rows inside a recognized
  table, so this failure class is invisible to every mechanical gate.

- **G1 passes with the NM-twin lattice (human ruling 2026-08-18).** The Phase-0
  → Phase-1 gate over P0-K (`45a2b381`) is approved with one adjustment: the
  region's twin answer revises `none → machine`. The `none` on `45a2b381`'s
  settings line is the superseded prior record; the twin commit's own settings
  line carries the new answer. The lattice, human verbatim: "we can have
  different nm twin docs at each level. the region nm will just cover the
  generator and shared kind signature, blackboxing how code is actually
  evaluated. then each evaluator can have its own nm filling in that blackbox
  and modeling any machinery between the generator and evaluation (like counting
  iterations, filtering meta-data events from the generator, etc.)."
  Consequences: the region root owes `notional-machine.md`, bounded at the
  committed contract (it must not pre-answer P0-E's construction-seam questions
  — drain-policy ownership stays open); run and intercept each owe their own
  `notional-machine.md` as their Phase-0 units' 0.2 artifacts, each modeling its
  own blackbox fill; the retrofit's AR shape is DEV.md's 0.2 re-open rule
  applied — a scoped ar-1 re-run over README + twin, no ar-2 re-run (sketch and
  types unchanged).
- **W3.a mechanics (human rulings 2026-08-18).** The iteration-guard copy
  executes AFTER P0-E's Phase-0 commit and before P0-R ∥ P0-I; it lands as its
  own pathspec commit; and creating the region's `lib/` via the copy owes a MINI
  PHASE-0 PASS — a scoped ar-1 over the copied README before the commit lands.
  This rules the CP-b-precedent question the plan of record carried open.
- **Danger's deferral stands; its fit is named (human ruling 2026-08-18).** HR-1
  and the re-kinding deferral are reaffirmed at the W3 plan review: danger
  re-kinds in its own named future campaign — the deprecated region's exit
  condition — and its target signature is the same `Evaluator` envelope, as a
  result-only `ExecutionBase` handle (the kind designs this in: "a result-only
  evaluator is a legal evaluator"). The region NM names danger's blackbox
  opening (an iframe, not the engine sandbox) alongside run's and intercept's.
- **P0-E design rulings (human rulings 2026-08-18, at the unit's design review —
  four ar-1 rounds, verdicts PAUSE ×3 then CONSIDER, all resolved).** The batch
  drain's home is the LIBRARY: one internal drainer behind every batch ignition,
  relieving backpressure and exiting on events-exhaustion or the settle,
  whichever first; ask policy stays evaluator-side, read from the mode the
  source learns at `start(mode)`. The memoized settle IS the source's `result`
  promise, defect-routed. The mode latch: the ignition touch fixes the
  consumption mode for the handle's life (an after-batch iterator is already
  ended; a post-iterate batch touch subscribes to the settle). The library
  installs the widening (extras built over `{ cancel }` controls; descriptors
  and freeze order are the factory's; four reserved keys compile-checked). One
  factory, overloads over two named source types (`events` required vs
  `events?: never`). Disposal of the source's iterator on a settle is
  BEST-EFFORT — attempted once, never awaited (the out-of-band teardown pins
  name the suspended-pull deadlock route). Two HR-4 exceptions, each with its
  strength argument: `stop()` is NOT called on a post-settlement cancel (the
  quarry still invoked its cancelFunction after a natural end; here the settle
  route owns resource release, and a second teardown door would double-release)
  and NOT on an inert cancel (nothing started, nothing to stop; the quarry's
  fixture pinned the opposite on a never-started execution, and the transported
  assertion re-targets to a post-ignition cancel). Recorded here per HR-21;
  encoded at `src/lib/study-lenses/evaluators/lib/execution-handle/README.md`.
- **P0-R design rulings (human rulings 2026-08-18, at the unit's design review —
  ar-1 round 1 PAUSE, four rulings given in-session, remaining concerns accepted
  fix-all).** The error phase is carried ONLY where it varies — run's
  `'javascript'` arm; the timeout, iteration-limit, and io arms are mid-run by
  construction and carry none, the invariant stated in prose (HR-20 unchanged:
  two values, spelled `'evaluation'`). A mock's liveness is the CONSUMER's own
  obligation — the machinery's budget pauses across an io exchange and no layer
  installs a watchdog; a never-settling mock holds the run and `cancel()` is the
  exit (the handle library's source-liveness clause, restated at the one
  callback run's consumers author). `RunResult` is DISCRIMINATED on `outcome` —
  an HR-4 exception with its strength argument: runtime values are identical to
  the reference's flat shape, but each arm carries exactly the fields that exist
  for it (the deprecated port's own commitment), so a consumer narrowing on
  `outcome` never reads an absent field; every reference name is kept. The
  `'io'` error kind is an ADDITION carrying a named supersede of the reference's
  classification (the reference surfaced a mock's failure as the learner-shaped
  `kind: 'javascript'`): HR-9's unmocked-verb posture requires an answer
  distinguishable from the learner's own error, a mock's failure is the same
  io-layer lesson, and adding the union member later breaks exhaustive consumers
  while adding it now is free. The shared capability probe HOISTS to
  `evaluators/lib/capability-probe/` (mini Phase-0, the W3.a scoped-ar-1
  precedent): the deprecated port carried the Worker/SharedArrayBuffer probe
  byte-identical in both evaluators, and the hoist keeps run's and intercept's
  environment-refusal wordings from drifting; spec refusals stay per-evaluator,
  and whether the kind's refusal shape grows a discriminating field is P0-K's
  question, named open. The engine's seconds-default export is its own additive
  engine increment in the W4b run chain (beside E2): run's always-populated
  `options.seconds` echo imports the machinery's default rather than
  re-declaring it, and the export does not exist today. An io answer the
  transport channel cannot carry classifies as an io error, never a machinery
  defect; the io increment's constraint is that the check runs on run's side of
  the call boundary, before the wrapper returns. Recorded here per HR-21;
  encoded at `src/lib/study-lenses/evaluators/run/README.md`.
- **P0-R design rulings, second round (human rulings 2026-08-19, at the ar-1
  round-2 and scoped-module reviews).** The hoisted module is NAMED
  `environment-refusal` (three-way homonym: the region's compile-time type
  probes, local-llm's device capability probe, and the module's own export being
  a refusal, not a probe), at
  `src/lib/study-lenses/evaluators/lib/environment-refusal/`. The two-species
  refusal taxonomy is CANONICAL AT THE REGION ROOT — a committed-contract edit
  approved: the region README § Outcomes gains the species pair (environment
  refusal: the shared wording; spec refusal: the evaluator's own words), the
  glossary gains both entries, and the region `types.ts` refusal comment is
  amended so the shared wording no longer falsifies it (comment only; no type
  shape changed). The module SPLITS a pure decision leaf from the environment
  read: the leaf takes the two capability facts and words the refusal
  (exhaustive plain-fixture suite, wording pinned by exact equality, arm order
  pinned), the wrapper alone reads globals with one node-tier and one
  browser-tier row — the deprecated port's own pair — per DEV.md's
  cross-the-boundary-by-moving rule; the round-1 stub-based suite is superseded.
  The shared halt-payload SHAPE question (run's and intercept's worker setups
  shared 57 of 106 lines in the port — buildHaltAuthor, readCap, the halt
  record) is BANKED TO P0-I: run's Phase 0 declares its own `RunHalt` per the
  port precedent, and P0-I's design inventory decides the dedup with both units'
  halt needs visible. And the settlement precedence gains STEP 0: a
  consumer-ended run settles `'cancel'`, outranking even the io flag — a Stop
  pressed during an in-flight mock answers `'cancel'`, never an io lesson the
  presser did not ask for (the mirror order — mock fails first, cancel second —
  still answers `'io'`). Recorded here per HR-21; encoded at
  `src/lib/study-lenses/evaluators/lib/environment-refusal/README.md` and
  `src/lib/study-lenses/evaluators/run/README.md`.
- **P0-I design rulings (human rulings 2026-08-19, at the unit's design review —
  ar-1 round 1 PAUSE, four rulings given in-session, remaining concerns accepted
  fix-all).** The reference's `.ast` member departs HR-8's enumeration TWICE,
  both approved: the NAME becomes `entwined` (strength: run's `ast` is a real
  `Program`; keeping `ast` for an entwined record mints a
  same-name/different-thing collision inside one region) and the Promise wrapper
  retires — the member is EAGER (strength: the kind's gate guarantees entwining
  before main is driven; a promise is ceremony around a value the caller's facts
  already hold). The region root's echo enumeration and glossary are amended
  accordingly (the two-species committed-contract-edit precedent). The
  enclosing-fallback attribution gains the campaign's ONE sanctioned STACK-PARSE
  EXCEPTION: a throw inside a live wrapped frame is attributed by the wrap's
  recorded innermost call site, worker-side and honest; a throw with NO live
  wrapped frame — the reference's statement-level residual — takes its position
  from a stack parse at the halt author, the one place the never-a-stack-parse
  ruling is escalated (strength: the only alternatives were losing attribution
  the reference had, or a fallback with no input at all); the parsed position is
  in the SPLICED text's coordinates, and the conversion through the guard's
  splice offsets is the enrichment increment's named design question. The
  unmocked-console posture is RECORD-ONLY — nothing forwards to the host console
  (a supersede of the reference's native forwarding, strength: the lens renders
  the observation and the host console stays the machinery's); the worker traps
  the WHOLE console surface and the event's `method` is an open string with the
  nineteen standard names documented (HR-18's all-of-JavaScript ground — an
  exotic legal call records faithfully instead of failing learner-shaped), while
  the mock KEYS stay closed over the nineteen. The per-yield fee waiver is
  CONDITIONAL (trigger narrowed in the second round below): `yieldCharge: false`
  iff the spec carries an `iterations` cap, so loop safety always has exactly
  one named owner and pin intercept:495 stays retained for the uncapped case it
  was written about. Fix-all resolutions recorded with them: `next()` is a
  WRAPPER over the handle's own iterator, substituting the settled result
  whenever `done` is true (the iterator-identity half superseded below by the
  ruled library amendment); the fail door is plumbed through the machinery's own
  `fail(reason)` at the source's stop (so `'failed'` is reachable and maps to
  `'fail'`), and pre-ignition `fail`/`throw` speak `'fail'` through the
  evaluator-authored inert-settle thunk; `visitCounts` counts RECORDS (one per
  dialog at its record, mock-independent — the reference's semantics; the ask is
  a moment but not a record) while `eventsByNode` joins every event, asks
  included; loc-null events are EXCLUDED from both (the banked null-key policy,
  decided — honest absence over a sentinel bucket); intercept's io error record
  declares its own `source` field (a dialog verb or `console.<method>`) — run's
  classification argument transfers, run's field shape does not; the halt SHAPE
  stays per-evaluator (intercept's carries the attributed call site run's cannot
  honestly stamp) while the halt-AUTHOR dedup (buildHaltAuthor/readCap, the
  banked 57 shared lines) is deferred to the W4b chain openers where the first
  worker-setup lands, the anti-drift argument carried. SECOND ROUND (human
  rulings 2026-08-19, at ar-1 round 2): the handle library gains the
  SELF-ITERATION guarantee — a named amendment to the committed contract:
  `[Symbol.asyncIterator]()` answers one memoized iterator for the handle's
  life, so intercept's stepping members alias the iterator a later `for await`
  continues; the region NM's no-hang absolutes are amended to "on its own
  account" with the consumer's sanctioned hold named (and the two stale
  committed parenthetical examples — the handle library's echo example, the
  engine README's unconditional intercept-waiver example — swept in the same
  breath); the two `IoMocks` spellings are KEPT as per-evaluator reference
  names, the decision recorded (the quarry's newer `InterceptIoMocks`
  alternative considered and declined); the in-stream `'error'` event gains an
  optional `source` mirroring the settlement io record's field (io failures
  discriminated in-timeline; the defect kind stays settlement-only); the fee
  waiver's trigger narrows to a FINITE, POSITIVE cap (a cap that cannot trip is
  not an owner); and `step` is worker-minted, strictly increasing, NOT
  contiguous — a mocked dialog's ask consumes an ordinal the stream never
  delivers, adjacency being an array-order property of `events`. Recorded here
  per HR-21; encoded at `src/lib/study-lenses/evaluators/intercept/README.md`,
  `src/lib/study-lenses/evaluators/intercept/notional-machine.md`, the region
  root `README.md` and `notional-machine.md`, the handle library's
  `README.md`/`DOCS.md`/suite, and the engine `README.md`'s example.
- **W4 opening rulings (human rulings 2026-08-25, at the wave's opening
  check-in, recorded ledger-first).** The banked halt-AUTHOR dedup resolves
  SHARED: a parameterized shared author module in `evaluators/lib/` — the
  environment-refusal hoist's anti-drift argument prevails; the loc-stamping
  seam rides a parameter so intercept's attributed call site stays its own —
  owing the mini-Phase-0 scoped ar-1 per the W3.a precedent, consumed by both
  chains' worker setups; the per-evaluator halt SHAPES stay as ruled 2026-08-19.
  E2 and the seconds-default export execute BEFORE BOTH chains — a named
  sequencing deviation from the run-chain-only slotting (2026-08-17): it clears
  intercept's phase rows too and keeps the engine's public-API edits out of the
  parallel window. The seconds default exports as its own constant file
  (`lib/engine/default-seconds.ts`, one concept per file), imported by the
  engine and both evaluators — never a second copy of the number. And X1 fires
  no 🔍: the handle library has no user-loadable surface, confirmed by the human
  under the no-agent-declared-skips rule.
- **The guarded-worker-base design rulings (2026-08-25/26, at the module's
  scoped ar-1 — verdict PAUSE, eleven concerns, all resolved).** The shared
  module ruled at the W4 opening reshapes to ONE concept:
  `createGuardedWorkerBase(workerConfig, finish?)` in
  `evaluators/lib/guarded-worker-base/`, answering the guard globals and the
  halt author together — the cap reading and the guard construction fold in as
  in-file helpers, retiring the last wiring duplicated across the port's two
  setups (composition delegated by the human to the agent's judgment, this
  round, and decided for the one-concept shape). The halt core is a
  DISCRIMINATED UNION on `natural` — the throw arm's `phase` narrows to the
  engine's `HaltPhase`, the natural arm pins the empty members, and no mapper
  ever fabricates a phase. The finisher is GUARDED AT THE BUILDER — a throwing
  finisher degrades to the unfinished core, never a lost halt (intercept's
  stack-parse residual is the named first client). Fix-alls applied with them:
  the deprecated setups are cited as the deprecated kind (never "the
  reference"); iteration-guard's § Excludes sentences amended to name the shared
  reader; the module scoped to the GUARDED engine-backed evaluators (the
  quarry's variables halt — no trip, no count — is the counterexample that makes
  the exclusion real); compile probes pin both worker-config shapes and the
  units' deferred `phase` members; the measured shared-line count is 48, not the
  banked 57.

### The ratification, and what it settled (human ruling 2026-08-06)

One pass over every row. The bulk confirm covered all proposed rows, including
the two mechanisms that had been earmarked for the Phase-0 design gate — the
creation-inert carve-out and the accessor-based enrichment — so that gate keeps
only its design-review role. The eight escalated decisions:

- **Error `line`** — restore on intercept, which already carries a wrap-style
  `loc` richer than the reference's line; DEFERRED on run, which has no wrap
  layer, so run-side call-site instrumentation is a named future increment.
  Never a stack parse; the reference's own untested numbers are not the
  conformance target.
- **`phase: 'creation' | 'execution'`** — IN SCOPE as its own engine increment:
  the engine's single try/catch splits and the worker stop record gains the
  discriminant. Additive, with its own AR pair.
- **Clean-arm `iterationCount`** — ADDED, reference-plus: the total is already
  computed under always-splice, and it rides the results rather than the
  settlement floor.
- **In-stream `ErrorEvent`** — RESTORED: intercept's event union gains an error
  arm carrying `step` and `loc`, landing in stream order; the settlement keeps
  the structured form. Both, as the reference had.
- **Enclosing-fallback attribution** — RESTORED AST-side, via
  `entwined.byOffset` at stop time: cheap, exact, and never a stack parse.
- **Gate arms (`rejections`, formatting)** — resolved by the current
  architecture rather than restored: level marking is the ORCHESTRATOR's
  (screening plus the language level's validate plus orchestrate marking,
  live-wired) and embody is level-blind by contract (`embody/DOCS.md:101-105`,
  _"a language level decides what is ALLOWED"_); formatting is level-shipped
  editor-support DATA (`language-levels/README.md:46-48`) consumed by the
  orchestrator's editor adapter (`orchestrate/README.md:113`). Already recorded
  in those regions' own docs — nothing returns to evaluators.
- **`.fail` on run** — NOT added. The reference asymmetry is kept: fail is the
  mid-stream consumer stop, run yields nothing, and cancel covers its one stop;
  fail rides intercept and the tracers.

## Pin dispositions

The rebuild happens in a new directory, so a new-region build never trips the
pinned-guard on the deprecated region's settled expectations. This table is what
prevents their re-litigation. Inventory
`[measured: grep -n "// PINNED" over create-run-stream.test.ts (9 rows) and create-intercept-stream.test.ts (18 rows)]`,
paths relative to `src/lib/study-lenses/evaluators-deprecated/`. Disposition
vocabulary (closed): **retained** — the behavior carries into the new region and
the pin's authority travels with it; **re-scoped** — true of the iteration path
once the await path exists; **superseded** — the new region deliberately
contradicts it. No pin fell outside the vocabulary.

| file:line     | pinned claim (short)                                                                            | disposition | authority                                                                                                                                                                                                                              |
| ------------- | ----------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| run:103       | nothing engine-side before the first pull; result access starts a run                           | retained    | HR-6 keeps it — await counts as consumption, not an earlier start                                                                                                                                                                      |
| run:140       | teardown answers OUT OF BAND, never via generator return queueing                               | retained    | carried into the new handle design (the `.return()` sequencing)                                                                                                                                                                        |
| run:154       | a pull after teardown never starts a fresh run                                                  | retained    | —                                                                                                                                                                                                                                      |
| run:208       | guard increments before comparing; cap N trips at N+1                                           | retained    | iteration-guard transports byte-identical                                                                                                                                                                                              |
| run:217       | guards splice on the ORIGINAL source; trip span faithful                                        | retained    | HR-12 relies on it                                                                                                                                                                                                                     |
| run:235       | iterations rides through unchanged (no clamp/default/gate)                                      | retained    | —                                                                                                                                                                                                                                      |
| run:272       | the engine's refinement hook goes unused; the stop record is authored where the raw throw lives | retained    | —                                                                                                                                                                                                                                      |
| run:289       | no machine ran → no machinery cause is honest                                                   | retained    | defect taxonomy rides as an HR-8 addition                                                                                                                                                                                              |
| run:300       | restart guard: a handle-only guard misses the defect route                                      | retained    | human ruling 2026-08-05 (H-7)                                                                                                                                                                                                          |
| intercept:118 | nothing engine-side before the first pull                                                       | retained    | HR-6, as run:103                                                                                                                                                                                                                       |
| intercept:173 | both sources join ONE arrival queue in worker post order                                        | retained    | carried into the stream factory's rebuild                                                                                                                                                                                              |
| intercept:200 | the wrap stamps the innermost call site, end to end                                             | retained    | —                                                                                                                                                                                                                                      |
| intercept:208 | statement-level throw outside any wrap → loc null; NO stack parse                               | superseded  | ledger ratification 2026-08-06 — enclosing-fallback attribution restored AST-side via `entwined.byOffset`; the never-a-stack-parse half re-opened once, for the no-live-frame residual only (human ruling 2026-08-19, the P0-I bullet) |
| intercept:250 | the fake rejects an async round-trip (property of the double)                                   | retained    | engine + fake untouched                                                                                                                                                                                                                |
| intercept:265 | a pull after teardown never starts a fresh run                                                  | retained    | —                                                                                                                                                                                                                                      |
| intercept:292 | teardown out of band, never through the engine's stream exit                                    | retained    | the `.return()` sequencing builds ON it                                                                                                                                                                                                |
| intercept:309 | teardown LATCHES; a later pull is inert                                                         | retained    | aligns with HR-2                                                                                                                                                                                                                       |
| intercept:337 | the stream must be pulled for every event; one pull starts, not finishes                        | re-scoped   | HR-6 — true of the ITERATION path; the await path drains without consumer pulls                                                                                                                                                        |
| intercept:356 | guards splice FIRST on the original text                                                        | retained    | —                                                                                                                                                                                                                                      |
| intercept:361 | splice order not interchangeable (columns shift)                                                | retained    | —                                                                                                                                                                                                                                      |
| intercept:394 | iterations rides through unchanged                                                              | retained    | —                                                                                                                                                                                                                                      |
| intercept:443 | no machine ran → no machinery cause is honest, inherited                                        | retained    | —                                                                                                                                                                                                                                      |
| intercept:456 | assemble-defect settlement frozen outside the mapper                                            | retained    | —                                                                                                                                                                                                                                      |
| intercept:465 | restart guard, both modules                                                                     | retained    | human ruling 2026-08-05 (H-7)                                                                                                                                                                                                          |
| intercept:480 | an outstanding pull completes as the stream's end, any route                                    | retained    | —                                                                                                                                                                                                                                      |
| intercept:495 | yield charge named; loop safety rests on iterations                                             | retained    | the engine's yield-charge opt-out is the narrow fix that ruling anticipated, not a contradiction                                                                                                                                       |
| intercept:504 | flat charge arithmetic; a floor of 500 is meaningful                                            | retained    | same note                                                                                                                                                                                                                              |

## Open items carried into execution

- **The engine's yield-charge opt-out** — raised first by the semantics tracer,
  re-raised by intercept (human ruling 2026-08-04): the engine deducts a flat
  charge per yielded event against a 5-second default, so a densely emitting
  program times out with almost no real runtime. The ruling was "budget for it,
  and treat the narrow fix as the engine's": emit everything, name the cost,
  rest loop safety on the iterations cap. The opt-out on the engine spec is this
  campaign's first engine increment; both intercept and trace/variables qualify
  for it. **DISCHARGED 2026-08-11 by `976baed9`** — `EvaluateSpec.yieldCharge`,
  optional and defaulting to true, waives the fee alone: the budget still pauses
  for every yield-wait and every serviced call, and real elapsed time still ends
  the run. Neither evaluator threads it through yet; that rides each rebuild.
- **`evaluators-deprecated/PHASE-1-HANDOFF.claude-delete-if-stale.md` is KEPT**
  (human ruling 2026-08-04). Untracked and never committed at ruling time, so
  deletion would have been unrecoverable (now TRACKED — swept into the human's
  `90c31797` checkpoint, 2026-08-11); 29 of its 148 lines are tracer material —
  quarry prior art, the variable/binding/environmentDiff event design, and open
  decisions for later sprints. It rides any region move untouched, and the
  variables phase is its natural reader.
