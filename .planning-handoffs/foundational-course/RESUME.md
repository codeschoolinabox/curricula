# Superseded

This handoff has been merged into
[`../computational-thinking/RESUME.md`](../computational-thinking/RESUME.md).

The two were written as sibling campaigns during the session that produced them.
They are not siblings: the `computational-thinking/` directory **is** this
course's theory, so a successor reading one without the other gets half a
campaign. The merged file carries both halves.

Nothing here is lost — every section moved. This directory can be deleted; it is
left in place only because the agent that merged them cannot remove files.
