const text = 'hello';
const len = text.length;
const first = text[0];
const last = text[text.length - 1];
const middle = text[2];
console.log(len);
console.log(first);
console.log(last);
console.log(middle);
