# Function Event Generators

Two generators for built-in function call/return events. In JEJ, learners call
but don't write functions — this covers Number(), Math.floor(), prompt(),
console.log(), string methods, etc.

## Files

- `create-function-call-event.ts` — function called with name and args
- `create-function-return-event.ts` — function returns with name and value
