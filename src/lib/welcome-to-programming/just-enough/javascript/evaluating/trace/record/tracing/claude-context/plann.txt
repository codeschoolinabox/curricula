


thoughts?  discuss!  Push me!   mapping out the trace events is _CRITICAL_!  This signature is how consuming learning environments will know what data to expect

reconstructing what was lost:
- it needed to instrument while loops no matter what for loop guarding, but only sent on the events if the config was on 
- do not instrument inside of a with statement
- actually use module instead of 'use strict' in script?
- need the full weave 
- was full bundle easier for setup?


other things I think we did wednesday night that might need more re-implementaiton
- not allowing multiple variable assignments on one line 
- ... ?




- labels shouldn't be in the config. they should always be traced if they're present which is not a big deal, it's just an extra field not a full event
- what is propertyCoercion?  whatever it is it likely doesn't belong in config.  we will trace coercion by default, with no option to disable it 


