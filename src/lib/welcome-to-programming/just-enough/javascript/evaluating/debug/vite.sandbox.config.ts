/**
 * @file Vite dev server config for the debug sandbox.
 *
 * Usage: npx vite --config src/lib/just-enough-javascript/evaluating/debug/vite.sandbox.config.ts
 */

import { defineConfig } from 'vite';

export default defineConfig({
	root: 'src/lib/just-enough-javascript/evaluating/debug',
});
