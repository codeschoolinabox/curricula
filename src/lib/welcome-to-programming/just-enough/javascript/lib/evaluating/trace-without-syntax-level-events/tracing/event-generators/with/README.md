# With Event Generator

Creates `WithEvent` objects for the `with` statement — an undocumented easter
egg. When active, the program runs in script mode instead of module mode.

## `createWithEvent`

**Inputs:** event (enter/leave), the object being `with`'d.
