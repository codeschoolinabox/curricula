1.  expanded into syntax-level tracer 
    todo: refactor /evaluating to 
        /evaluating 
            /run - rewrite, it should:
                accept async mocks for I/O 
                have a max seconds & iterations (light-weight loop guards)
                and a cancel handle
                but no event emission or resolve data, all data collection / program visualization is done through async I/O mocks
            /debug - rewrite, it should:
                just add optional loop guards and open it in the debugger
                no mocks, no traces, no resolves
            /trace - two full tracers, /syntax being a layer of abstraction around /semantics
    
                /syntax 
                /semantics
2. todo, told it the 3rd dimension will be syntax features not langauge levles
3. in progress, told 