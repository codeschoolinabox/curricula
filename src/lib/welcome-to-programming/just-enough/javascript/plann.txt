status as of lsat closing
    error interpreter should be ready
    micro-decisions is in progress 
    still figuring out how to sandbox-test the tracer (also getting some internal errors)
    docusaurus sidebar work to go


this directory will have everything for a full-feature run/trace/debug JS editor
    dynamic
        - errors are auto-passed to error interpretation and hints suggested
        run/debug 
        trace + config + pop-up scrollable rendering
        popup trace tables
    static
        parse -> gutter errors
        JEJ validation -> gutter alerts
        hints -> gutter warnings 
        auto-completes 
        pop-up docs
        editor
    control panel 
        [format]
        [table][options]
        [lenses v] (dropdown?)
        dynamics are greyed out if the code isn't formatted or has parse errors:
            [run]
            [debug]
            [trace][options]
            [predict][lines,variables,operators,] -> a self-checking trace table configured for only variables
            [open in][options] (iframe in-place like in demo curriculum)


static or other lenses can be stored elsewhere