/**
 * @file Public entry for the `study` lens component.
 *
 * The swizzled `src/theme/MDXComponents.js` imports from this path;
 * resolving via `index.ts` keeps the import stable against future
 * file renames inside this directory.
 *
 * Uses extensionless imports — webpack (Docusaurus bundle) resolves
 * `.tsx` automatically; vitest does the same via its Vite transform.
 */

export { default } from './study-lens';
export type { StudyLensProps } from './study-lens';
