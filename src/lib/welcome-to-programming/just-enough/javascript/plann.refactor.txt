dev journies for this refactor
    -> contextualized to building Welcome to Programming
    parse markdown files and replace all codeblocks with jej-compliant code with a study view 
    for the logic inside react components
    use function interfaces in vibe-coded exercises
    use the default object for easy code analysis

is and isn't 
    is 
        core language support for jej 
        static and dynamic code/program data collection
        component buttons for doing ^ in learning environments
    n't 
        static study lenses (parsons, blanks)
        learning environment 
        pedagogical framework
        editing 
        code/program editing view

/just-enough-javascript - proposed refactor
    - include some features to address audiences and their treatment?
    /api
        - chains things together into full LE use cases
        run(code, seconds) -> resolving stream
        trace(code, seconds)-> resolving stream
        debug(code, iterations) -> resolving stream
        hint(code, options?) -> misconceptions/smells + their hints  (rename?)
        validate(code) -> list of all features w/ stats + compliance report  (rename this function?) (current rejections)
        format(code) -> formatted code 
        default: a function that takes code and returns an object of functions/getters acting on the saved code
            -> all are pure and closed so they can be destructured
            .code <- getter & setter.  used internally by other funcs and getters
                set: a string or function with no args
                get: if string return string, if function return function's return value
            .formatted <- same as .code but formats it first
            .hints, .validations (rename?) <- returns current hints and rejections
            .isJej <- validates and returns a boolean
            .run(max), .trace(max), .debug(max) 
    /evaluating
        promise -> sync
        /run & /trace emit an event stream for realtime visualization
        /debug too, but just for errors (nothing else is intercepted for "native" devtools experience)
        promise resolves to full history array (like now)
    /formatting - jej convention formatting
        function wrapping prettier
        basically standard prettier style, but with tabs
    /hinting - misconceptions and smells
        - currently warnings in /verify-language-level
        distinction between code smells (style) and misconceptions
            where a linter assumes someone made a bad choice, ours assumes you made a confusion
        misconceptions
            using assignment in a check is an assignment/comparison misconception
            while () {} else {}
        captures syntax/parsing errors and suggests hings
        can use some eslint or plugin rules internally
    /validating? - checks for disallowed language features
            - currently rejections in /verify-language-level
            /validate 
            /isJej <- runs validate then returns true or false if 0 rejections
    reference.md
    README.md - what it is + the non-language-feature parts of current reference.md