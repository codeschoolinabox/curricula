/**
 * @file The level's human-facing name — shown where a learner picks a level, and
 * the value that satisfies the spine `LanguageLevel.label`.
 */

const LABEL = 'Just Enough JavaScript';

export default LABEL;
