# Fixture: `no-lenses-json/`

Used by `resolve-cascade.test.ts` Increment A.1 ("Zero — no `lenses.json`
anywhere in the cascade"). This directory intentionally contains no
`lenses.json` file at this level or any ancestor under the `fixtures/` root the
test considers.

Do not add a `lenses.json` file here or anywhere under this directory — doing so
breaks A.1's precondition.
