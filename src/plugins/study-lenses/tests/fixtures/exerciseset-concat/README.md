# Fixture: `exerciseset-concat/`

Used by `resolve-cascade.test.ts` Increment A.10 ("Interface —
`exerciseSetPrefixes` array-concat across cascade"). Root sets
`["sl-"]`; chapter adds `["es-"]`. Resolved at chapter must yield
`["sl-", "es-"]` with root-first ordering preserved.
