# Fixture: `no-js-files/`

Used by `discover-siblings.test.ts` B.2 ("mode !== 'off' but no `.js`
files anywhere under pageDir → frozen []"). The fixture contains no
`.js` files at any depth — only this README.
