# Fixture: `single-js-in-pagedir/`

Used by `discover-siblings.test.ts` B.4 ("single `.js` directly in
pageDir + `defaults.js` configured → one `Sibling`"). Contains one
`.js` file directly alongside this README. The walker must discover
it, read its contents, and annotate with `label='greeting'`,
`lang='js'`, `lens='study'`.
