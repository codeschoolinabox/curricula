// @ts-nocheck
// Fixture file used by discover-siblings.test.ts B.3. Content is
// irrelevant — the test asserts the walker skips this file when the
// resolved config has no defaults.js entry.
console.log('hello');
